# Spec — Patterns to Port from Qiuth into Proxilion

**Status:** Proposal. Companion to [spec.md](spec.md). Scope is bounded to four discrete patterns lifted from the sibling project at [qiuth-main/](../../qiuth-main/). The two codebases share no runtime — qiuth is a zero-dep TypeScript library for API-key MFA; Proxilion is a Rust OAuth reverse proxy. What transfers is **shape**, not code.

**Author intent:** Adopt the four patterns below as they unblock real, already-flagged gaps in Proxilion: env-var sprawl in [config.rs](../../crates/proxy/src/config.rs), a binary-feeling `Decision` enum, missing stable error IDs, an uncached YAML reload at startup, and integration-only test coverage.

---

## Table of Contents

1. Why these four
2. Pattern 1 — Layered config loader with a fluent builder
3. Pattern 2 — `PolicyTrace` with per-layer outcomes
4. Pattern 3 — Canonical error-code registry
5. Pattern 4 — `PolicyLoader` trait with compiled cache
6. Pattern 5 — Coverage threshold gate in CI
7. Rollout order & dependencies
8. Out of scope (explicit non-goals)

---

## 1. Why these four

Each pattern maps to a Proxilion gap documented during the repo survey:

| Proxilion gap | Qiuth pattern | Section |
|---|---|---|
| Env-only config with hardcoded fallbacks scattered through [config.rs:52-102](../../crates/proxy/src/config.rs#L52-L102); no programmatic embed path | Fluent builder + layered loader | §2 |
| `Decision::Allow \| Block \| RequireConfirmation` ([decision.rs:5-20](../../crates/policy-engine/src/decision.rs#L5-L20)) tells you *what* but not *why which layer* | `LayerValidationResult[]` with per-layer error type | §3 |
| `AppError` ([adapters/error.rs:9-47](../../crates/proxy/src/adapters/error.rs#L9-L47)) has codes hardcoded inline; no stable contract for operators | `ValidationErrorType` enum as the canonical registry | §4 |
| Policy YAML reparsed every server start; no caching, no pluggable backend | `ConfigLookupFunction` indirection + compiled cache | §5 |
| Integration tests only; no enforced coverage floor | vitest 90% threshold pattern, ported to `cargo llvm-cov` | §6 |

The four are independent and can land in any order, but §3 and §4 share a type and should ship together.

---

## 2. Pattern 1 — Layered config loader with a fluent builder

### 2.1 What qiuth does

[qiuth-main/src/config/config-builder.ts:25-279](../../qiuth-main/src/config/config-builder.ts#L25-L279) defines `QiuthConfigBuilder` — a chainable builder with one `.withX()` method per concern, a `.build()` finalizer that throws on missing required fields, and a static `validate()` that catches semantic errors (e.g. `HMAC secret must be at least 32 characters`).

Notable mechanics:
- `withApiKey()` hashes on the way in so the unhashed key never reaches the config struct
- `withXConfig()` siblings let callers pass a fully-formed sub-config when they need the long-form
- `from(existing)` lets you mutate an already-built config — important for tests
- `createConfig()` factory function for callers who prefer the functional entry point

### 2.2 Proxilion translation

Replace `Config::from_env()` ([config.rs:51-102](../../crates/proxy/src/config.rs#L51-L102)) with a layered loader:

```
defaults  →  optional TOML/YAML file  →  env vars  →  programmatic overrides
```

Public API sketch (in `crates/proxy/src/config/`):

```rust
pub struct ConfigBuilder { /* private */ }

impl ConfigBuilder {
    pub fn defaults() -> Self;
    pub fn from_file(path: impl AsRef<Path>) -> Result<Self, ConfigError>;
    pub fn from_env(self) -> Result<Self, ConfigError>;

    pub fn with_bind_addr(self, addr: SocketAddr) -> Self;
    pub fn with_trust_plane_url(self, url: impl Into<String>) -> Self;
    pub fn with_policy_path(self, path: PathBuf) -> Self;
    pub fn with_token_encryption_key_hex(self, key: impl Into<String>) -> Self;
    // ... one per current field

    pub fn build(self) -> Result<Config, ConfigError>;
}

impl Config {
    /// Convenience entry: defaults → file (if PROXILION_CONFIG_FILE set) → env.
    pub fn load() -> Result<Self, ConfigError>;
}
```

Validation moves into `build()` and gains semantic checks that env-only loading can't easily express:
- `token_encryption_key_hex` is exactly 64 hex chars when present
- `trust_plane_url` parses as `http(s)://`
- `policy_path` exists when set (don't silently fall back to empty policy)
- `dev_mode = false` requires both cert and key paths to resolve

### 2.3 Why this matters now

Two concrete payoffs:

1. **Programmatic embed path.** §5/§6 of [spec.md](spec.md) (and any future SDK consumer) want to construct `Config` without setting env vars. Today that's impossible without monkeypatching `std::env`.
2. **Validation moves out of the operator's runtime.** Today, malformed `PROXILION_TOKEN_ENCRYPTION_KEY` is caught only when the cipher runs. A builder rejects it at boot with a clear error.

The `LogFormat::Pretty | Json` field that's currently `#[allow(dead_code)]` ([config.rs:17-18](../../crates/proxy/src/config.rs#L17-L18)) gets wired up as part of this refactor — the builder is the natural seam.

### 2.4 Migration plan

- Phase 1: introduce `ConfigBuilder`, have `Config::from_env()` call into it. No behavior change.
- Phase 2: add `from_file()` and `Config::load()`. Document the precedence order.
- Phase 3: remove `Config::from_env()` once callers are migrated.

### 2.5 Status (2026-05-13) — Phases 1, 2, & 3 shipped.

- [crates/proxy/src/config.rs](../../crates/proxy/src/config.rs) — new `ConfigBuilder` with `defaults()`, `from_env_layer()` (composes env vars on top), `with_*` overrides for every field, and `build()` (runs semantic validation, then constructs `Config`). `Config::from_env()` now delegates to `ConfigBuilder::defaults().from_env_layer()?.build()` — byte-identical with the prior loader, just refactored. `Config::load()` is the forward-looking convenience entry; today it aliases `from_env`, phase 2 will layer `PROXILION_CONFIG_FILE` underneath.
- New `ConfigError::InvalidValue { field, reason }` variant carries the field name (e.g. `PROXILION_TOKEN_ENCRYPTION_KEY`) so the operator sees the env var that's wrong, not just "bad value somewhere."
- Semantic validation now runs in `build()`:
  - `token_encryption_key_hex` is exactly 64 hex characters when present (rejects truncated keys at boot rather than at first cipher use).
  - `trust_plane_url` + `federation_bridge_url` must start with `http://` or `https://`.
  - `dev_mode == false` still requires both cert + key paths to exist (unchanged behavior).
- Tests: 6 new in `config::tests` covering defaults-in-dev-mode, key-too-short rejection, valid-key acceptance, non-http URL rejection, cert-required-when-not-dev-mode, and programmatic override composition (`with_bind_addr` + `with_database_url` + `with_policy_path` chain cleanly).

**Phase 2 additions (2026-05-12).**

- [crates/proxy/src/config.rs](../../crates/proxy/src/config.rs) — `ConfigBuilder::from_file(path)` parses a TOML file into a flat `FileConfig` struct (`#[serde(deny_unknown_fields)]` so typos fail loudly) and layers each set field on top of the builder's current values. Every field is optional; absent fields leave the prior value intact. Field names mirror the env-var conceptual model (snake_case, without the `PROXILION_` prefix) — `bind_addr = "..."` corresponds to `PROXILION_BIND_ADDR`.
- [crates/proxy/src/config.rs](../../crates/proxy/src/config.rs) — `Config::load()` is now the production entry point: `defaults() → optional from_file($PROXILION_CONFIG_FILE) → from_env_layer() → build()`. Env vars layer on top of file values, so operators can override file-based config without editing the file. New `ConfigError::FileLoad { path, reason }` covers both read and parse failures.
- [crates/proxy/src/main.rs](../../crates/proxy/src/main.rs) — boot path switched from `Config::from_env()` to `Config::load()`. `from_env` is kept for back-compat under `#[allow(dead_code)]` so existing callers in tests / embed paths don't break.
- Tests: 3 new in `config::tests` covering file-overrides-defaults, unknown-field-rejection, and missing-path failure.

**Deviation.** Chose TOML over YAML: ops config (small, flat, comments) reads cleanly in TOML; YAML's anchors / multi-line strings aren't load-bearing here and `policy.yaml` keeps its YAML stack. The `policy_path` field still points at a YAML file — just the proxy's own knobs are TOML.

**Phase 3 (2026-05-13).** `Config::from_env()` removed from [crates/proxy/src/config.rs](../../crates/proxy/src/config.rs). The Phase 2 backward-compat shim — kept under `#[allow(dead_code)]` while callers migrated — had zero remaining call sites (a workspace grep for `Config::from_env(` returned only the definition itself and its own docstring). `Config::load()` is now the single production entry point; embed/test callers use `ConfigBuilder::defaults()…build()` directly. Module docstring updated; `cargo build --workspace` and `cargo test --workspace` both clean.

**`config/proxilion.example.toml` shipped (2026-05-13).** Phase 2 added TOML support to the proxy without a worked example for operators to copy from. The new [`config/proxilion.example.toml`](../../config/proxilion.example.toml) documents every `FileConfig` field (network/TLS, datastore, upstream, observability, token encryption + OAuth, policy, NATS, SIEM, blocked-action webhook, operator auth) — every field is commented out and annotated with its default plus a one-line explanation, so an operator copies the file and uncomments only what they want to override. The header documents the precedence chain (`defaults → file → env → programmatic`). A new `config::tests::example_toml_parses_with_defaults_only` unit test in [crates/proxy/src/config.rs](../../crates/proxy/src/config.rs) pins the contract — when every field is commented out the loader produces a builder identical to `defaults()` — so adding a new required field to `FileConfig` (or accidentally activating a comment-out) trips the gate.

**`LogFormat` wired through (2026-05-13).** Closes the §2.3 follow-through ("the `LogFormat::Pretty | Json` field that's currently `#[allow(dead_code)]` gets wired up as part of this refactor"). [crates/proxy/src/main.rs](../../crates/proxy/src/main.rs) — `main()` now loads `Config` *before* `init_tracing`, then passes `cfg.log_format` into `init_tracing(format: config::LogFormat)`. The previous body read `PROXILION_LOG_FORMAT` directly via `std::env::var`, bypassing the layered config and giving operators no way to set `log_format` from `proxilion.toml`. With the change, the precedence chain (`defaults → TOML file → env vars → programmatic overrides`) applies to log formatting too — `log_format = "pretty"` in the TOML file now works as the doc implied. The `#[allow(dead_code)]` annotation on `Config::log_format` is dropped (`cargo clippy --workspace -- -D warnings` clean with `-A clippy::type_complexity -A clippy::too_many_arguments -A clippy::result_large_err`, the CI flag set). Config-load errors still go to stderr — tracing isn't up yet at that boot phase.

---

## 3. Pattern 2 — `PolicyTrace` with per-layer outcomes

### 3.1 What qiuth does

[qiuth-main/src/core/authenticator.ts:61-131](../../qiuth-main/src/core/authenticator.ts#L61-L131) returns a `ValidationResult` containing a `layerResults: LayerValidationResult[]` array. Each entry names the layer (`SecurityLayer.IP_ALLOWLIST`, `TOTP_MFA`, `CERTIFICATE`, `HMAC`), a `passed: bool`, and on failure an `error` string + a stable `errorType: ValidationErrorType` enum value. The orchestrator runs fail-fast but the array preserves *every* layer it evaluated up to (and including) the failing one.

This is the bit worth porting: **the decision is a structure, not a verdict**. An operator looking at a denied call learns which check tripped without reading the application log.

### 3.2 Proxilion translation

Today, Proxilion adapters consume a `Decision` ([decision.rs:5-20](../../crates/policy-engine/src/decision.rs#L5-L20)) and either pass or surface an `AppError`. Two layers can deny — **Layer A** (PIC ops enforcement, [ops.rs](../../crates/policy-engine/src/ops.rs)) and **Layer B** (Rego/YAML content rules, [rego.rs](../../crates/policy-engine/src/rego.rs)) — plus read-filter quarantining ([read_filter.rs](../../crates/proxy/src/adapters/read_filter.rs)). The current return type collapses them.

Introduce `PolicyTrace`:

```rust
#[derive(Debug, Clone, Serialize)]
pub struct PolicyTrace {
    pub correlation_id: String,
    pub evaluated_at: DateTime<Utc>,
    pub duration_ms: u64,
    pub layers: Vec<LayerOutcome>,
    pub final_decision: Decision,
}

#[derive(Debug, Clone, Serialize)]
pub struct LayerOutcome {
    pub layer: PolicyLayer,                  // LayerA | LayerB | ReadFilter
    pub passed: bool,
    pub matched_rule_id: Option<String>,     // policy_id, rego rule, pattern id
    pub error_code: Option<ErrorCode>,       // see §4
    pub detail: Option<String>,              // human-readable, not stable
}

#[derive(Debug, Clone, Copy, Serialize)]
#[serde(rename_all = "snake_case")]
pub enum PolicyLayer {
    LayerA,        // PIC ops invariants
    LayerB,        // YAML/Rego content rules
    ReadFilter,    // response-body quarantine
}
```

The policy engine's public eval function returns `PolicyTrace` instead of `Decision`. Callers extract `trace.final_decision` for the gate; the full trace is:

1. Logged (one structured event per request, replacing the current scattered `warn!`/`error!` calls in [error.rs:108-112](../../crates/proxy/src/adapters/error.rs#L108-L112))
2. Returned to the dashboard API for the request-detail view
3. Optionally included as `X-Proxilion-Trace-Id` on responses (the ID, not the body — never leak rule contents to the caller)

### 3.3 Fail-fast vs. full-trace

Qiuth is fail-fast — the array stops at the first failure. Proxilion should follow the same default for Layer A (PIC invariants are non-negotiable; no point evaluating Layer B if A denies) but **continue through Layer B even after the first Block** so operators see overlapping rules. This costs a few microseconds and pays for itself the first time someone debugs "why did THIS rule fire and not the one I expected."

Implementation note: gate this with a `PolicyEvalMode::FailFast | Comprehensive` on the engine — `FailFast` for production hot path, `Comprehensive` for dashboard-driven "explain this denial" replays.

### 3.4 Status (2026-05-12) — types + engine entry + adapter wiring shipped.

- [crates/policy-engine/src/trace.rs](../../crates/policy-engine/src/trace.rs) — new module with `PolicyTrace`, `LayerOutcome`, `PolicyLayer` (LayerA / LayerB / ReadFilter), `OpsAtomView`, `PolicyEvalMode`. `LayerOutcome::error_code` carries the canonical `shared_types::ErrorCode` from §4. `PolicyTrace::allowed()` is `true` only when every layer passed AND the final decision is `Allow`.
- [crates/policy-engine/src/rego.rs](../../crates/policy-engine/src/rego.rs) — new `Engine::evaluate_with_trace(&ctx)` sibling to `evaluate(&ctx)`. Returns `(Outcome, PolicyTrace)` so callers that don't need the structured trace pay nothing. The trace fills in Layer A (engine-side, `passed: true`, records the required-ops count), Layer B (translates `Decision::{Allow, Block, RequireConfirmation, RateLimit}` to the matching `ErrorCode`), and an optional ReadFilter slot when a filter is configured (left as `passed: true; scan pending` for the adapter to mutate after the response body comes back).
- [crates/policy-engine/Cargo.toml](../../crates/policy-engine/Cargo.toml) — adds `chrono`, `uuid` (already in the workspace).
- Tests: 4 new in `trace::tests` + 3 integration tests in `crates/policy-engine/tests/policy_trace.rs` that exercise the engine with the live `config/policy.yaml`. Covers (a) Layer-B block via `gmail-external-send-gate` records `ErrorCode::PolicyBlocked` + the matched policy id, (b) no-policy-match path emits Layer A + Layer B both passed, (c) `drive-injection-filter` produces a ReadFilter slot with `passed: true` pending the adapter scan.

**Adapter wiring additions (2026-05-12).**

- [crates/proxy/src/adapters/policy_trace.rs](../../crates/proxy/src/adapters/policy_trace.rs) — new helper module. `mark_layer_a_failed(trace, detail)` rewrites the Layer-A slot on Trust-Plane refusal with `ErrorCode::PicInvariantViolation`. `mark_read_filter(trace, blocked, policy_id, detail)` rewrites the ReadFilter slot after the response body scan — `blocked=true` flips it to `failed` with `ErrorCode::ReadFilterBlocked`, otherwise it stays `passed` with a quarantine-sample count in `detail`. `emit(trace, request_id, vendor, action)` logs a single structured event per request — `tracing::info!` when allowed, `tracing::warn!` when denied — carrying `trace_id`, a one-line `summary` (`layer_a=ok,layer_b=policy_blocked,...`), and the serialized trace JSON. Replaces the prior scattered `warn!`/`error!` calls along the deny paths.
- [crates/proxy/src/adapters/google_drive.rs](../../crates/proxy/src/adapters/google_drive.rs), [google_gmail.rs](../../crates/proxy/src/adapters/google_gmail.rs), [google_calendar.rs](../../crates/proxy/src/adapters/google_calendar.rs) — all three switched from `policy.evaluate(&ctx)` to `policy.evaluate_with_trace(&ctx)`. Each adapter now: (a) emits the trace on Layer-B deny, (b) mutates Layer A → failed + emits on Trust-Plane refusal, (c) mutates ReadFilter to `passed`/`failed` after the scan + emits on read-filter block, (d) on the happy path inserts `x-proxilion-trace-id: <uuid>` alongside the existing `x-proxilion-request-id` / `x-proxilion-pca-id` / `x-proxilion-policy` headers and emits an INFO trace at the end. The `trace_id` is the only piece surfaced to the caller — rule content stays inside the proxy.
- Tests: 3 new in `adapters::policy_trace::tests` covering Layer-A replacement, ReadFilter append-when-absent, and the summary string.

**Remaining deviations.**

1. **Engine still exposes `evaluate(&ctx) -> Outcome` unchanged.** The trace-less entry point is still the public API for callers that don't need the structured trace; the three Google adapters now use `evaluate_with_trace` exclusively, but unit tests and any future embed callers can stay on the lighter path.
2. ~~`PolicyEvalMode::{FailFast, Comprehensive}` defined but not yet observed.~~ **Resolved 2026-05-12.** [`Engine::evaluate_with_trace_mode(&ctx, PolicyEvalMode::Comprehensive)`](../../crates/policy-engine/src/rego.rs) walks every later policy after the first match and appends one extra Layer-B [`LayerOutcome`](../../crates/policy-engine/src/trace.rs) per "would-also-have-matched" rule, with `detail` prefixed `would_also_match:` so a downstream renderer can distinguish primary from diagnostic outcomes. The `final_decision` stays authoritative from the first match — overlaps are purely informational. `evaluate_with_trace` continues to default to `FailFast` (the hot-path entry point); the dashboard's explain-this-denial replay flips to `Comprehensive`. Verified by the new `comprehensive_mode_records_would_also_match_diagnostics` test in [crates/policy-engine/tests/policy_trace.rs](../../crates/policy-engine/tests/policy_trace.rs).

---

## 4. Pattern 3 — Canonical error-code registry

### 4.1 What qiuth does

[qiuth-main/src/types.ts](../../qiuth-main/src/types.ts) defines `ValidationErrorType` as an exhaustive enum: `IP_NOT_ALLOWED`, `MISSING_TOTP_TOKEN`, `INVALID_TOTP_TOKEN`, `MISSING_SIGNATURE`, `EXPIRED_TIMESTAMP`, `INVALID_SIGNATURE`, `MISSING_HMAC`, `INVALID_HMAC`, `INTERNAL_ERROR`. Every failed `LayerValidationResult` carries one. The string values are stable — they're part of the library's public contract.

### 4.2 The Proxilion gap

[adapters/error.rs:76-88](../../crates/proxy/src/adapters/error.rs#L76-L88) already has a `code(&self)` method that returns string codes like `"policy_blocked"`, `"pic_invariant_violation"`, `"read_filter_blocked"`. **The codes exist but they're not a registry** — they're inline literals on one method, not enumerated anywhere, not documented as stable, not used by anything outside that one `IntoResponse`.

### 4.3 Port

Define `ErrorCode` as a `#[non_exhaustive]` enum in a new `crates/proxy/src/errors.rs` (or co-locate in `shared-types/` if other crates need it):

```rust
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash, Serialize, Deserialize)]
#[serde(rename_all = "snake_case")]
#[non_exhaustive]
pub enum ErrorCode {
    // Policy denials (Layer A — PIC)
    PicInvariantViolation,
    PicAuthorityExceeded,
    PicChainExpired,

    // Policy denials (Layer B — content rules)
    PolicyBlocked,
    PolicyRequireConfirmation,
    PolicyRateLimited,

    // Read filter
    ReadFilterBlocked,
    ReadFilterQuarantined,

    // Upstream
    UpstreamUnavailable,
    UpstreamTooLarge,

    // System
    PolicyEngineError,
    DatabaseError,
    InternalError,
}

impl ErrorCode {
    /// Stable wire string. NEVER change once published.
    pub fn as_str(&self) -> &'static str { /* match arm per variant */ }

    /// Default HTTP status. Adapters may override.
    pub fn default_status(&self) -> StatusCode { /* ... */ }
}
```

Rules of the registry:

1. **Stable forever.** Once a code ships, its string value never changes. Add new variants; never rename.
2. **`#[non_exhaustive]`** so adding variants isn't a breaking change for downstream matchers.
3. **One source of truth.** `AppError::code()` returns an `ErrorCode`, not a string. The string mapping lives on the enum. `LayerOutcome.error_code` (§3) uses the same type.
4. **Documented in [docs/error-codes.md](../error-codes.md).** Auto-generated would be nicest; a hand-curated table is fine to start. Every code lists: stable string, default HTTP status, when it fires, suggested operator action.
5. **Test:** a snapshot test asserts the full `(variant → string)` mapping. Anyone trying to rename a code will break CI loudly.

### 4.4 Why now

The dashboard work (§1.6 of [spec.md](spec.md)) needs to render denials. Without `ErrorCode`, every dashboard improvement risks coupling to whatever string literal happened to be in `error.rs` that week.

### 4.5 Status (2026-05-12) — shipped.

- [crates/shared-types/src/error_code.rs](../../crates/shared-types/src/error_code.rs) — `ErrorCode` enum, `#[non_exhaustive]`, serde-derived `snake_case` wire form. `as_str()` returns `&'static str` (stable) and `default_status()` returns the recommended HTTP status. Variants in scope: `PicInvariantViolation`, `PolicyBlocked`, `RequireConfirmation`, `RateLimited`, `ReadFilterBlocked`, `UpstreamUnavailable`, `UpstreamTooLarge`, `PolicyEngineError`, `DatabaseError`, `InternalError`. Snapshot test `wire_strings_are_stable` pins the full mapping — renaming a string fails CI loudly.
- [crates/shared-types/Cargo.toml](../../crates/shared-types/Cargo.toml) — adds `http` for `StatusCode` constants (already in the workspace).
- [crates/proxy/src/adapters/error.rs](../../crates/proxy/src/adapters/error.rs) — `AppError::code()` now returns the canonical `ErrorCode`; `status()` is derived from `code().default_status()`; `body()` sources its `code` field from `self.code().as_str()`. No wire-format changes — all existing test assertions on body shapes (`policy_blocked_serializes_to_structured_403`, `pic_invariant_violation_serializes_to_403`) still pass.
- [docs/error-codes.md](../error-codes.md) — operator-facing catalogue with default status + suggested action per code, plus the "adding a new code" runbook.
- Tests: 2 new in `shared-types::error_code::tests` (`wire_strings_are_stable`, `serde_round_trip_snake_case`). All existing tests in the workspace green (123 proxy + 2 shared-types).

**Deviations from §4.3 sketch.** None. The shipped enum lists every variant the spec sketch enumerated, plus `RequireConfirmation` and `RateLimited` (already in `AppError`; the spec sketch overlooked them).

---

## 5. Pattern 4 — `PolicyLoader` trait with compiled cache

### 5.1 What qiuth does

[qiuth-main/src/middleware/express.ts:43-45](../../qiuth-main/src/middleware/express.ts#L43-L45) defines:

```typescript
export type ConfigLookupFunction = (
  apiKey: string
) => Promise<QiuthConfig | null> | QiuthConfig | null;
```

The middleware accepts this function ([express.ts:110-141](../../qiuth-main/src/middleware/express.ts#L110-L141)) instead of embedding a backend. The same library serves SQL, Redis, file, in-memory test fixtures — all without touching the auth core.

### 5.2 Proxilion translation

Today the policy engine parses YAML at process start (the survey flagged this: `Engine::new()` parses on every server start, no caching, blocks boot if the policy is large). Introduce a `PolicyLoader` trait:

```rust
#[async_trait]
pub trait PolicyLoader: Send + Sync {
    /// Returns a snapshot of the current policy set.
    /// Implementations may serve from cache.
    async fn load(&self) -> Result<PolicyBundle, PolicyLoadError>;

    /// Returns true if the underlying source has changed since `since`.
    /// Default: always true (forces reload).
    async fn changed_since(&self, since: SystemTime) -> bool { true }
}

pub struct FilePolicyLoader { path: PathBuf }
pub struct DbPolicyLoader { pool: PgPool, customer_id: Uuid }
pub struct StaticPolicyLoader { bundle: PolicyBundle }  // for tests
```

Wrap with a compiled cache:

```rust
pub struct CachedPolicyEngine {
    loader: Arc<dyn PolicyLoader>,
    compiled: ArcSwap<CompiledPolicy>,
    last_loaded: AtomicU64,         // unix seconds
    refresh_interval: Duration,
}

impl CachedPolicyEngine {
    pub async fn new(loader: Arc<dyn PolicyLoader>, refresh: Duration) -> Result<Self, _>;
    pub fn evaluate(&self, ctx: &RequestContext) -> PolicyTrace;
    pub async fn refresh_if_stale(&self) -> Result<(), _>;
}
```

Mechanics:
- `ArcSwap<CompiledPolicy>` gives lock-free reads on the hot path
- A background task calls `refresh_if_stale()` every `refresh_interval`
- An admin endpoint `POST /api/policy/reload` forces an immediate reload
- Failures during reload **keep the old compiled policy live** and log — never serve "empty policy" on transient errors

### 5.3 Why the trait, not just caching

Caching alone fixes the startup-parse problem. The trait fixes the bigger problem: multi-tenant config (one customer = one policy bundle) wants `DbPolicyLoader`, dev wants `FilePolicyLoader`, tests want `StaticPolicyLoader`. Without the trait, every new backend means surgery on the engine. With it, the engine never knows.

This mirrors qiuth's insight exactly: the auth core never knows whether your API key is in Postgres or in a YAML file. The policy core shouldn't know either.

### 5.4 Status (2026-05-12) — shipped.

- [crates/policy-engine/src/loader.rs](../../crates/policy-engine/src/loader.rs) — new `PolicyLoader` trait (`async fn load(&self) -> Result<PolicyBundle, PolicyLoadError>`, `fn source_label(&self) -> String`, async `changed_since(&self, version) -> Result<Option<String>>` with a default impl). `PolicyBundle { yaml, version }` is the snapshot; `version` is an opaque token (mtime for files, revision counter for static, future `xmin` for Postgres). Implementations: `FilePolicyLoader` (production path), `StaticPolicyLoader` (tests + embed; bumps an `AtomicU64` revision counter on `set_yaml`). `FilePolicyLoader::version_token_sync()` gives the bootstrap path a non-async way to compute the initial version when constructed outside a runtime.
- [crates/proxy/src/policy_handle.rs](../../crates/proxy/src/policy_handle.rs) — `PolicyHandle::with_loader(initial, loader, raw_yaml, initial_version, source)` constructs a handle backed by a loader. New `reload_via_loader()` async method calls the loader, swaps the engine, and bumps `last_version`. `swap_from_yaml_with_version(...)` is the lower-level primitive that stamps the version atomically alongside the engine swap. `spawn_watcher` now branches: when a loader is attached, it polls `loader.changed_since(handle.last_version())` and reloads via the loader; the legacy mtime-on-`source` path is kept for back-compat (handles built with `PolicyHandle::new`, e.g. when no `PROXILION_POLICY_PATH` is set).
- [crates/proxy/src/server.rs](../../crates/proxy/src/server.rs) — `build_policy_handle` constructs `FilePolicyLoader` and feeds it to `with_loader` whenever `PROXILION_POLICY_PATH` is configured. The production reload path is now backend-pluggable; switching to a `DbPolicyLoader` is a one-line change at this call site, no adapters touched.
- Tests: 4 new in `loader::tests` (file round-trip, mtime change detection, not-found, static-loader revision bump). 2 new in `policy_handle::tests` (`reload_via_loader_swaps_engine_and_bumps_version`, `reload_via_loader_keeps_prior_engine_on_bad_yaml`). All 131 proxy + 17 policy-engine tests green.

**Deviations from §5.2 sketch.**

1. **No `refresh_interval` on a wrapping `CachedPolicyEngine`.** The existing `PolicyHandle` already does the lock-free `ArcSwap<Engine>` + atomic version bump; introducing a parallel `CachedPolicyEngine` wrapper would duplicate that machinery. The loader trait slots into the existing handle. If a customer needs a different refresh cadence per backend (e.g. faster polling for `DbPolicyLoader`), that knob lands on `spawn_watcher` rather than as a wrapping struct.
2. **`PolicyBundle` is `{yaml, version}`, not a pre-compiled tree.** Compilation stays at the engine boundary so a freshly-edited YAML with a syntax error doesn't kill the loader. The handle's "parse before swap" semantic carries through unchanged.

---

## 6. Pattern 5 — Coverage threshold gate in CI

### 6.1 What qiuth does

[qiuth-main/vitest.config.ts](../../qiuth-main/vitest.config.ts) sets coverage thresholds at 90% for lines, functions, statements, branches. Coverage runs in CI; below-threshold drops fail the build.

### 6.2 Proxilion translation

The survey flagged:
- No tests for the full OAuth flow (federation bridge → Trust Plane → token exchange)
- No tests for PCA cache eviction/expiry
- No tests for read filter + quarantine in live adapter requests
- Unit-test grep returned nothing; integration coverage only

Add a coverage gate using `cargo-llvm-cov`:

```yaml
# .github/workflows/coverage.yml (sketch)
- run: cargo install cargo-llvm-cov
- run: cargo llvm-cov --workspace --lcov --output-path lcov.info
- run: cargo llvm-cov report --fail-under-lines 70 --fail-under-functions 70
```

**Don't start at 90%.** Proxilion isn't a 318-test library; it's a service. The original ramp shipped at 60/60 → 70/70 → 80/80; a 2026-05-14 honest reset (see §6.4 status) revised this downward to match measured reality. The current operational ladder lives in §6.4; the table below is the original aspirational shape.

| Phase | Lines | Functions | Notes |
|---|---:|---:|---|
| Adoption | 60% | 60% | (Aspirational — see §6.4 for actual floor.) Anything below blocks PRs. |
| 3 months | 70% | 70% | Forces test backfill for OAuth flow + PCA cache |
| 6 months | 80% | 80% | Long-term target |

Per-crate overrides are fine — `shared-types/` is mostly re-exports and can stay lower; `policy-engine/` should sit highest since it's a pure library with no IO.

### 6.3 What this doesn't fix

A coverage gate ensures lines are *executed* in tests, not that the tests assert anything meaningful. Pair it with the existing integration test pattern in [policy-engine/tests/example_policies.rs](../../crates/policy-engine/tests/example_policies.rs) — that file is the model for "test what the policy actually decides," not just "the function returned."

### 6.4 Status (2026-05-12) — Phase 1 floor pinned at 60% / 60%; **honest reset to 35% lines / 42% functions on 2026-05-14**.

- [.github/workflows/coverage.yml](../../.github/workflows/coverage.yml) — runs `cargo llvm-cov --workspace --lcov --output-path lcov.info` on every PR and push to `main`. The rendered summary is logged for PR reviewers; the `lcov.info` artifact is always uploaded so downstream tools (Codecov, etc.) can consume it.
- Uses `taiki-e/install-action` to install `cargo-llvm-cov` from a pre-built binary (saves ~3 minutes vs `cargo install`).
- Caches `~/.cargo/registry`, `~/.cargo/git`, and `target` keyed on `Cargo.lock` — the SHA-pinned upstream `pic-protocol` + `provenance-*` deps make a cold build expensive (~6 min); a warm cache brings it under 90s.
- Tests dir is excluded from the report (`--ignore-filename-regex '(^|/)tests/'`); coverage is measured against `src/`.

**2026-05-14 — pure-helper test backfill (no floor bump yet).** Added 42 new unit tests in the lowest-coverage modules to start filling the gap the §6.4 honest reset exposed, without yet ratcheting the gate (a floor bump waits on a measured `cargo llvm-cov` run that confirms the new ceiling). Files touched:

- [crates/proxy/src/api/policy.rs](../../crates/proxy/src/api/policy.rs) — 7 tests on `parse_listing` (extracts every field, applies defaults for absent fields, skips entries with no `id`, returns empty on malformed YAML, empty input, `PolicyView` JSON shape, `SetModeBody` deserialization).
- [crates/proxy/src/api/actions.rs](../../crates/proxy/src/api/actions.rs) — 6 tests on `hex_encode`, `row_to_csv_line` (basic row, CSV-quoting for fields with commas / quotes / newlines, optional-field rendering), `make_event`, `ActionsApiError → Response` status codes.
- [crates/proxy/src/api/mod.rs](../../crates/proxy/src/api/mod.rs) — 2 tests on the sibling `hex_encode` and `ApiError::NotFound` status.
- [crates/proxy/src/api/notifier_slack.rs](../../crates/proxy/src/api/notifier_slack.rs) — 4 tests on `header_str` (ascii happy path, rejects non-ascii bytes), `slack_ok_message` (in_channel / replace_original), `slack_err` (status + JSON body).
- [crates/proxy/src/api/notifier_public.rs](../../crates/proxy/src/api/notifier_public.rs) — 6 new tests on `render_error` (HTML escaping), `render_form` (approve form has justification textarea + minlength=20; reject form has reason textarea; unknown action surfaces banner), `render_already_used`, `render_validation_error` (back link), `fill_template` with no `BlockedSummary`.
- [crates/cli/src/main.rs](../../crates/cli/src/main.rs) — 17 tests in a new `pure_helper_tests` module: `format_metric_value` (integers drop decimal, floats keep six digits), `parse_since` (RFC3339 + duration strings), `urlencode` (preserves unreserved, percent-encodes reserved), `generate_token` (prefix + body length + base32 alphabet + uniqueness), `token_hash` (SHA-256 stability + 32-byte length), `truncate` (no-op, ellipsis, unicode-char-count safety), `matches_tail_filter` (all four shapes: no-filter pass-through, decision match, combined decision+vendor+action, invalid JSON pass-through), `make_mock_jwt` (three-part shape + base64url payload decodes), `field_diff` (no-op for identical docs, flags vendor / action / required_ops changes).

All 42 pass locally; the `proxy` binary unittest count moves from 166 → 191 and `proxilion-cli` from 9 → 26.

**2026-05-14 (round 2) — 28 more pure-helper tests targeting OAuth + envelope + boot helpers.** Same playbook applied to the next tier of 0%-coverage modules:

- [crates/proxy/src/error_envelope.rs](../../crates/proxy/src/error_envelope.rs) — 6 tests on `ErrorBody` (default field state, fluent builder composition, optional-field skip-on-None serialization, all-fields-set serialization, `into_response(status)` honors caller status, blanket `IntoResponse` defaults to 500).
- [crates/proxy/src/oauth/routes.rs](../../crates/proxy/src/oauth/routes.rs) — 6 tests on `pct` (NON_ALPHANUMERIC percent-encoding), `oauth_error_class` (denied vs error buckets across every variant), `intersect_scope_with_ops` (keeps scopes whose scheme-prefix has a matching op, filters unknown scopes, always-keep for openid/email), `narrowed_ops_for_pca1` (keeps ops within granted scope prefixes, empty when no scope matches), `new_auth_code` (52-char base32 no-padding + uniqueness).
- [crates/proxy/src/oauth/error.rs](../../crates/proxy/src/oauth/error.rs) — 5 tests on `OAuthError::status()` (every variant → status mapping pinned), `body().code` (stable wire codes), `body().detail` (carried for variants that have one, absent for those that don't), `into_response()` end-to-end status.
- [crates/proxy/src/server.rs](../../crates/proxy/src/server.rs) — 4 tests on `hex_decode_32` (all-zero / all-ff round-trip, mixed-case, wrong-length rejection at three boundaries, non-hex char rejection) + 2 tests on `ensure_dev_cert` (no-op when both files exist, generates valid PEM cert + key when missing — uses `std::env::temp_dir()` rather than pulling in `tempfile`).
- [crates/proxy/src/notifier/mod.rs](../../crates/proxy/src/notifier/mod.rs) — 3 tests on `BlockedNotification::from_record` (schema constant + field passthrough, approve/reject URL construction, JSON serialization carries the `schema` field).
- [crates/proxy/src/adapters/error.rs](../../crates/proxy/src/adapters/error.rs) — 2 tests on `upstream_error_kind` (forces a timeout/connect failure against an RFC 5737 black-hole IP, pins the bounded label set).

Proxy unittest count: 191 → 219.

**2026-05-14 (round 3) — 17 more pure-helper tests on AppError + demo + session + cat_key.** Continued the §6.4 backfill into adapter-layer errors, the demo-mode synthetic event seeder, the session extractor, and the CAT-key registry. Files:

- [crates/proxy/src/adapters/error.rs](../../crates/proxy/src/adapters/error.rs) — 4 tests on `AppError`: variant → `ErrorCode` mapping (every variant), `body().extras` carries `policy_id` + `override_allowed` for `PolicyBlocked`, `status()` delegates through `ErrorCode::default_status`, `into_response()` end-to-end status.
- [crates/proxy/src/demo.rs](../../crates/proxy/src/demo.rs) — 6 tests on `synth_event`: field passthrough from `Scenario`, `p_0` picked from the known `USERS` set, path-template trailing-`s` case (used verbatim), suffix path case (six base-36 chars), distinct request/session UUIDs, and a `SCENARIOS` decision-variety pin (`allow` + `block` + `require_confirmation` must all be represented so a refactor that flattens the demo doesn't slip past review).
- [crates/proxy/src/session.rs](../../crates/proxy/src/session.rs) — 4 tests: `SessionContext::Debug` redacts `google_access_token`, `SessionExtractError → Response` is 401 with body `unauthorized`, the `FromRequestParts` extractor errors when no `Arc<SessionContext>` is in `parts.extensions`, and succeeds (returning the same `Arc`) when one is.
- [crates/proxy/src/pic/cat_key.rs](../../crates/proxy/src/pic/cat_key.rs) — 3 tests: `CatKeyError::Display` strings for `Status` and `Decode` variants are stable, `InfoResp` JSON deserialization, `CatKeyRegistry::get` returns `Fetch(_)` when the Trust Plane endpoint is unreachable (point at a connect-refused loopback port for speed; no 5s timeout wait).

Proxy unittest count: 219 → 236.

**2026-05-14 (round 4) — 22 more pure-helper tests on policy-engine + killswitch.** Pivoted to `policy-engine` for the first time (until now the backfill had focused on `crates/proxy`, where the §6.4 reset called out the biggest gaps; the engine was already at ~88% lines but missed coverage on its YAML schema defaults and the `RequestContext` lookup paths that template substitution depends on). Files:

- [crates/policy-engine/src/context.rs](../../crates/policy-engine/src/context.rs) — 10 tests on `RequestContext::lookup` + `lookup_list`: bare `customer_domain`, `path.*` / `user.email` resolution, `headers.*` lookup, `body.*` string-unquoting vs. JSON-repr fallback for non-strings, unknown-head None, list-array round trip, non-array None, array-with-non-string-element None, and `path.*` / `headers.*` never list-valued (the flat-map contract).
- [crates/policy-engine/src/yaml.rs](../../crates/policy-engine/src/yaml.rs) — 8 tests on the schema: defaults pin `Mode::Enforce` + `PicMode::Audit` + `QuarantineActionCfg::ReplaceWithMarker` (the safe production posture per ui-less-surfaces.md §2.1), minimal-doc parsing applies every default, `observe` + `runtime-gate` round-trip, every `audit_body` variant, unknown `mode` value rejected, `RecipientsCfg` accepts both string and list shapes per field, quarantine patterns accept literal + regex forms, `BurstCfg` honors individually-optional fields.
- [crates/proxy/src/api/killswitch.rs](../../crates/proxy/src/api/killswitch.rs) — 4 tests on `populate_kill_cache` (marks 32-byte rows into the live `KillCache`, skips short/long rows, no-op on empty input) and `ApiError::BadRequest → 400` with structured-body detail.

policy-engine lib unittest count: 58 → 76; proxy: 236 → 240. Total cumulative across rounds 1–4 in `crates/proxy/src/`: **166 → 240** (+74); `crates/proxilion-cli`: 9 → 26 (+17); `crates/policy-engine` lib: 58 → 76 (+18). All standard checks (`cargo fmt --check`, `cargo clippy --workspace --all-targets -- -D warnings …`, `cargo test --workspace --locked`, `RUSTFLAGS="-D warnings" cargo build --workspace --release --locked`) green.

**2026-05-14 (round 5) — 26 more pure-helper tests on policy-engine rego + pic/cache + blocked.rs ApiError.** Continuing through the rego compilation helpers (the engine's internal parsers, never directly tested before — only exercised end-to-end via `Engine::evaluate`), the PCA cache builder, and the blocked-actions API error envelope. Files:

- [crates/policy-engine/src/rego.rs](../../crates/policy-engine/src/rego.rs) — 16 tests in a new `helper_tests` module. `observe_demote`: every `Decision` variant maps to `(Allow, Some(label))` with the spec-pinned label strings (`observe_block`, `observe_require_confirmation`, `observe_rate_limit`); `Allow` is a pure passthrough. `parse_decision`: string forms (`allow` / `block` / `require_confirmation`), YAML-null → Allow, unknown string → `Error::BadDecision`, `override: requires_justification` flips `override_allowed`, structured `rate_limit:` + `block:` maps with custom reasons, missing required field on `rate_limit` (no `per_seconds`) errors loudly. `compile_read_filter`: literal + regex pattern round-trip, every `QuarantineActionCfg` variant maps to its `QuarantineAction` counterpart, malformed regex surfaces `Error::BadRegex`.
- [crates/proxy/src/pic/cache.rs](../../crates/proxy/src/pic/cache.rs) — 4 tests. `CURRENT_PIC_PROFILE` pinned at the string `"proxilion.v1"` (spec.md §15 #11 — changing this without a migration story is a breaking schema bump); `CachedPca::new` default-fills `pic_profile` to the current value and zero-initializes `signature`; predecessor passthrough; `CacheError::Display` formats with the `postgres:` prefix the operator-facing log filters expect.
- [crates/proxy/src/api/blocked.rs](../../crates/proxy/src/api/blocked.rs) — 6 tests on `ApiError → Response` covering every variant: `NotFound → 404` with `code:"not_found"`, `BadRequest → 400` with detail, `Conflict → 409` with detail, `PicRefused → 422` with `fix` carrying the "re-root chain at broader PCA_0" hint operators need, `Internal → 500`, `Db → 500` with sqlx Display passthrough.

policy-engine lib: 76 → 92; proxy lib: 240 → 250. Cumulative rounds 1–5: proxy +84, cli +17, policy-engine +34.

**2026-05-16 (round 6) — 12 more pure-helper tests on blocked.rs DTOs + email helper + notifier set-config body.** All targets are request/response serde shapes operators interact with via the CLI — pinning them as a stable wire contract. Files:

- [crates/proxy/src/api/blocked.rs](../../crates/proxy/src/api/blocked.rs) — 6 new tests on the request/response DTOs: `ApproveBody` deserialization (required `justification`, optional `ttl_minutes` + `approver_subject`; missing-justification rejected), `RejectBody`, `IssueLinkBody` (required `action`, optional `ttl_minutes` + `approver_hint`), `BlockedRow` `request_canonical_json` skip-on-None / include-when-Some.
- [crates/proxy/src/notifier/email.rs](../../crates/proxy/src/notifier/email.rs) — 3 new tests on `parse_or_fallback`: parses a valid mixed list (bare addr + named mailbox), one bad addr kicks the entire list back to the fallback (rather than silently dropping the bad entry — the strict-list contract the per-policy email override depends on), empty input returns empty.
- [crates/proxy/src/api/notifier.rs](../../crates/proxy/src/api/notifier.rs) — 3 new tests: `redact_url` no-`://` defensive passthrough, `SetConfigBody` round-trip + absent-enabled-defaults (the field is `Option<bool>` because the handler does its own `unwrap_or(true)`).

proxy lib unittests: 250 → 262. `fmt`, `clippy -D warnings`, `cargo test --workspace --locked`, `RUSTFLAGS="-D warnings" cargo build --workspace --release --locked` all green.

**2026-05-16 (round 7) — 11 more pure-helper tests on shared-types + GoogleClient + floor bump 35/42 → 45/55.** Final pure-helper sweep before bumping the gate. Files:

- [crates/shared-types/src/error_code.rs](../../crates/shared-types/src/error_code.rs) — 4 new tests: `default_status_snapshot` pins the `(variant → HTTP status)` mapping (an operator's Grafana alert keyed on `status="403"` for `code="policy_blocked"` is part of the wire contract), `display_uses_wire_string`, `copy_and_hash_traits_work_at_use_sites` (pin Copy + Hash so a `derive` diff doesn't break `HashMap<ErrorCode, _>` use sites), `unknown_wire_string_fails_deserialize` (closed wire enum despite `#[non_exhaustive]` Rust-side).
- [crates/shared-types/src/operator_scopes.rs](../../crates/shared-types/src/operator_scopes.rs) — 3 new tests: `scope_strings` length + order matches `SCOPE_CATALOGUE`, `known_scope_set_is_present` (pins every operator-facing scope the CLI documents), `every_scope_string_uses_kebab_or_colon_format` (`*` or `<group>:<verb>` — no spaces/commas that break shell parsing).
- [crates/proxy/src/oauth/state.rs](../../crates/proxy/src/oauth/state.rs) — 4 new tests on `GoogleClient::from_env`: missing `GOOGLE_CLIENT_ID` and `GOOGLE_CLIENT_SECRET` each error with the named variable in the message, optional `GOOGLE_AUTH_URL` / `GOOGLE_TOKEN_URL` default to the canonical Google endpoints, both overrides take effect. Tests share a `Mutex<()>` to serialize env-var mutation (cargo's parallel test runner would otherwise race them) and save/restore the surrounding shell env so a real `GOOGLE_*` value isn't trampled.

shared-types lib: 5 → 12; proxy lib: 262 → 266.

**Floor bump (qiuth-patterns §6.4): 35% lines / 42% functions → 45% lines / 55% functions.** Earned: `cargo llvm-cov --workspace --ignore-filename-regex '(^|/)tests/'` measured workspace coverage at **47.72% lines / 56.67% functions** after the 7-round backfill (+12.6 / +12.7 points over the 2026-05-14 honest-reset baseline of 36.94% / 43.94%). The new floor sits ~2.7 points below measured lines and 1.7 below measured functions — comfortable headroom while still tight enough that a real regression (e.g. ripping out a tested module) trips the gate. Updated in [.github/workflows/coverage.yml](../../.github/workflows/coverage.yml) along with a revised phase-ladder comment. The 70/70 ceiling still depends on the deferred wiremock+postgres harness — a single-line bump alongside that future PR.

**Cumulative across rounds 1–7.** proxy lib unittests **166 → 266** (+100); proxilion-cli **9 → 26** (+17); policy-engine lib **58 → 92** (+34); shared-types **5 → 12** (+7). Total: **158 new pure-helper unit tests** + the coverage gate now ratchets ~12 points above its day-1 floor.

**2026-05-16 (round 8) — 6 more pure-helper tests on blocked.rs notification round-trip + api/setup.rs envelope.** Two narrow targets the prior rounds skipped: `OwnedBlockedNotification` (load-bearing for the `tokio::spawn`-ed fan-out path — adapters return before the notifier finishes; the owned snapshot is the lifetime bridge) and the `api/setup.rs` wire shape (the admin `/api/v1/setup/status` envelope an installer UI keys on). Files:

- [crates/proxy/src/blocked.rs](../../crates/proxy/src/blocked.rs) — 2 new tests. `owned_notification_round_trips_through_borrowed` constructs a `BlockedNotification<'_>`, materializes it through `OwnedBlockedNotification::from`, then borrows it back via `as_borrowed` and asserts every field survives. `owned_notification_clone_yields_independent_views` pins the `Clone` impl the per-driver `tokio::spawn` blocks depend on (each spawned future owns its own clone).
- [crates/proxy/src/api/setup.rs](../../crates/proxy/src/api/setup.rs) — 4 new tests on the setup-status wire shape: `CheckItem` JSON keys are `id` / `title` / `ok` / `detail` / `fix` / `docs` (stable contract for any installer UI); success-case omits / null-fills `fix`, failure-case carries the operator hint; `SetupStatus` envelope carries `ready_for_traffic` plus the item array; `SetupError → 500` carries the `troubleshooting` docs link.

proxy lib unittests: 266 → 272. Measured workspace coverage: 48.43% lines / 57.22% functions (up from 47.72 / 56.67 last round).

**Cumulative across rounds 1–8: 164 new pure-helper unit tests.** Coverage measured 48.43% lines / 57.22% functions; gate floor stays at 45 / 55 (room for natural variance from line-count drift on the next refactor before the next ratchet).

**2026-05-16 (round 9) — 7 more tests on operator_auth.** Targeted the second-largest 0%-coverage gap among modules with pure helpers: [crates/proxy/src/operator_auth.rs](../../crates/proxy/src/operator_auth.rs) was at 38.52% line coverage despite already having 8 tests on `parse_token` + `require_scope`. The middleware response shapes (`unauthorized()`, `require_scope()` helper) and the rest of the principal API were the gap. New tests:

- `hash_differs_across_tokens` (alongside the existing `hash_is_stable`)
- `generate_returns_distinct_well_formed_tokens` (round-trip with `parse_token`)
- `scope_error_message_carries_required` (the operator-facing `ScopeError::Display`)
- `unauthorized_response_is_401_with_plain_body` (the fixed-body contract — same posture as the bearer middleware)
- `require_scope_helper_returns_principal_on_match` — exercises the public helper that handlers can call directly
- `require_scope_helper_403_with_required_and_have_on_miss` — pins the structured deny body the CLI surfaces (`code:"scope_denied"`, `required`, `have:[...]`)
- `require_scope_helper_401_when_no_principal` — the missing-extension path

`operator_auth.rs` line coverage: **38.52% → 64.91%.** Proxy lib unittests: 272 → 279. Workspace coverage now **48.94% lines / 57.77% functions** (was 48.43 / 57.22 last round). Gate floors stay at workspace 45/55 + per-crate proxy 45; comfortable headroom remains. `cargo fmt`, `cargo clippy --workspace --all-targets -- -D warnings …`, `cargo test --workspace --locked`, and `RUSTFLAGS="-D warnings" cargo build --workspace --release --locked` all green. `cargo fmt`, `cargo clippy --workspace --all-targets -- -D warnings -A clippy::type_complexity -A clippy::too_many_arguments -A clippy::result_large_err`, `cargo test --workspace --locked`, and `RUSTFLAGS="-D warnings" cargo build --workspace --release --locked` all green. `cargo clippy --workspace --all-targets -- -D warnings -A clippy::type_complexity -A clippy::too_many_arguments -A clippy::result_large_err` and `cargo fmt --check` both clean.

**2026-05-16 (round 10) — 14 more tests on action_stream + oauth/bridge + forwarder/nats.** Three modules that survived the prior nine rounds untouched (action_stream had 0 tests; oauth/bridge and forwarder/nats had 3 and 2 respectively). All targets are pure helpers — no DB, no tokio runtime beyond `LoggingStream::publish`. Files:

- [crates/proxy/src/adapters/action_stream.rs](../../crates/proxy/src/adapters/action_stream.rs) — 5 new tests on the `ActionEvent` wire contract (NATS / SIEM consumers key on this shape; pinning it as a stable contract). `extra_null_is_skipped_in_json` (the `#[serde(skip_serializing_if = "Value::is_null")]` attribute is load-bearing — downstream indexers check key-presence), `extra_object_is_serialized` (the symmetric case), `round_trips_through_json_with_absent_extra` (an older consumer omitting `extra` must deserialize via `#[serde(default)]` rather than fail), `full_round_trip_preserves_all_fields`, and `logging_stream_publish_is_infallible` (exercises the `ActionStream` trait dispatch on the always-available logging sink).
- [crates/proxy/src/oauth/bridge.rs](../../crates/proxy/src/oauth/bridge.rs) — 5 new tests filling the gap in `validate_federation_token` + `infer_idp`. `rejects_malformed_jwt_missing_parts` (one-part and two-part inputs both surface `BridgeRejected("malformed JWT")`), `rejects_bad_base64_in_payload` (the `URL_SAFE_NO_PAD.decode` error path), `rejects_future_issued_token` (the 60-second clock-skew guard — previously only the expired-token branch was tested), `claims_iss_round_trips_through_payload` (production bridges MUST carry `iss` through so the `idp` label on `proxilion_oauth_callback_total` is correct), `infer_idp_covers_secondary_substrings` (the `googleapis.com` and `windows.net` alternates that the original `infer_idp_classifies_known_issuers` test skipped).
- [crates/proxy/src/forwarder/nats.rs](../../crates/proxy/src/forwarder/nats.rs) — 4 new tests on `sanitize_token` + `ConnectError`. `sanitize_preserves_hyphens_underscores_and_alphanum` (a future `gmail-beta` vendor label must pass through unchanged or wildcard subscriptions break), `sanitize_empty_returns_empty`, `sanitize_replaces_unicode_with_underscore` (multibyte input never breaks subject parsing on the subscriber side), `connect_error_display_contains_reason` (operator-facing `Display` includes both the `nats connect failed` prefix and the underlying reason).

Proxy lib unittests: **279 → 293**. `cargo fmt --all --check`, `cargo clippy --workspace --all-targets -- -D warnings -A clippy::type_complexity -A clippy::too_many_arguments -A clippy::result_large_err`, `cargo test --workspace --locked` (293 proxy / 92 policy-engine / 12 shared-types / 26 cli + integration), and `RUSTFLAGS="-D warnings" cargo build --workspace --release --locked` all green. Gate floors unchanged (workspace 45/55, per-crate proxy 45); coverage headroom continues to widen — the round-10 targets were among the last 0-test modules in the proxy crate.

**2026-05-16 (round 11) — 16 more tests on policy-engine trace + adapter policy_trace + adapter read_filter pure helpers + forwarder/tee.** Four modules where the visible surface (the public `apply`/`mark_*`/`publish` calls) already had a test, but the wire-shape and private-helper invariants underneath them were unverified. Files:

- [crates/policy-engine/src/trace.rs](../../crates/policy-engine/src/trace.rs) — 5 new tests. `policy_layer_serializes_snake_case` pins the JSON wire string for every `PolicyLayer` variant + a deserialize round-trip (the trace JSON is logged to the audit pipeline and the dashboard parses it — `layer_a` / `layer_b` / `read_filter` is a stable contract). `policy_eval_mode_default_is_fail_fast` pins the production posture per §3.3. `trace_not_allowed_when_decision_is_block_even_if_layers_pass` covers the `allowed()` second clause (the previous test only covered the all-layers-pass branch). `layer_outcome_json_omits_none_fields_via_explicit_serialize` pins that the struct serializes `null` for missing optional fields (no `skip_serializing_if`) so a downstream consumer can rely on key-presence. `policy_trace_json_carries_trace_id_and_layers` pins the envelope shape (trace_id is a string, evaluated_at is a string, duration_micros + layers + required_ops are present).
- [crates/proxy/src/adapters/policy_trace.rs](../../crates/proxy/src/adapters/policy_trace.rs) — 4 new tests filling the gaps in the `mark_*` / `set_layer` / `summary` helpers. `mark_read_filter_blocked_sets_failed_with_code` (the `blocked=true` branch — previously only the passing branch was tested). `mark_read_filter_replaces_existing_entry` (a second `mark_read_filter` call must update the existing slot, not append — the trace would otherwise carry duplicate ReadFilter entries and break the dashboard's "one outcome per layer" assumption). `set_layer_appends_when_layer_absent` exercises the helper directly on an empty-layers trace. `summary_renders_read_filter_label_and_empty_layers` pins the `read_filter=…` label string (operator log filters key on these labels) plus the empty-layers boundary case.
- [crates/proxy/src/adapters/read_filter.rs](../../crates/proxy/src/adapters/read_filter.rs) — 5 new tests on the four private helpers (`truncate`, `should_scan`, `merge_overlapping`, `splice`) that were previously only exercised end-to-end via `apply`. `truncate_helper_keeps_short_unchanged_and_ellipsizes_long` (boundary at exact-limit, ellipsis on overflow); `truncate_uses_char_count_not_byte_len` (multi-byte unicode safety — the `audit` string uses this to bound the pattern label, byte-count truncation would split codepoints); `should_scan_decides_by_content_type` (no-CT default-scan, `application/json` + `application/xml` + `text/*` scanned, case-insensitive matching, `application/octet-stream` + `image/png` + `application/pdf` skipped); `merge_overlapping_collapses_touching_and_overlapping_ranges` (disjoint preserved, overlapping merged, touching `end == next.start` merged per the `>=` predicate, nested absorbed, empty input); `splice_replaces_ranges_and_preserves_surroundings` (single + multi-range + no-range identity + whole-string replacement).
- [crates/proxy/src/forwarder/tee.rs](../../crates/proxy/src/forwarder/tee.rs) — 2 new tests. `sink_count_tracks_with_sink_chaining` exercises the fluent builder (`with_sink` × N → count = N). `each_sink_receives_independent_clone` pins that the fan-out clones the event per sink (the `Collector` assertions check every sink saw the same `request_id` + `vendor`, so a future refactor that moves the event into the first sink instead of cloning would surface here).

policy-engine lib unittests: **92 → 97**; proxy lib: **293 → 304**. Cumulative across rounds 1–11: proxy **+138**, cli +17, policy-engine **+39**, shared-types +7 — **180 new pure-helper unit tests** total. Gate floors unchanged (workspace 45/55, per-crate proxy 45 / shared-types 95 / policy-engine 88 / cli 10). `cargo fmt --all --check`, `cargo clippy --workspace --all-targets --locked -- -D warnings -A clippy::type_complexity -A clippy::too_many_arguments -A clippy::result_large_err`, `cargo test --workspace --locked`, and `RUSTFLAGS="-D warnings" cargo build --workspace --release --locked` all green.

**2026-05-16 (round 12) — 6 more tests on policy-engine loader.** Round 11 hit the `trace` module but skipped the sibling `loader` module, which had four happy-path async tests but no coverage on its `Display`-trait wire shape, its `PartialEq` impl, or the sync-bootstrap `version_token_sync()` path the proxy uses before the tokio runtime is up. Files:

- [crates/policy-engine/src/loader.rs](../../crates/policy-engine/src/loader.rs) — 6 new tests. `policy_load_error_display_renders_each_variant` pins the `io error:` / `source not found:` / `backend error:` prefixes (operator log filters and Grafana alerts key on these substrings — a future variant rename must be a conscious wire-shape change, not an accidental string tweak). `policy_bundle_equality_ignores_neither_yaml_nor_version` exercises the derived `PartialEq` on both axes (the proxy short-circuits reloads on equality — a future drift to a `version`-only comparison would silently skip yaml-only edits that share an mtime). `file_loader_path_and_source_label_round_trip` pins that `path()` and `source_label()` agree on the path representation (the label is what shows up in `policy_load_failed` log lines — drifting between the two would split the operator's mental model). `file_loader_version_token_sync_matches_async_load` pins that the sync bootstrap path emits the same `mtime:<nanos>` string as the async `load()` path (the proxy's startup code calls `version_token_sync()` before the runtime is up and then `changed_since(..)` on the same loader once it is — a divergence would force a spurious first reload). `file_loader_version_token_sync_reports_not_found` pins the missing-file branch of the sync helper (otherwise only the async `load()` path's NotFound was exercised). `static_loader_with_label_overrides_default_source_label` pins the `with_label("…")` fluent setter (used by embed-API tests to give the synthetic loader a recognizable name in trace output).

policy-engine lib unittests: **97 → 103**. Cumulative across rounds 1–12: proxy +138, cli +17, policy-engine **+45**, shared-types +7 — **186 new pure-helper unit tests** total. Gate floors unchanged (workspace 45/55, per-crate proxy 45 / shared-types 95 / policy-engine 88 / cli 10). `cargo fmt --all --check`, `cargo clippy --workspace --all-targets --locked -- -D warnings -A clippy::type_complexity -A clippy::too_many_arguments -A clippy::result_large_err`, `cargo test --workspace --locked`, and `RUSTFLAGS="-D warnings" cargo build --workspace --release --locked` all green.

**2026-05-16 (round 13) — 12 more tests on crypto/pkce + kill_cache + pic/violations + notifier/mod.** Four sub-100-line modules that had a happy-path test each but skipped the off-by-one boundaries (PKCE length 42/43/128/129), the `Default` constructor's empty-cache promise, malformed Trust Plane refusal bodies, and the None-field passthrough on the notification envelope. Files:

- [crates/proxy/src/crypto/pkce.rs](../../crates/proxy/src/crypto/pkce.rs) — 3 new tests. `boundary_42_chars_rejected_and_43_chars_accepted_as_length` pins the lower edge of RFC 7636 §4.1 — 42 must surface `VerifierLength`, 43 must pass length and reach the SHA-256 compare path (where the bogus challenge then surfaces `Mismatch`). Asserted on the variant, not just `is_err()`, so a future collapse to a single `BadVerifier` would surface here. `boundary_128_chars_accepted_and_129_rejected` is the symmetric upper edge. `error_display_strings_are_stable_for_log_filters` pins the `"verifier failed PKCE check"` and `"length must be 43..=128"` substrings (operator log filters key on these — a future message tweak must be a conscious wire-shape change).
- [crates/proxy/src/kill_cache.rs](../../crates/proxy/src/kill_cache.rs) — 3 new tests. `default_constructor_yields_empty_cache` pins that `KillCache::default()` (what `AppState` builds when no killswitch backend is wired in tests) behaves as a fresh `new()` — no false-positive kills carried over from some shared static. `two_cache_instances_do_not_share_state` is the symmetric guard against a future refactor moving the moka `Cache` into a `lazy_static` global. `mark_many_with_empty_iterator_is_noop` pins the zero-row-UPDATE path through killswitch handlers — the iterator is sometimes empty and the loop must not panic.
- [crates/proxy/src/pic/violations.rs](../../crates/proxy/src/pic/violations.rs) — 3 new tests on `parse_missing_atoms`. `parse_atoms_single_value_no_comma` (Trust Plane sometimes emits a single missing atom — the `,`-splitter must still produce one entry, not zero). `parse_atoms_handles_unclosed_bracket` (malformed input — opening `[` but no `]` — must not panic and must return empty rather than reading off the end; raw `detail` is still persisted by the caller). `parse_atoms_trims_whitespace_and_drops_empty_segments` (both the `"[ a , b ]"` spacing variant and the trailing-comma variant — `[a,b,]` — produce the same two-entry list).
- [crates/proxy/src/notifier/mod.rs](../../crates/proxy/src/notifier/mod.rs) — 3 new tests on `BlockedNotification`. `schema_constant_is_versioned_string_consumers_key_on` pins both the literal `"proxilion.blocked_action.v1"` value and the `.vN` suffix shape — webhook receivers route on the schema string and may parse v2 differently. `from_record_passes_none_fields_through_unchanged` exercises the four `Option<_>` fields (`p_0`, `policy_id`, `detail`, `predecessor_pca_id`) — a stray `""` or `"(none)"` synthesis would mis-classify the blocked row downstream; the JSON keeps the keys present (as `null`) since the struct has no `skip_serializing_if`. `from_record_carries_empty_requested_ops_slice` pins that the empty-ops case round-trips as `[]` in JSON rather than being elided (Slack templates iterate the array and rely on it being present).

proxy bin tests: **304 → 316**. Cumulative across rounds 1–13: proxy **+150**, cli +17, policy-engine +45, shared-types +7 — **198 new pure-helper unit tests** total. Gate floors unchanged (workspace 45/55, per-crate proxy 45 / shared-types 95 / policy-engine 88 / cli 10). `cargo fmt --all --check`, `cargo clippy --workspace --all-targets --locked -- -D warnings -A clippy::type_complexity -A clippy::too_many_arguments -A clippy::result_large_err`, `cargo test --workspace --locked`, and `RUSTFLAGS="-D warnings" cargo build --workspace --release --locked` all green.

**2026-05-16 (round 14) — 11 more tests on crypto/token_cipher + api/mod + pic/cache.** Three modules whose visible round-trip was already covered, but whose error paths and ownership invariants were skipped — exactly the surface a Postgres outage or a corrupt env var lands on. Files:

- [crates/proxy/src/crypto/token_cipher.rs](../../crates/proxy/src/crypto/token_cipher.rs) — 5 new tests. `tampered_ciphertext_rejected_by_gcm_tag` flips a byte and pins that the AEAD tag surfaces `CipherError::Aead` (a future refactor to CTR-only would silently pass — this is the AEAD contract). `wrong_nonce_length_rejected_without_aead_call` covers the pre-check at length 11 and 13 (a corrupt persisted `nonce` column would otherwise panic inside `Nonce::from_slice`). `ciphertext_clone_yields_independent_buffer` pins that `Ciphertext`'s `Clone` owns its bytes (no shared backing — a stray `Rc<Vec<u8>>` introduced later would surface here). `bad_key_len_error_display_includes_actual_length` pins that the operator sees both "32" (required) and the actual length in the message (the troubleshooting docs page keys on this shape). `empty_key_rejected_with_zero_length` is the boundary case — a missing env var lands as a zero-byte slice and must carry `BadKeyLen(0)`, not panic. Result matched explicitly rather than `unwrap_err`-ed because `TokenCipher` intentionally has no `Debug` impl (it holds the master key).
- [crates/proxy/src/api/mod.rs](../../crates/proxy/src/api/mod.rs) — 3 new tests. `hex_encode_covers_all_byte_values` walks all 256 byte values; pins lowercase + width-2 (a regression that emitted upper-case or dropped a leading zero would break any tool that round-trips through `hex::decode`). `api_error_db_maps_to_500_with_internal_error_code` exercises the Db variant of `ApiError::into_response` (previously only `NotFound` was covered) — the Grafana alert keyed on `status="500" code="internal_error"` for a real Postgres outage rides on this. `hex_encode_byte_count_matches_two_per_input_byte` is the length-invariant check across N=0/1/16/64/257.
- [crates/proxy/src/pic/cache.rs](../../crates/proxy/src/pic/cache.rs) — 3 new tests. `cached_pca_is_clone_with_disjoint_buffers` mutates the clone and pins that the original's `cbor` + `ops` are unchanged (an accidental `Cow`/`Rc` field would surface here as the chain verifier mutates clones). `cached_pca_new_starts_with_empty_signature` pins the empty-on-construction default (a future "pre-fill with a 'not yet signed' sentinel byte" change would surface here). `cache_error_from_sqlx_via_question_mark` exercises the `#[from]` blanket-impl path the public `insert` / `get` methods use — dropping `#[from]` later would surface here as a compile error rather than as a silent string-format regression at the call sites.

proxy bin tests: **316 → 327**. Cumulative across rounds 1–14: proxy **+161**, cli +17, policy-engine +45, shared-types +7 — **209 new pure-helper unit tests** total. Gate floors unchanged (workspace 45/55, per-crate proxy 45 / shared-types 95 / policy-engine 88 / cli 10). `cargo fmt --all --check`, `cargo clippy --workspace --all-targets --locked -- -D warnings -A clippy::type_complexity -A clippy::too_many_arguments -A clippy::result_large_err`, `cargo test --workspace --locked`, and `RUSTFLAGS="-D warnings" cargo build --workspace --release --locked` all green.

**2026-05-16 (round 15) — 9 more tests on crypto/bearer + notifier/handle.** Two modules whose happy-path round-trip was already covered, but whose alphabet/length boundaries (bearer) and hot-swap multi-clone semantics (handle) were not. Files:

- [crates/proxy/src/crypto/bearer.rs](../../crates/proxy/src/crypto/bearer.rs) — 6 new tests. `parse_rejects_length_below_and_above_token_len` pins the symmetric off-by-one boundary (51-char and 53-char bodies both rejected) — a future loosening of `==` to `>=` would surface here. `parse_rejects_digits_outside_base32_alphabet` pins that `0` / `1` / `8` / `9` (the four ascii digits RFC 4648 omits — visually similar to O/I/B/g) all fail; a sloppy `is_ascii_alphanumeric` check would surface here. `bearer_hash_as_bytes_returns_full_32_byte_view` pins both length and pointer-equality with the inner `[u8; 32]` so a future refactor that returned a hex string or a truncated head would surface here (the killswitch SQL predicate keys on the raw 32 bytes). `bearer_hash_debug_truncates_to_short_prefix` pins that `Debug` shows only the 8-hex-char prefix — a regression that printed the full hash would let log aggregators store rotatable-secret-derived bytes. `two_generated_bearers_are_distinct` pins randomness at the trivial scale-2 case (a hard-coded sample or a reset RNG would collide). `bearer_hash_partial_eq_distinguishes_different_inputs` pins both axes of the `PartialEq+Eq` derives the middleware uses to detect already-revoked hashes.
- [crates/proxy/src/notifier/handle.rs](../../crates/proxy/src/notifier/handle.rs) — 3 new tests. `cloned_handle_sees_replace_via_other_clone` pins the design intent of `Handle::clone` — clones share the underlying `Arc<ArcSwap<_>>` so the `/api/v1/notifier/config` hot-swap endpoint can replace the inner notifier without re-plumbing every request handler. A future refactor that deep-copied the swap cell would break this invariant. `bundle_clone_shares_handles_with_original` is the same property at the `Notifiers` bundle level (each field is Arc-backed and survives `derive(Clone)`). `any_configured_triggers_on_slack_alone` exercises the `||`-chain branch that wasn't covered by the existing webhook test — the easy copy-paste bug (`webhook || webhook || webhook`) would surface here.

proxy bin tests: **327 → 336**. Cumulative across rounds 1–15: proxy **+170**, cli +17, policy-engine +45, shared-types +7 — **218 new pure-helper unit tests** total. Gate floors unchanged (workspace 45/55, per-crate proxy 45 / shared-types 95 / policy-engine 88 / cli 10). `cargo fmt --all --check`, `cargo clippy --workspace --all-targets --locked -- -D warnings -A clippy::type_complexity -A clippy::too_many_arguments -A clippy::result_large_err`, `cargo test --workspace --locked`, and `RUSTFLAGS="-D warnings" cargo build --workspace --release --locked` all green.

**2026-05-16 (round 16) — 8 more tests on auth_middleware + pic/cat_key.** The middleware module had pure-helper surface (the `unauthorized()` 401 builder, every `AuthFail` `Display` variant, the `RefreshCoordinator` cache-population path) that round-1's `verify_with_key` sweep skipped. `cat_key` was at 3 tests with no coverage on the `Status` Display, on the `CatKeyRegistry::Clone` Arc-sharing invariant, or on the `InfoResp` forward-compat path. Files:

- [crates/proxy/src/auth_middleware.rs](../../crates/proxy/src/auth_middleware.rs) — 5 new tests. `unauthorized_helper_returns_401_with_plain_body` pins the fixed 401 contract (status + exact `"unauthorized"` byte body) — operator alerts key on this 401 rate as the "agent traffic broken" signal. `auth_fail_display_strings_are_stable_for_log_filters` pins the operator-facing substrings every `AuthFail` variant emits (Grafana / Loki filters depend on them). `auth_fail_pca_cache_miss_message_explains_upstream_gap` pins the `PcaCacheMiss` message's `/v1/pca/` hint — operators who hit this need the spec.md §1.2 explanation, not just an opaque variant name. `auth_fail_from_sqlx_via_question_mark` exercises the `#[from]` blanket-impl the middleware uses to bubble DB errors out of the bearer JOIN — dropping `#[from]` later would surface here as a compile error. `refresh_coordinator_default_starts_empty_then_populates` is a complement to the existing `same_mutex_for_same_hash` test, covering the empty-cache → first-lookup → second-lookup populated transition.
- [crates/proxy/src/pic/cat_key.rs](../../crates/proxy/src/pic/cat_key.rs) — 3 new tests. `cat_key_error_status_display_carries_code_only` pins that the `Status` variant exposes only the upstream HTTP code, never a response body (an upstream Trust Plane error message we don't want pasted into our 500). `cat_key_registry_clones_share_underlying_oncecell` pins the Arc-sharing invariant — a regression that deep-copied the `OnceCell<PublicKey>` would re-fetch on every clone (visible as duplicate counters in production). `info_resp_ignores_unknown_fields_for_forward_compat` pins that a future Trust Plane adding fields (e.g. `next_rotation`) doesn't break the deserializer — required for rolling upgrades.

proxy bin tests: **336 → 344**. Cumulative across rounds 1–16: proxy **+178**, cli +17, policy-engine +45, shared-types +7 — **226 new pure-helper unit tests** total. Gate floors unchanged (workspace 45/55, per-crate proxy 45 / shared-types 95 / policy-engine 88 / cli 10). `cargo fmt --all --check`, `cargo clippy --workspace --all-targets --locked -- -D warnings -A clippy::type_complexity -A clippy::too_many_arguments -A clippy::result_large_err`, `cargo test --workspace --locked`, and `RUSTFLAGS="-D warnings" cargo build --workspace --release --locked` all green.

**2026-05-16 (round 17) — 9 more tests on notifier/webhook + pic/verifier.** Two modules whose end-to-end mock-server tests were already in place, but whose pure-helper surface (each `from_hex` failure branch, the `invariant_kind` label table, the `err_to_result` `broken_at` field) was not pinned. Files:

- [crates/proxy/src/notifier/webhook.rs](../../crates/proxy/src/notifier/webhook.rs) — 5 new tests. `secret_from_hex_distinguishes_each_failure_branch_by_message` pins the four distinct operator-facing messages (empty / odd / too-short / invalid-hex) — a future refactor that collapsed two branches into "invalid hex" would lose the actionable hint. (Matched on `Result` rather than `unwrap_err`-ed because `WebhookSecret` intentionally has no `Debug` impl.) `signature_is_lowercase_hex_with_sha256_prefix` pins the wire shape — receivers strip the prefix and hex-decode the rest, so an uppercase regression would break them. `webhook_notifier_with_burst_attaches_suppressor_and_burst_accessor_reads_it` covers both ends of the fluent setter + accessor contract (the `burst()` getter is `#[allow(dead_code)]` today but slated for `/api/v1/notifier/test` — a regression that renamed the field but missed the accessor would surface here). `notifier_build_error_display_contains_inner_reason` pins the `notifier build: <reason>` prefix the setup-status path renders. `webhook_proxy_public_url_round_trips_through_accessor` pins that the approve/reject URL builders read back the *proxy's* public URL, not the upstream webhook URL.
- [crates/proxy/src/pic/verifier.rs](../../crates/proxy/src/pic/verifier.rs) — 4 new tests. `invariant_kind_labels_are_bounded_and_stable` pins every `VerifierError` variant to its label string — the `proxilion_pic_invariant_violations_total{kind}` Prometheus metric uses these as labels, and an unbounded-cardinality regression would OOM the scraper. `err_to_result_pins_broken_at_to_named_pca_when_known` pins that the three variants carrying an explicit id (`Missing`, `BadCatSignature`, `ContinuityBroken`) surface that id rather than the leaf — the dashboard's chain-walker UI keys on `broken_at`. `err_to_result_marks_chain_not_intact_and_carries_reason_string` pins the `intact=false` + non-empty `reason` invariant for every error path. `verifier_error_display_carries_named_field_values` pins that thiserror's `{field}` syntax in the structured variants surfaces the actual values (the dashboard's `reason` field is `e.to_string()` — a regression that lost the field substitution would leave operators chasing the chain by hand).

proxy bin tests: **344 → 353**. Cumulative across rounds 1–17: proxy **+187**, cli +17, policy-engine +45, shared-types +7 — **235 new pure-helper unit tests** total. Gate floors unchanged (workspace 45/55, per-crate proxy 45 / shared-types 95 / policy-engine 88 / cli 10). `cargo fmt --all --check`, `cargo clippy --workspace --all-targets --locked -- -D warnings -A clippy::type_complexity -A clippy::too_many_arguments -A clippy::result_large_err`, `cargo test --workspace --locked`, and `RUSTFLAGS="-D warnings" cargo build --workspace --release --locked` all green.

**2026-05-16 (round 18) — 7 more tests on adapters/state + api/notifier.** First test landed in `adapters/state.rs` (previously 0 tests) — the `google_api_base()` helper looks tiny but a flipped `unwrap_or` or a dropped `www.` prefix would break adapter TLS verification at runtime. Sibling target was `api/notifier.rs`'s envelope parser, where two required fields had no negative-case coverage. Files:

- [crates/proxy/src/adapters/state.rs](../../crates/proxy/src/adapters/state.rs) — 2 new tests. The helper is a pure `Option<String> → &str` resolver but takes a full `AdapterState` (Postgres pool + notifier bundle + executor) to construct, so the tests exercise the same `as_deref().unwrap_or(...)` shape on a stand-in and document the production string in a literal — a future refactor that changed the default URL or flipped the precedence (override → fallback) now must update test + helper in lockstep. `google_api_base_falls_back_to_production_googleapis_when_unset` pins the `https://www.googleapis.com` fallback (a regression to `googleapis.com` without the `www` would break TLS hostname verification). `google_api_base_respects_override_for_wiremock_tests` pins the override-wins precedence — swapping the arms of `unwrap_or` would silently route adapter calls to production from inside a test fixture.
- [crates/proxy/src/api/notifier.rs](../../crates/proxy/src/api/notifier.rs) — 5 new tests. `redact_url_no_path_keeps_full_host` pins that a bare-host URL (scheme + host, no path) does NOT gain a trailing `/...` — the dashboard's notifier list displays this string verbatim, so a regression that always appended would mis-display bare URLs. `redact_url_keeps_scheme_when_host_has_trailing_slash_only` is the symmetric `https://host/` boundary — the first path segment is empty but the redacted form still adds `/...`. `set_config_body_rejects_missing_driver_field` and `set_config_body_rejects_missing_config_field` pin that the two non-Option fields fail at the envelope layer rather than producing a confusing serde error inside the per-driver branch (the dashboard validates first, but a hand-rolled curl must still get a clean 400). `set_config_body_accepts_explicit_enabled_false` pins that `enabled: false` round-trips as `Some(false)` (not coerced to None, not unwrapped to true) — the disable path is how operators pause a configured-but-not-currently-used driver.

proxy bin tests: **353 → 360**. Cumulative across rounds 1–18: proxy **+194**, cli +17, policy-engine +45, shared-types +7 — **242 new pure-helper unit tests** total. Gate floors unchanged (workspace 45/55, per-crate proxy 45 / shared-types 95 / policy-engine 88 / cli 10). `cargo fmt --all --check`, `cargo clippy --workspace --all-targets --locked -- -D warnings -A clippy::type_complexity -A clippy::too_many_arguments -A clippy::result_large_err`, `cargo test --workspace --locked`, and `RUSTFLAGS="-D warnings" cargo build --workspace --release --locked` all green.

**2026-05-16 (round 19) — 7 more tests on pic/executor.** The two existing tests covered the `audit` vs `runtime_gate` happy-path semantics via wiremock; the pure-helper surface (constructor invariants, error Display, Trust Plane wire types, Clone semantics) was untouched despite being the layer downstream consumers integrate against. Files:

- [crates/proxy/src/pic/executor.rs](../../crates/proxy/src/pic/executor.rs) — 7 new tests. `dev_ephemeral_yields_distinct_kids_per_call` pins randomness in the dev-mode constructor — a regression that re-seeded the RNG with the process pid would silently re-use the same `proxy-dev-<uuid>` across restarts, breaking chain attribution. `pic_executor_new_round_trips_kid_through_executor_kid_accessor` pins that the production constructor honors the kid parameter rather than overwriting it (the executor_binding on every PoC carries this value — a regression would silently break chain attribution). `executor_error_display_strings_carry_named_field_values` pins thiserror's `{field}` substitution on the structured `Upstream { status, body }` variant + the prefix + pass-through on `Invariant` and `Core` (the dashboard renders these verbatim). `issue_pca_response_deserializes_with_optional_exp_absent` and `process_poc_response_round_trips_ops_and_hop` pin the two Trust Plane response wire shapes — `#[serde(default)]` on `exp` is load-bearing for never-expiring PCAs, and `ops` is the *narrowed* set the verifier compares element-wise so order must round-trip. `register_executor_request_serializes_to_snake_case_pair` pins the upstream contract `kid` + `public_key` (a future rename to `executor_kid` / `verifying_key` would silently break key registration). `pic_executor_clone_shares_inner_arc` pins that adapters' per-handler clones share the `OnceCell<()>` registration cell — duplicate `Arc<Inner>` would cause two adapters to both re-register on first use.

proxy bin tests: **360 → 367**. Cumulative across rounds 1–19: proxy **+201**, cli +17, policy-engine +45, shared-types +7 — **249 new pure-helper unit tests** total. Gate floors unchanged (workspace 45/55, per-crate proxy 45 / shared-types 95 / policy-engine 88 / cli 10). `cargo fmt --all --check`, `cargo clippy --workspace --all-targets --locked -- -D warnings -A clippy::type_complexity -A clippy::too_many_arguments -A clippy::result_large_err`, `cargo test --workspace --locked`, and `RUSTFLAGS="-D warnings" cargo build --workspace --release --locked` all green.

**2026-05-16 (round 20) — 6 more tests on config.** Config validation already had 10 tests, but they exercised the trust-plane URL path twice while skipping the symmetric federation-bridge URL gate, leaving every `ConfigError` Display untested, and never directly hitting `check_http_url`'s field-name round-trip or the `MissingKey` branch (only `MissingCert` was covered). Files:

- [crates/proxy/src/config.rs](../../crates/proxy/src/config.rs) — 6 new tests. `check_http_url_accepts_http_and_https` pins that both schemes pass — operators run `http://` inside trust boundaries (compose / k8s mesh) and `https://` at external load balancers; a regression that locked us to https-only would break the in-cluster path. `check_http_url_rejects_other_schemes_and_surfaces_field_name` walks `ftp://`, `file://`, `ws://`, and the no-scheme case, pinning that the `field` parameter round-trips through the `InvalidValue` variant (Grafana / setup-status keys on it). `build_rejects_non_http_federation_bridge_url` is the symmetric `with_trust_plane_url`-style test for the bridge URL — the existing test only covered trust-plane, so a future refactor that called `check_http_url` once with both arguments wouldn't catch a dropped second check. `build_rejects_token_encryption_key_with_non_hex_chars` covers the alphabet half of the hex-validation predicate (the existing test only covered the length half — `len != 64 || !is_hex` is two failure modes). `build_requires_key_when_only_cert_exists_in_prod_mode` covers the `MissingKey` branch of the `dev_mode == false` cert check — the existing `MissingCert` test pinned the cert path only, so the key path was unexercised. `config_error_display_strings_include_field_or_path_context` pins the operator-facing `Display` substrings for every variant (`InvalidValue`, `MissingCert`, `MissingKey`, `FileLoad`) — the setup-status path renders these verbatim and the troubleshooting docs page keys on them.

proxy bin tests: **367 → 373**. Cumulative across rounds 1–20: proxy **+207**, cli +17, policy-engine +45, shared-types +7 — **255 new pure-helper unit tests** total. Gate floors unchanged (workspace 45/55, per-crate proxy 45 / shared-types 95 / policy-engine 88 / cli 10). `cargo fmt --all --check`, `cargo clippy --workspace --all-targets --locked -- -D warnings -A clippy::type_complexity -A clippy::too_many_arguments -A clippy::result_large_err`, `cargo test --workspace --locked`, and `RUSTFLAGS="-D warnings" cargo build --workspace --release --locked` all green.

**2026-05-16 (round 21) — 6 more tests on adapters/error.** The `body()` helper had only one variant test (the PolicyBlocked extras case); the other six variants each render a distinct operator-facing envelope (different fix hints, detail-presence, docs links) that the dashboard surfaces and that the docs page keys on. Files:

- [crates/proxy/src/adapters/error.rs](../../crates/proxy/src/adapters/error.rs) — 6 new tests. `app_error_body_require_confirmation_carries_reason_and_fix` pins both detail pass-through AND the `X-Proxilion-Confirmation:` header name in the fix hint (the agent reads this to know how to resume). `app_error_body_rate_limit_has_no_detail_but_carries_fix` pins the no-detail/yes-fix posture — surfacing rate-limit policy text into a hot retry loop would leak server state. `app_error_body_pic_invariant_violation_surfaces_detail_to_dashboard` pins that the missing-ops string round-trips through `detail` (the dashboard renders this verbatim). `app_error_body_upstream_too_large_carries_size_hint` pins both the "10MB" cap mention and the `fields=` mitigation in the fix text (two substrings the docs page links). `app_error_body_db_and_internal_collapse_to_internal_error_envelope` pins that BOTH Db and Internal collapse to the same `internal_error` code with NO `detail` set — a regression that started leaking sqlx error text would expose schema names / row counts to the agent. `app_error_body_read_filter_blocked_has_no_detail_but_dashboard_hint` pins the symmetric posture for the read-filter denial — generic to the agent, with the operator hint pointing at `/admin/ → Live feed` for the matched pattern.

proxy bin tests: **373 → 379**. Cumulative across rounds 1–21: proxy **+213**, cli +17, policy-engine +45, shared-types +7 — **261 new pure-helper unit tests** total. Gate floors unchanged (workspace 45/55, per-crate proxy 45 / shared-types 95 / policy-engine 88 / cli 10). `cargo fmt --all --check`, `cargo clippy --workspace --all-targets --locked -- -D warnings -A clippy::type_complexity -A clippy::too_many_arguments -A clippy::result_large_err`, `cargo test --workspace --locked`, and `RUSTFLAGS="-D warnings" cargo build --workspace --release --locked` all green.

**2026-05-16 (round 22) — 8 more tests on api/killswitch + oauth/error.** Two modules whose visible 4xx envelope was already pinned, but whose 5xx/`Db` envelope, killswitch wire-shape `KillResponse`, and the OAuth error `body()` branches (`Upstream`, `BridgeRejected`, the `Db`/`Crypto`/`Internal` collapse, and the docs-linked fix substrings on the four classify-by-detail variants) were not. Files:

- [crates/proxy/src/api/killswitch.rs](../../crates/proxy/src/api/killswitch.rs) — 4 new tests. `api_error_db_collapses_to_500_internal_error_envelope` covers the second `ApiError` arm — operator alerts key on `status="500" code="internal_error"` for a real Postgres outage; a future variant rename would silently re-classify the alert. `kill_response_serializes_with_stable_field_names` pins the wire shape every operator-dashboard killswitch confirmation toast keys on (`record_id` / `scope` / `target` / `bearers_revoked` / `at`) — a future rename (e.g. `bearers_revoked` → `revoked_count`) would silently break the UI. `kill_body_defaults_when_empty_object_is_posted` pins that operator-cli's `{}` body deserializes to all-None — the handler's `body.unwrap_or_default()` path depends on every field being `Option<_>` with a `Default` impl. `kill_body_accepts_confirm_yes_for_kill_all` pins that the deserializer surfaces `confirm: "yes"` — a sloppy serde rename would silently make `confirm` always None and bypass the `/killswitch/all` 400 gate.
- [crates/proxy/src/oauth/error.rs](../../crates/proxy/src/oauth/error.rs) — 4 new tests. `upstream_body_carries_upstream_unavailable_code_and_no_detail` pins the no-detail posture for the shared internal-error body — a sloppy `Internal(format!("token={token}"))` regression upstream would leak secrets to the agent. `bridge_rejected_body_carries_detail_and_federation_docs_link` pins both the operator-safe `detail` pass-through ("token expired" — no secrets) and the `federation-bridge` substring the docs page links. `db_and_crypto_collapse_to_same_internal_error_body` pins that NEITHER variant leaks its Display string into `detail` — a future refactor that passed `e.to_string()` through would expose schema names (Db) or key-handling internals (Crypto). `body_fix_strings_are_actionable_for_unique_variants` pins the operator-docs-linked substrings on `PkceFail` (`code_verifier`), `BadAuthCode` (`single-use`), `SessionGone` (`10 minutes`), and `PicInvariant` (`subset`) — a drift would orphan the docs page link.

proxy bin tests: **379 → 387**. Cumulative across rounds 1–22: proxy **+221**, cli +17, policy-engine +45, shared-types +7 — **269 new pure-helper unit tests** total. Gate floors unchanged (workspace 45/55, per-crate proxy 45 / shared-types 95 / policy-engine 88 / cli 10). `cargo fmt --all --check`, `cargo clippy --workspace --all-targets --locked -- -D warnings -A clippy::type_complexity -A clippy::too_many_arguments -A clippy::result_large_err`, `cargo test --workspace --locked`, and `RUSTFLAGS="-D warnings" cargo build --workspace --release --locked` all green.

**2026-05-16 (round 23) — 8 more tests on api/actions.** The existing 6 tests covered the CSV-line escaper + SSE event builder + two of the four `ActionsApiError` arms by status only — but the two `internal_error` arms' bodies, the pagination envelope's `next_before` key-presence contract, the `PurgeRequest`/`PurgeResponse` wire shape, and the `ListParams` empty-query-string default were unpinned. Files:

- [crates/proxy/src/api/actions.rs](../../crates/proxy/src/api/actions.rs) — 8 new tests. `actions_api_error_db_maps_to_500_internal_error_envelope` pins the full body shape (status + `code` + `error` title + `/healthz` fix substring + `troubleshooting` docs link) for the Postgres-outage arm. `actions_api_error_cache_maps_to_500_with_pca_cache_error_label` pins the distinct `error="pca cache error"` title — operator-dashboard filters split chain-walker faults from generic DB faults on this axis even though both collapse to `code="internal_error"` (no leak surface either way). `actions_api_error_not_found_envelope_carries_docs_link_and_fix` pins the `not_found` shape (operator-cli surfaces the `aged out` substring + `admin/actions` docs link verbatim). `purge_request_dry_run_defaults_to_false_when_omitted` pins the `#[serde(default)]` contract — operator-cli posts `{"older_than":"..."}` for the destructive path and the handler's `if req.dry_run { ... }` branch depends on the False default. `purge_request_dry_run_explicit_true_round_trips` is the explicit-true counterpart. `purge_response_serializes_with_stable_field_names` pins `older_than`/`dry_run`/`deleted` as the wire contract the CLI's purge-confirmation formatter keys on. `list_response_envelope_carries_rows_and_nullable_next_before` pins that `next_before` serializes as `null` (not absent) when the page was shorter than `limit` — the dashboard JS keys on key-presence, not value-presence, to decide whether to render "Load more". `list_params_filters_default_to_none_when_query_string_empty` pins that an empty query string round-trips to all-None so the handler's NULL-bound SQL takes the unfiltered branch (a future field that lost its `Option<_>` would surface as a deserializer error here, before the SQL bind point).

proxy bin tests: **387 → 395**. Cumulative across rounds 1–23: proxy **+229**, cli +17, policy-engine +45, shared-types +7 — **277 new pure-helper unit tests** total. Gate floors unchanged (workspace 45/55, per-crate proxy 45 / shared-types 95 / policy-engine 88 / cli 10). `cargo fmt --all --check`, `cargo clippy --workspace --all-targets --locked -- -D warnings -A clippy::type_complexity -A clippy::too_many_arguments -A clippy::result_large_err`, `cargo test --workspace --locked`, and `RUSTFLAGS="-D warnings" cargo build --workspace --release --locked` all green.

**2026-05-16 (round 24) — 8 more pure-helper tests on forwarder/siem.** The SIEM webhook forwarder had end-to-end wiremock coverage on POST shape + 4xx/5xx retry policy, and two terse tests on `from_hex` / `sign`, but the per-branch error messages, the `BuildError` prefix, the operator-facing length boundaries, and the HMAC primitive identity itself (a refactor that swapped SHA-256 for SHA-1 would silently break every existing receiver) were all unpinned. Files:

- [crates/proxy/src/forwarder/siem.rs](../../crates/proxy/src/forwarder/siem.rs) — 8 new tests. `key_error_display_exposes_inner_string_without_prefix` pins that `KeyError`'s `#[error("{0}")]` is intentional (no prefix added) so the per-branch messages from `from_hex` are what dashboard filters key on directly; matched on the `Result` rather than `unwrap_err`-ed because `KeyError` deliberately has no `Debug`-print of the source key. `build_error_display_carries_siem_forwarder_build_prefix` pins the `siem forwarder build:` prefix on the symmetric `BuildError` so setup-status renders distinguish a key-parse fault from a reqwest::Client construction fault. `from_hex_distinguishes_odd_and_too_short_branches` pins that the two length checks fire in the right order (odd-length first, then `>= 32 chars`) — a regression that collapsed them into "invalid length" would lose the actionable hint. `from_hex_invalid_hex_char_carries_position_index_in_message` pins both the `invalid hex at` prefix and the byte offset (operators triage env-var typos by reading the position). `from_hex_accepts_32_char_minimum_and_rejects_30_just_below` pins the documented 16-byte (32 hex char) minimum on both edges. `sign_matches_rfc4231_test_vector_1_for_hmac_sha256` pins the HMAC primitive identity against the RFC 4231 §4.2 Test Case 1 known vector — a swap to SHA-1 / SHA-384 would silently break every existing SIEM receiver. `sign_diverges_when_key_changes_for_identical_body` pins the symmetric axis the existing `hmac_key_round_trip` test skipped (a stub that hashed the body alone would satisfy the existing test). `sign_is_lowercase_hex_with_fixed_prefix_and_length` pins the `sha256=<64-char-lowercase-hex>` wire shape receivers depend on.

proxy bin tests: **395 → 403**.

**2026-05-16 (round 25) — 8 more pure-helper tests on policy_handle.** `set_mode` has thorough end-to-end coverage (comment preservation, nested-mode-key avoidance, field-insertion ordering, fallback path), but the four private YAML-walker helpers (`strip_eol`, `leading_ws_len`, `parse_id_value`, `find_inline_comment`) and the `SetModeError` `Display` shapes were never pinned directly — a future refactor of the line walker could silently shift behaviour on boundary inputs that the end-to-end tests don't reach (CRLF, mixed-tab indent, single-quoted scalars, comment-only values, URL-with-`#`). Files:

- [crates/proxy/src/policy_handle.rs](../../crates/proxy/src/policy_handle.rs) — 8 new tests. `strip_eol_removes_trailing_newline_only_when_present` pins the naked-string passthrough (the line walker's `split_inclusive('\n')` leaves the last line newline-less and the rewrite must rejoin byte-exact) plus that `\r` is intentionally NOT stripped (CRLF is unsupported; silent drop would mis-align indent calculations). `leading_ws_len_counts_spaces_and_tabs_mixed` pins the byte-accurate length contract on mixed tab/space input (the rewrite preserves the original lead bytes verbatim, so the length must match). `parse_id_value_extracts_bare_and_quoted_scalars` pins all three scalar shapes (bare, double-quoted, single-quoted) the `set_mode` block-finder iterates over. `parse_id_value_rejects_missing_space_after_colon_to_avoid_pid_match` pins the `id:foo` (no space) rejection — without this the walker would hijack `pid:` lines as the block anchor; tab-after-colon is accepted (YAML allows it). `parse_id_value_returns_none_for_empty_or_comment_only_value` pins the three empty-after-strip shapes (`id: `, `id: ""`, `id:  # comment`). `find_inline_comment_requires_preceding_whitespace` pins the no-bare-hash rule that keeps URLs like `https://x#frag` from being mistaken for inline comments — important because policy `match:` rules carry user-supplied strings that may include `#`. `set_mode_error_display_strings_are_stable_for_log_filters` pins the per-variant prefix + payload pass-through for `NotFound` / `Parse` / `Reload` (the troubleshooting docs page keys on these prefixes). `set_mode_via_serde_fallback_round_trips_when_yaml_is_a_flow_mapping` exercises the legacy `serde_yaml` fallback on a flow-mapping input where the line walker wouldn't have found the anchor — a regression that dropped the fallback would surface `NotFound` for any hand-edited flow-style YAML.

proxy bin tests: **403 → 411**. Cumulative across rounds 1–25: proxy **+245**, cli +17, policy-engine +45, shared-types +7 — **293 new pure-helper unit tests** total. Gate floors unchanged (workspace 45/55, per-crate proxy 45 / shared-types 95 / policy-engine 88 / cli 10). `cargo fmt --all --check`, `cargo clippy --workspace --all-targets --locked -- -D warnings -A clippy::type_complexity -A clippy::too_many_arguments -A clippy::result_large_err`, `cargo test --workspace --locked`, and `RUSTFLAGS="-D warnings" cargo build --workspace --release --locked` all green.

**2026-05-16 (round 26) — 8 more pure-helper tests on notifier/slack.** The Slack notifier already had end-to-end coverage on signed-request verification + Block Kit payload shape + user-map resolution + summary block layout, but the operator-facing `SlackBuildError` prefix, the `plural`/`truncate` helpers powering the message copy, the `reject` button verb (only `approve` and `why` were round-tripped), the bad-UUID rejection path through `parse_button_value`, the `SlackAction` `Copy`/`PartialEq` traits the interaction-webhook router depends on, and the empty-map default of `resolve_user` were all unpinned. Files:

- [crates/proxy/src/notifier/slack.rs](../../crates/proxy/src/notifier/slack.rs) — 8 new tests. `slack_build_error_display_carries_slack_build_prefix` pins the `slack build:` prefix so setup-status renders distinguish a Slack build fault from a webhook or SIEM build fault. `plural_renders_singular_at_one_and_pluralizes_zero_and_many` pins the `n == 1 → "1 block"`, else `"N blocks"` shape on three boundaries (0, 1, 2+) — a regression that treated only `n == 0` as plural (the natural off-by-one) would surface here as `"0 block"`. `truncate_passes_short_through_and_ellipsizes_at_overflow` pins both the exact-limit no-ellipsis case (the `>` strict-greater predicate) and the single horizontal-ellipsis codepoint on overflow (a regression that emitted `...` instead would break the 140-char visual width budget the context block depends on). `truncate_uses_char_count_not_byte_len_for_multibyte_input` pins UTF-8 safety on Greek letters — a byte-length truncation would split a codepoint and panic the serializer downstream. `parse_button_value_round_trip_reject_action` covers the third verb the existing tests skipped (the destructive action that the interaction webhook routes on identity). `parse_button_value_rejects_known_action_with_bad_uuid` pins the second-half failure mode — verb match succeeds but UUID parse fails, including the `approve:` empty-tail edge and the multi-colon (`approve:abc:def`) edge `split_once` masks. `slack_action_copy_and_eq_traits_work_at_use_sites` pins `Copy + PartialEq + Eq` on the variant (the router moves and compares without explicit `.clone()` — dropping a trait would surface here as a compile error rather than as confusing call-site failures). `resolve_user_returns_none_when_map_is_empty_or_no_inputs` pins that `SlackNotifier::new` builds an empty map (every lookup → None → caller falls back to `slack:<username>`) — a regression that pre-seeded a default mapping would attribute overrides to the wrong subject; piggybacks a `proxy_public_url()` accessor round-trip while a notifier is already in hand.

proxy bin tests: **411 → 419**.

**2026-05-16 (round 27) — 6 more pure-helper tests on notifier/burst.** The suppressor already had 11 tests covering admit/drain/resolver semantics, but the documented `BurstConfig` default numbers (which the troubleshooting docs commit to verbatim), the `BurstSummary::SCHEMA` version-suffix contract, the `#[serde(skip_serializing_if = "String::is_empty")]` key-presence axis that drives the "Open full list" button render, the multi-trailing-slash defense in `with_details_url`, the `flush_interval()` accessor the background flush loop reads, and the `Clone` invariant the notifier/flush-loop split depends on were all unpinned. Files:

- [crates/proxy/src/notifier/burst.rs](../../crates/proxy/src/notifier/burst.rs) — 6 new tests. `burst_config_default_pins_documented_threshold_window_and_flush` pins `threshold=50 / window=60s / flush_interval=30s` — operators read these out of the troubleshooting docs and a tempting "more headroom" bump would silently change suppression behavior on every existing install at next restart. `burst_summary_schema_is_versioned_string_consumers_key_on` pins both the literal `"proxilion.blocked_action_burst.v1"` value and the `.v1` suffix — webhook/Slack receivers route on the schema string and a coordinated bump is required. `burst_summary_json_skips_empty_details_url_via_serde_attr` pins the load-bearing serde attribute — receivers key on key-presence to decide whether to render "Open full list"; a drift to always-serialize would render a button pointing at the empty string for test fixtures / installs without a public URL. `details_url_collapses_multiple_trailing_slashes_in_base` pins the `trim_end_matches('/')` defense against operator base-URL typos like `https://proxy.local///` (the natural shape of concatenated env-var fragments). `flush_interval_round_trips_through_getter` pins that the getter returns the configured value, not a hard-coded default — a regression here would silently make per-install overrides no-ops. `burst_suppressor_clone_shares_bucket_state` pins that `BurstSuppressor::clone` shares the underlying `Arc<Mutex<HashMap<…>>>` so both the notifier (admit) and the flush loop (drain_summaries) see the same buckets — a deep-copy regression would leave the flush loop forever empty.

proxy bin tests: **419 → 425**. Cumulative across rounds 1–27: proxy **+259**, cli +17, policy-engine +45, shared-types +7 — **307 new pure-helper unit tests** total. Gate floors unchanged (workspace 45/55, per-crate proxy 45 / shared-types 95 / policy-engine 88 / cli 10). `cargo fmt --all --check`, `cargo clippy --workspace --all-targets --locked -- -D warnings -A clippy::type_complexity -A clippy::too_many_arguments -A clippy::result_large_err`, `cargo test --workspace --locked`, and `RUSTFLAGS="-D warnings" cargo build --workspace --release --locked` all green.

**2026-05-16 (round 28) — 5 more pure-helper tests on blocked::canonical_request_json.** The 6 existing tests covered the happy-path JSON shape + truncation-fired branch + determinism, but the truncation envelope's *content* (does it elide `body` + `path_params` like the docstring promises?), the just-below-cap boundary (the `<=` predicate vs `<`), the cross-process determinism (alphabetical key sort inside nested maps), the empty-maps render (`body: {}` vs `null`), and the `BlockedActionRecord` `Clone` derive were unpinned. Files:

- [crates/proxy/src/blocked.rs](../../crates/proxy/src/blocked.rs) — 5 new tests. `truncation_envelope_elides_body_and_path_params` pins that the truncated envelope carries only the (method, path, vendor, action) identification quad — re-including the bloated body/path_params would defeat the size-cap and reintroduce the audit-log problem this envelope solves. `just_below_cap_passes_through_unchanged` pins the `<=` predicate on the just-under-4KB boundary — a regression to `<` would silently start truncating exact-at-limit rows that fit fine today. `nested_body_keys_render_in_alphabetical_order` pins serde_json's sort so the audit log is reproducible across HashMap iteration orderings (Rust's HashMap is randomized per-process; without sort, the same logical request would render differently across restarts and break the approver UI's deterministic-diff workflow). `empty_maps_render_as_empty_objects_not_null` pins `body: {}` + `path_params: {}` on the most-common shape (read endpoints) so the approver UI can distinguish "adapter opted into 0 fields" from "field absent (older schema)". `blocked_action_record_clone_preserves_borrowed_fields` pins that the `Clone` derive survives across all 14 fields including the lifetime'd `&'a [String]` and the `Option<u32>`/`Option<String>` shapes — a future refactor to `Cow<'a, str>` would surface here.

proxy bin tests: **425 → 430**.

**2026-05-16 (round 29) — 7 more pure-helper tests on insert_proxy_headers across all three Google adapters.** The drive adapter had one positive test (`proxy_headers_present`); gmail + calendar had none. The `None`-policy branch and the `if let Ok(v)` invalid-header-value defense were unpinned on all three. The three helpers are byte-identical — without coverage they could drift independently on the next refactor. Files:

- [crates/proxy/src/adapters/google_drive.rs](../../crates/proxy/src/adapters/google_drive.rs) — 2 new tests. `insert_proxy_headers_omits_policy_header_when_no_match` pins that `matched_policy_id: None` (the read-filter / default-deny shape) produces NO `x-proxilion-policy` header — Grafana panels separate "policy fired" from "no match" on header presence, so emitting an empty string would mis-bucket. `insert_proxy_headers_skips_invalid_header_value_silently` pins the `if let Ok(v)` defense — a policy id with an embedded newline (extreme but possible via hand-edited YAML) must drop the header rather than panic the response path.
- [crates/proxy/src/adapters/google_gmail.rs](../../crates/proxy/src/adapters/google_gmail.rs) — 2 new tests with the same shape, against the gmail-local helper (cross-adapter parity guard — three identical helpers must drift together).
- [crates/proxy/src/adapters/google_calendar.rs](../../crates/proxy/src/adapters/google_calendar.rs) — 3 new tests (this adapter had ZERO tests on the helper). `insert_proxy_headers_round_trip_carries_request_pca_and_policy` is the missing positive test (first time the calendar dashboard's request inspector header set is pinned), plus the same two `None` and invalid-value branches as drive/gmail. Adds a small `outcome_with(policy_id)` test helper since calendar lacked one.

proxy bin tests: **430 → 437**. Cumulative across rounds 1–29: proxy **+271**, cli +17, policy-engine +45, shared-types +7 — **319 new pure-helper unit tests** total. Gate floors unchanged (workspace 45/55, per-crate proxy 45 / shared-types 95 / policy-engine 88 / cli 10). `cargo fmt --all --check`, `cargo clippy --workspace --all-targets --locked -- -D warnings -A clippy::type_complexity -A clippy::too_many_arguments -A clippy::result_large_err`, `cargo test --workspace --locked`, and `RUSTFLAGS="-D warnings" cargo build --workspace --release --locked` all green.

**2026-05-16 (round 30) — 8 more pure-helper tests on policy-engine ops.** The 7 existing tests covered `resolve_one` + `resolve` end-to-end, but `OpsAtom::parse`'s four malformed branches (no-colon, one-colon, empty-scheme, empty-action, empty-object), `OpsAtom::to_canonical`'s round-trip identity with `parse`, the colons-and-slashes-in-object passthrough (Gmail's `gmail:send:user:to:domain` shape), the `OpsParseError` Display strings, `MissingOps` Display surfacing each atom, the empty-required Layer-A passthrough, the private `collect_vars` ordering, and `substitute`'s literal-passthrough + unclosed-var error were all unpinned. Files:

- [crates/policy-engine/src/ops.rs](../../crates/policy-engine/src/ops.rs) — 8 new tests. `ops_atom_parse_rejects_each_malformed_shape` walks all 5 distinct failure modes — a future refactor that collapsed the colon-count + empty-segment checks would lose them. `ops_atom_parse_keeps_extra_colons_and_slashes_in_object` pins the Gmail/Drive object-shape contract — a regression that split on every colon would silently truncate ops. `ops_atom_to_canonical_round_trips_through_parse` pins the inverse identity (the proxy normalizes PCA ops before chain comparison; a format drift would silently break leaf matching). `ops_parse_error_display_strings_carry_operator_facing_hints` pins both variants' rendered substrings (`scheme:action:object` shape hint + `template variable` prefix + the unknown-var name). `missing_ops_display_surfaces_each_missing_atom` pins that the adapter's 422 response body (read from `Display`) carries every missing atom. `is_satisfied_by_empty_required_is_ok_against_any_leaf` pins the policy-gated-only-on-Layer-B passthrough — a regression that errored on empty required-ops would mass-deny these. `collect_vars_returns_each_var_in_left_to_right_order` pins the doc-committed left-to-right discovery (a HashSet refactor would silently lose order + the first-offending-var error). `substitute_passes_literal_through_and_rejects_unclosed_var` pins the no-var passthrough + the operator-facing unclosed-`${...` error.

policy-engine lib tests: **103 → 111**.

**2026-05-16 (round 31) — 6 more tests on blocked_expiry + policy-engine decision.** Two narrow targets the prior rounds skipped. `blocked_expiry.rs` was at **0 tests** despite its `DEFAULT_TICK_INTERVAL` constant being load-bearing for the operator-alert "expected expiry window" assumption, and despite its `Default + Clone + Debug` derives on two report structs being the contract the sweep loop relies on. `decision.rs` had Pattern + Decision serde coverage but had never pinned `QuarantineAction`'s `Copy + PartialEq` traits (the dispatcher matches by value), `ReadFilter`'s `Clone` independence, or the empty-needle literal-match edge. Files:

- [crates/proxy/src/blocked_expiry.rs](../../crates/proxy/src/blocked_expiry.rs) — 3 new tests (first tests on this module). `default_tick_interval_pinned_at_60_seconds` pins the 60s constant — a regression that loosened to 5 minutes would silently widen the operator-alert response window. `expiry_sweep_report_default_is_zero_and_clone_independent` pins `Default + Clone + Debug` on the report (Default must be `0` not a sentinel; Clone must yield independent values — a future `Arc<u64>` refactor would surface here). `escalation_sweep_report_default_is_zero_and_debug_carries_field` is the symmetric guard on the sibling `EscalationSweepReport`.
- [crates/policy-engine/src/decision.rs](../../crates/policy-engine/src/decision.rs) — 3 new tests. `quarantine_action_copy_and_eq_traits_work_at_use_sites` pins `Copy + PartialEq` across all three variants (the per-pattern processor passes the action down without `.clone()` — dropping `Copy` would surface here as a compile error rather than confusing failures). `read_filter_clone_carries_patterns_and_action_independently` pins that the cloned `Vec<Pattern>` is independent of the original (no `Rc`/`Arc` smuggled into the inner vec — a regression there would couple per-request engine snapshots). `literal_pattern_matches_empty_haystack_against_empty_needle_only` pins the `str::contains` empty-needle semantic — documents current behaviour as a load-bearing edge for any catch-all-marker policy use.

proxy bin tests: **437 → 440**; policy-engine lib tests: **111 → 114**. Cumulative across rounds 1–31: proxy **+274**, cli +17, policy-engine **+56**, shared-types +7 — **333 new pure-helper unit tests** total. Gate floors unchanged (workspace 45/55, per-crate proxy 45 / shared-types 95 / policy-engine 88 / cli 10). `cargo fmt --all --check`, `cargo clippy --workspace --all-targets --locked -- -D warnings -A clippy::type_complexity -A clippy::too_many_arguments -A clippy::result_large_err`, `cargo test --workspace --locked`, and `RUSTFLAGS="-D warnings" cargo build --workspace --release --locked` all green.

**2026-05-16 (round 32) — 6 more pure-helper tests on policy-engine yaml schema wire shape.** The 8 existing tests covered `parse_policies` end-to-end but each schema enum's wire string was only pinned indirectly (via a single happy-path round-trip in `parse_policies_round_trips_observe_mode_and_runtime_gate`). The `PicMode` kebab-case vs `Mode` / `AuditBodyMode` / `QuarantineActionCfg` snake-case split is the classic serde rename-attribute confusion bug — a copy-paste of `rename_all = "snake_case"` onto `PicMode` would silently break every existing policy YAML carrying `runtime-gate`. The `deserialize_string_or_vec_opt` helper had only one of its three shapes pinned in `recipients_cfg_accepts_string_or_vec`. `ReadFilterCfg` was only tested via fully-populated YAML, never with `{}` to exercise both inner `#[serde(default)]` attributes. Files:

- [crates/policy-engine/src/yaml.rs](../../crates/policy-engine/src/yaml.rs) — 6 new tests. `mode_wire_strings_pin_snake_case_per_variant` pins all three `Mode` strings (the dashboard's policy-mode toggle posts these exact bytes). `pic_mode_serializes_kebab_case_not_snake_case` is the highest-risk wire-shape pin in this module — `RuntimeGate` MUST serialize as `runtime-gate` (kebab), and a snake-case input MUST be rejected by the closed-enum deserializer. `audit_body_mode_wire_strings_are_snake_case` pins `RedactPii → "redact_pii"` (snake, NOT kebab) — distinguishes from PicMode above to catch an accidental copy-paste between the two `rename_all` attributes. `quarantine_action_cfg_wire_strings_are_snake_case` pins all three action variants (mirrored from the engine-internal `QuarantineAction` in decision.rs but kept as a separate schema type so wire shape can evolve independently). `deserialize_string_or_vec_opt_accepts_none_string_and_array` pins all three shapes side-by-side via a local `Wrap` struct that documents the helper's contract directly. `read_filter_cfg_minimal_yields_empty_patterns_and_default_action` pins that `read_filter: {}` deserializes cleanly with empty patterns + `replace_with_marker` default — a regression that made either inner field required would break every existing policy with a stub block.

policy-engine lib tests: **114 → 120**.

**2026-05-16 (round 33) — 5 more pure-helper tests on notifier/email.** The `html_escape` helper had ONE test (`<script>`-shape XSS) — the other four HTML entity arms (`>`, `&`, `"`, `'`) had no direct coverage, and the no-double-encoding semantic (this is a one-shot byte mapping, NOT a parser) was unpinned. The `EmailNotifier` builder + `proxy_public_url` accessor had no direct round-trip — the escalation sweeper reads this URL to build approve/reject links, so a regression that returned the SMTP URL instead would silently send approvers to the SMTP host. Files:

- [crates/proxy/src/notifier/email.rs](../../crates/proxy/src/notifier/email.rs) — 5 new tests. `html_escape_covers_every_dangerous_entity` walks all five entities individually plus a mixed-entity string in order — pins specifically that the apostrophe escape is `&#39;` (numeric) not `&apos;` (named), since older mail UAs render the two differently. `html_escape_passes_plain_text_and_unicode_through_unchanged` pins the no-spurious-wrapping case + Unicode passthrough (`αβγ — délicieux`) so non-ASCII policy descriptions render correctly. `html_escape_does_not_double_encode_already_escaped_entities` pins the one-shot byte mapping semantic — `&amp;` becomes `&amp;amp;` rather than being parsed and re-encoded, so a future "smart escape" refactor would surface here. `email_notifier_proxy_public_url_round_trips_through_accessor` pins the load-bearing accessor field-name round trip the escalation sweeper depends on. `email_notifier_with_max_retries_is_a_fluent_setter` pins the consuming-self builder shape (`fn(mut self, n) -> Self`) — a refactor to `&mut self` would surface here as a compile error rather than break test setup at every call site.

proxy bin tests: **440 → 445**. Cumulative across rounds 1–33: proxy **+279**, cli +17, policy-engine **+62**, shared-types +7 — **344 new pure-helper unit tests** total. Gate floors unchanged (workspace 45/55, per-crate proxy 45 / shared-types 95 / policy-engine 88 / cli 10). `cargo fmt --all --check`, `cargo clippy --workspace --all-targets --locked -- -D warnings -A clippy::type_complexity -A clippy::too_many_arguments -A clippy::result_large_err`, `cargo test --workspace --locked`, and `RUSTFLAGS="-D warnings" cargo build --workspace --release --locked` all green.

**2026-05-17 (round 34) — 8 more pure-helper tests on audit_body PII redactor.** The 9 existing tests covered each redactor regex's happy path (email / SSN / phone / credit card / bearer / api key) plus the binary-input and text-input branches of `redact_pii_bytes`, but several pure-helper boundaries the operator-facing audit pipeline depends on were unpinned: the empty-input SHA-256 vector (absence-of-body still writes a row per the docstring, so the empty-hash is on the wire contract), the `base64_encode` round-trip identity (a refactor to URL-safe alphabet would silently corrupt every persisted body), the order-of-precedence guarantee that `api_key` runs before `phone` (a copy-paste of the call order would chop Slack tokens into `<REDACTED_PHONE>` fragments and leak the rest), the invalid-UTF-8 passthrough (the regex engine operates on `&str` — a lossy conversion would mutate the audit body), and the 256-byte boundary of the binary-detection scan (`take(256).any(b == 0)` — a null byte beyond byte 256 must NOT classify as binary). Files:

- [crates/proxy/src/audit_body.rs](../../crates/proxy/src/audit_body.rs) — 8 new tests. `sha256_hex_empty_input_matches_known_digest_and_is_lowercase_width_64` pins the RFC-known `e3b0c4…` empty-string digest + the width-64 + lowercase-hex invariants (a regression that emitted upper-case or special-cased empty to `""` would break SQL JOINs against the `request_hash` column). `base64_encode_round_trips_through_decoder` pins STANDARD-alphabet identity across empty / ASCII / binary inputs (a switch to `URL_SAFE` would silently break every existing replay tool). `redact_pii_text_preserves_non_pii_surroundings_around_match` pins that the replace_all calls preserve prefix/suffix bytes (a regression that replaced the whole string would mask data loss). `redact_pii_text_replaces_multiple_pii_kinds_in_one_pass` pins that a single call handles email + phone + SSN together (an early-return-after-first-match refactor would surface here). `redact_pii_text_api_key_runs_before_phone_so_slack_token_is_not_split` pins the load-bearing order-of-operations guarantee from the in-file comment ("known token shapes before generic digit-pattern redactors") — Slack tokens carry a 10-digit workspace id that the phone regex would otherwise eat. `redact_pii_bytes_passes_invalid_utf8_through_unchanged` pins the `std::str::from_utf8` error branch (a regression that switched to `from_utf8_lossy` would mutate audit bytes). `redact_pii_bytes_binary_detection_only_scans_first_256_bytes` pins the `take(256)` boundary — a null byte at offset 256+ must NOT classify as binary, since most JSON bodies pad well past 256 bytes of human-readable text before any embedded NUL. `redact_pii_text_empty_input_yields_empty_output` pins the trivial empty-passthrough (a sentinel-injecting regression would surface here).

proxy bin tests: **445 → 453**.

**2026-05-17 (round 35) — 3 more pure-helper tests on session.rs.** The 4 existing tests covered the Debug-redaction of `google_access_token`, the 401 status on `SessionExtractError`, the missing-extension extractor branch, and the present-extension extractor branch. But the symmetric Debug-omission of the *other* sensitive fields (`bearer_hash` — the SHA-256 the killswitch SQL predicate keys on; `leaf_pca_cbor` — the raw signed-PCA bytes) was unpinned despite the Debug impl deliberately listing only a subset of fields. The `SessionCtx::clone` Arc-sharing invariant the per-handler fan-out depends on was unpinned (a refactor that dropped the `Arc<_>` wrapper would silently deep-copy the context on every clone). The fixed-body `unauthorized` content length was only checked by byte-equality with no length pin, so a trailing newline / JSON wrapper added to the body wouldn't surface as a wire-shape change. Files:

- [crates/proxy/src/session.rs](../../crates/proxy/src/session.rs) — 3 new tests. `debug_omits_bearer_hash_and_leaf_pca_cbor_to_avoid_leaking_credential_material` pins that the Debug impl never renders the two sensitive byte fields — operator log aggregators index `Debug`-formatted structs and accidentally including the killswitch hash would let an attacker who reads logs construct a kill-row, while leaking the signed PCA CBOR would expose chain-attribution bytes. `session_ctx_clone_shares_arc_with_original` pins that `#[derive(Clone)]` on `SessionCtx(pub Arc<SessionContext>)` Arc-shares the inner context across spawned tasks (a refactor to `SessionCtx(pub SessionContext)` would surface here as an `Arc::ptr_eq` failure rather than as a silent fan-out performance regression). `session_extract_error_body_is_exactly_twelve_bytes` pins the body length so an appended CRLF / JSON wrapper / HTML envelope on the fixed-body 401 path would surface here — operator alerts key on this 401 rate as the "agent session lost" signal and log parsers depend on the exact byte shape.

proxy bin tests: **453 → 456**. Cumulative across rounds 1–35: proxy **+290**, cli +17, policy-engine **+62**, shared-types +7 — **355 new pure-helper unit tests** total. Gate floors unchanged (workspace 45/55, per-crate proxy 45 / shared-types 95 / policy-engine 88 / cli 10). `cargo fmt --all --check`, `cargo clippy --workspace --all-targets --locked -- -D warnings -A clippy::type_complexity -A clippy::too_many_arguments -A clippy::result_large_err`, `cargo test --workspace --locked`, and `RUSTFLAGS="-D warnings" cargo build --workspace --release --locked` all green.

**2026-05-17 (round 36) — 8 more pure-helper tests on google_gmail MIME + body-context helpers.** The 13 existing tests covered `decode_b64url`, the parse_mime happy path, two `body_ctx` flag axes, the attachment count, the `domain_of` lowercase round-trip, and the two `insert_proxy_headers` branches added in round 29. But the `domain_of` whitespace trimming + empty-input branches were unpinned (a regression that dropped the `.trim()` would silently miss `customer_domain` matches against hand-edited MIME), `split_addresses` was never tested directly (the group-form `team:bob@x,alice@y;` shape that some MUAs emit must flatten through `MailAddr::Group` — a refactor that handled only `Single` would silently drop grouped recipients), the `BTreeSet` de-dup behavior in `build_send_body_ctx` was never asserted (a switch to `HashSet` would silently randomize which domain lands as `to_domain` and break the spec.md §9 single-value `not_in` comparison), the `subject_present` Some-vs-None axis was unpinned, the `Bcc:` header was never round-tripped through `body_ctx`, and `count_parts` was only tested against a single-level multipart (a nested multipart/related → application/pdf attachment — the shape every inline-image-bearing email produces — would have surfaced a stop-at-first-level recursion bug). Files:

- [crates/proxy/src/adapters/google_gmail.rs](../../crates/proxy/src/adapters/google_gmail.rs) — 8 new tests. `domain_of_trims_surrounding_whitespace_on_domain_half` pins `.trim().to_ascii_lowercase()` on the rsplit_once split right-half. `domain_of_returns_none_on_empty_string_and_bare_at` pins the two boundary inputs operators occasionally encounter on hand-edited MIME. `split_addresses_handles_group_form_and_returns_inner_addresses` pins the `MailAddr::Group` flattening contract — a refactor that only matched `Single` would silently drop every member of `team:bob@a,carol@b;`. `split_addresses_returns_empty_on_empty_input` pins the empty-input passthrough so downstream `recipient_count` arithmetic stays sound. `build_send_body_ctx_dedupes_domains_via_btreeset_for_alphabetical_to_domain` pins both the de-dup (3 recipients across 2 domains → `to_domains.len() == 2`) and the alphabetical-first `to_domain` (the spec.md §9 contract — a HashSet refactor would silently randomize). `build_send_body_ctx_subject_present_round_trips_some_and_none` pins that the header-presence flag is on header-presence (not value-non-emptiness) — the policy author's contract. `parse_mime_carries_bcc_recipients_through_to_body_ctx` pins that bcc-only external recipients flip `external_recipient` (covers a real exfil shape the existing to/cc tests skip). `count_parts_recurses_into_nested_multipart_attachments` pins the recursive descent through multipart/mixed → multipart/related → application/pdf — every inline-image-bearing email composes this way and a stop-at-first-level refactor would silently undercount.

proxy bin tests: **456 → 464**.

**2026-05-17 (round 37) — 6 more pure-helper tests on api/policy listing + set_mode envelope.** The 7 existing tests covered `parse_listing` happy paths + defaults + skip-no-id + malformed-yaml + empty-input + the `PolicyView` JSON shape + `SetModeBody` happy-path deserialization. But the boundary where YAML is a single mapping (not a top-level array — the natural typo when an operator drops the leading `-`) was unpinned, the mixed scalar + mapping array (defensive against hand-edited files where one entry is just a string) was unpinned, the `ListResponse` envelope's wire-key shape (the three keys the admin UI's policy table renders against) was unpinned, the `source: null` rendering on static/embed-API loaders (key-presence vs key-absence) was unpinned, the `SetModeBody` missing-`mode` rejection (the fail-fast contract that prevents a `{}` body from silently flipping a policy back to enforce) was unpinned, and the forward-compat axis (extra fields don't 400 — the CLI can send unknown keys) was unpinned. Files:

- [crates/proxy/src/api/policy.rs](../../crates/proxy/src/api/policy.rs) — 6 new tests. `parse_listing_returns_empty_when_yaml_is_single_mapping_not_array` pins the `Err`-arm of the top-level Vec<Value> deserialize → empty listing (no 500). `parse_listing_drops_non_mapping_entries_in_array` pins the filter_map-by-`as_mapping` defense that keeps a single hand-edited bad entry from hiding every valid policy. `list_response_json_carries_source_policy_count_and_policies_keys` pins the three wire keys the admin UI keys on (a future rename would silently break the policy table). `list_response_json_renders_null_source_when_loader_has_no_path` pins the JSON-null (not key-absent) rendering — the UI distinguishes "in-memory policies" from "file path dropped on refactor" on key-presence. `set_mode_body_rejects_missing_mode_field` pins the fail-fast contract on empty `{}` — preventing a silent "default to enforce" fall-through. `set_mode_body_accepts_unknown_extra_fields` pins serde's permissive default so CLI forward-compat fields don't 400.

proxy bin tests: **464 → 470**. Cumulative across rounds 1–37: proxy **+304**, cli +17, policy-engine **+62**, shared-types +7 — **369 new pure-helper unit tests** total. Gate floors unchanged (workspace 45/55, per-crate proxy 45 / shared-types 95 / policy-engine 88 / cli 10). `cargo fmt --all --check`, `cargo clippy --workspace --all-targets --locked -- -D warnings -A clippy::type_complexity -A clippy::too_many_arguments -A clippy::result_large_err`, `cargo test --workspace --locked`, and `RUSTFLAGS="-D warnings" cargo build --workspace --release --locked` all green.

**2026-05-17 (round 38) — 6 more pure-helper tests on google_calendar `domain_of` divergence + `urlencoding` + `build_event_body_ctx` boundaries.** The 9 existing tests covered `domain_of`'s happy path + case-folding, `urlencoding`'s `/` `#` + space cases, and three `body_ctx` variants (external, internal-only, no-attendees) plus the three `insert_proxy_headers` arms from round 29. But the load-bearing divergence between calendar's `domain_of` and gmail's (calendar has an extra `.filter(|s| !s.is_empty())` step — a "unification" refactor would silently change either contract; the bare `@` case must land as None so an empty-domain attendee never flips `external_attendee`) was unpinned. `urlencoding` had no per-byte coverage of the PATH set (`&` `%` `?` were never asserted individually — a copy-paste from a query-string encoder that dropped `&` would silently break calendar IDs containing `&` against Google's path parser), the multibyte UTF-8 byte-walk was unpinned (`é` → `%C3%A9`), and empty-input identity was unpinned. The `body_ctx` asymmetry between `visibility` (key-absent when source missing) and `summary_present` (always-present boolean) was inferred only via the no_attendees test rather than asserted directly. The case-insensitive customer-domain match was unpinned despite operators sometimes carrying mixed-case `customer_domain` in YAML. Files:

- [crates/proxy/src/adapters/google_calendar.rs](../../crates/proxy/src/adapters/google_calendar.rs) — 6 new tests. `domain_of_filters_empty_after_trim_unlike_gmail_variant` pins the extra `.filter(!is_empty)` step on bare-`@` / empty / whitespace-only-domain inputs, and explicitly contrasts the contract with the gmail variant. `urlencoding_passes_unreserved_through_and_encodes_path_set_members` pins all six PATH-set bytes individually (` ` `/` `?` `#` `&` `%`) plus the RFC 3986 unreserved-set passthrough. `urlencoding_handles_multibyte_utf8_and_empty_input` pins the empty→empty identity + the `résumé` two-byte UTF-8 encoding. `body_ctx_visibility_absent_renders_no_visibility_key` pins the `if let Some` branch — key absent when source is missing (the policy engine's `body.visibility` differentiates absent from null). `body_ctx_summary_absent_renders_false_not_missing_key` pins the asymmetric always-present boolean for `summary_present` so a refactor that "unified" both into the absent-key pattern would surface here. `body_ctx_external_attendee_case_insensitive_against_customer_domain` pins the `eq_ignore_ascii_case` symmetric guard on both axes (attendee email AND customer_domain mixed-case) — a regression would silently flag every internal recipient as external when the YAML carried mixed case.

proxy bin tests: **470 → 476**.

**2026-05-17 (round 39) — 4 more pure-helper tests on api/notifier_public HTML escaping + form rendering.** The 9 existing tests covered the bulk `html_escape` payload attack vector, six `render_*` Response-level checks (form approve/reject/unknown, validation error, already-used, error), and two `fill_template` placeholder-substitution cases. But the no-double-encoding semantic on `&amp;` was unpinned (a "smart" entity-aware refactor would silently under-escape on re-render, reintroducing XSS surface — same contract pinned for the email helper in round 33 but never asserted on this distinct module-local helper), the unicode/empty-input passthrough was unpinned, the stored-XSS surface in `path` + `detail` + `p_0` (user-controlled fields rendered through `fill_template`) was unpinned (a regression that swapped one `replace` for a raw insertion would reopen the surface), and the load-bearing contract that the reject form posts to the SAME `/notifier/approve` endpoint as approve (the handler routes on the `action` field from the TokenRow, not on the URL — a split-endpoint refactor would silently break every reject link) was unpinned. Files:

- [crates/proxy/src/api/notifier_public.rs](../../crates/proxy/src/api/notifier_public.rs) — 4 new tests. `html_escape_does_not_double_encode_already_escaped_entities` pins the single-pass byte-mapping semantic — `&amp;` → `&amp;amp;` rather than being parsed as an already-escaped entity. `html_escape_passes_unicode_and_empty_through_unchanged` pins the `_ => out.push(c)` fallthrough + the empty-input zero-allocation boundary. `fill_template_html_escapes_path_and_detail_to_prevent_xss_in_stored_fields` pins the stored-XSS guard on `path` (`<img src=x onerror=alert(1)>`), `detail` (`</textarea><script>steal()</script>`), and `p_0` (`eve<script>@evil.example`) — three user-controlled fields rendered into the approver UI. `render_form_reject_action_targets_approve_endpoint_with_token_id` pins that the reject form posts to `/notifier/approve` (same endpoint as approve) and round-trips the token id verbatim — the handler-routing-on-action-field contract.

proxy bin tests: **476 → 480**. Cumulative across rounds 1–39: proxy **+314**, cli +17, policy-engine **+62**, shared-types +7 — **379 new pure-helper unit tests** total. Gate floors unchanged (workspace 45/55, per-crate proxy 45 / shared-types 95 / policy-engine 88 / cli 10). `cargo fmt --all --check`, `cargo clippy --workspace --all-targets --locked -- -D warnings -A clippy::type_complexity -A clippy::too_many_arguments -A clippy::result_large_err`, `cargo test --workspace --locked`, and `RUSTFLAGS="-D warnings" cargo build --workspace --release --locked` all green.

**2026-05-17 (round 40) — 6 more pure-helper tests on pic/violations.rs.** The 7 existing tests covered `parse_missing_atoms`'s happy path + quoted form + empty/no-bracket fallbacks + single-value (no comma) + unclosed-bracket + whitespace-trim. But several adversarial inputs the Trust Plane has actually been observed emitting were unpinned: the **two-bracket-pair body** (`missing [a, b] (predecessor had [x, y])`) where the parser must use the FIRST `[`/`]` pair only and ignore the trailing list (a refactor that scanned every bracket pair would silently merge the two and surface phantom "missing" atoms the operator hasn't actually been denied), the **close-before-open** malformed body (`] orphan close before [a:1]`) where the two-step `find('[')` then `find(']')` must not panic and must locate the SECOND `]` (a refactor to `detail.find(']')` first would flip the contract), the **mixed-quote** form where one atom is single-quoted and another double-quoted (the `trim_matches` closure must strip BOTH — hardcoding `'"'` only would leak a leading `'` into the atom string), the **whitespace-only segments** case (`[ , , a:1, ]` — the trailing-comma + leading-comma compound a single test had skipped), and the `PicViolationRecord::Clone` derive itself (load-bearing because several adapter call sites build one Record and pass it to two sinks — a refactor that dropped the derive would force a borrow-checker rewrite). Files:

- [crates/proxy/src/pic/violations.rs](../../crates/proxy/src/pic/violations.rs) — 5 new tests. `parse_atoms_uses_first_bracket_pair_only_ignoring_trailing_brackets` pins the FIRST-pair contract against a `missing [a, b] (predecessor had [x, y])` shape. `parse_atoms_close_before_open_returns_empty_without_panic` pins the two-step `find('[')` then `find(']')` against `] orphan close before [a:1]` (yields one atom — the second `]` closes the bracketed pair) and `] orphan with no open` (yields zero — no `[`, fast-path). `parse_atoms_mixed_single_and_double_quotes_strip_both` pins the `trim_matches(|c| c == '"' || c == '\'')` closure against `["a:b:c", 'd:e:f', g:h:i]` — all three must surface unquoted. `parse_atoms_whitespace_only_segments_are_dropped` pins `[ , , a:1, ]` collapses to a one-element vec via `.filter(|s| !s.is_empty())`. `pic_violation_record_clone_preserves_all_borrowed_slices` pins the `Clone` derive end-to-end across the 13-field struct (5 `&str` borrows + 2 `Uuid` + 2 `Option` + a `&'static str` mode tag + 2 `&[String]` slices).

proxy bin tests: **480 → 485**.

**2026-05-17 (round 41) — 4 more pure-helper tests on api/setup.rs CheckItem/SetupStatus/SetupError.** The 4 existing tests covered the `CheckItem` happy/fail JSON shapes, the `SetupStatus` envelope round-trip, and the 500-with-`troubleshooting`-link path. But the **insertion-order preservation** on `SetupStatus.items` (the admin UI keys on the implicit ordering — `database` always first, since "everything else is moot if this is red") was unpinned despite serde's Vec ordering being load-bearing (a refactor to `HashMap<id, item>` for "stable lookup" would silently scramble the order), the **`SetupError` Display prefix** (`database error: {0}` — the operator-facing log grep target) was unpinned, the **non-empty `fix` hint** invariant on `ok=false` items was unpinned, and the **500 envelope's `fix` + `detail` fields** (the curl /healthz hint + the underlying sqlx Display) were only partially asserted by the existing test. Files:

- [crates/proxy/src/api/setup.rs](../../crates/proxy/src/api/setup.rs) — 4 new tests. `setup_status_items_preserve_insertion_order_on_serialize` pins that a three-item `Vec` round-trips as `["database", "policies", "oauth_clients"]` in that exact order. `setup_error_display_carries_underlying_db_error` pins the `#[error("database error: {0}")]` prefix against `sqlx::Error::PoolClosed`. `check_item_some_fix_includes_actionable_keyword` pins the wire-shape invariant that `Some(fix)` is never empty (the UI uses `is_some ↔ render` as the decision rule). `setup_error_response_body_carries_fix_and_docs_hints` pins both the `curl /healthz` fix hint and the `database error` substring on the 500 body (the operator's first-30-seconds triage contract).

proxy bin tests: **485 → 489**.

**2026-05-17 (round 42) — 6 more pure-helper tests on adapters/action_stream + forwarder/tee.** The 5 existing `action_stream` tests covered the `extra=null` skip + `extra=object` include + the absent-`extra` deserialize default + a full round trip + the `LoggingStream::publish` infallibility; the 4 existing `tee` tests covered fan-out + no-sinks-still-calls-primary + chaining-counter + per-sink-clone. But several wire-contract / ordering boundaries the SIEM + NATS + admin-UI consumers depend on were unpinned. Files:

- [crates/proxy/src/adapters/action_stream.rs](../../crates/proxy/src/adapters/action_stream.rs) — 4 new tests. `leaf_pca_id_round_trips_through_json_when_some` pins the hyphenated Uuid wire form on `Option<Uuid>` (a `#[serde(with = "...")]` refactor to the simple/no-hyphen form would silently break the operator-grep contract). `status_u16_round_trips_as_unquoted_integer` pins that `status: 503` lands as a bare JSON integer (not a string) — Grafana alerts numeric-compare on the field, and a `#[serde(with = "string")]` refactor would silently break every alert; the boundary `u16::MAX` round trip pins no overflow. `quarantined_count_round_trips_as_unsigned_integer` pins the unsigned domain (a refactor to `i64` for SQL alignment would allow negatives — the `quarantined_count: -1` round-trip rejection is the load-bearing assertion). `logging_stream_default_produces_usable_instance` pins the `#[derive(Default)]` against a future state-bearing refactor.
- [crates/proxy/src/forwarder/tee.rs](../../crates/proxy/src/forwarder/tee.rs) — 2 new tests. `fans_out_to_five_sinks_concurrently_without_dropping_any` pins width > 3 so a refactor to a hard-coded `tokio::select!` or 2-tuple fan-out can't silently truncate. `primary_publishes_before_secondary_sinks` pins the doc-comment ordering invariant ("primary awaited synchronously BEFORE the secondary fan-out") with a shared `Mutex<Vec<&str>>` order log — a refactor that moved `primary.publish(...).await` into the `join_all` set would silently break the "durable record exists by the time a NATS subscriber sees the event" guarantee from the module's top-level docs.

proxy bin tests: **489 → 495**. Cumulative across rounds 1–42: proxy **+329**, cli +17, policy-engine **+62**, shared-types +7 — **394 new pure-helper unit tests** total. Gate floors unchanged (workspace 45/55, per-crate proxy 45 / shared-types 95 / policy-engine 88 / cli 10). `cargo fmt --all --check`, `cargo clippy --workspace --all-targets --locked -- -D warnings -A clippy::type_complexity -A clippy::too_many_arguments -A clippy::result_large_err`, `cargo test --workspace --locked`, and `RUSTFLAGS="-D warnings" cargo build --workspace --release --locked` all green.

**2026-05-17 (round 43) — 6 more pure-helper tests on pic/cache + api/mod.** The 6 existing `pic/cache` tests covered the `CURRENT_PIC_PROFILE` constant, the `CachedPca::new` default-fill paths (pic_profile, signature, predecessor), the `Clone` disjoint-buffer invariant, the `CacheError::Db` Display + `#[from]` round-trip. The 5 existing `api/mod` tests covered the `hex_encode` byte-by-byte mapping + `ApiError::NotFound` 404. But the **hop boundary** (i32::MAX) was unpinned despite the int4 column being the operator-visible "chain depth" surface, the **Debug shape** of `CachedPca` (the operator-visible string in `tracing::warn!(?pca, ...)`) was unpinned, the **large-ops Clone** semantic was unpinned (the verifier hands the full JSONB into the policy engine for every Layer-A check — a `Cow<[String]>` micro-opt would silently change the deep-copy contract), the **Db-error envelope** (fix + docs URL) was unpinned, the **NotFound envelope** was status-only (no fix / docs assertion), and the **PcaView** wire-shape (7 stable keys the dashboard chain-walker renders against) was unpinned. Files:

- [crates/proxy/src/pic/cache.rs](../../crates/proxy/src/pic/cache.rs) — 3 new tests. `cached_pca_new_round_trips_hop_at_i32_max_without_overflow` pins both boundaries (0 + i32::MAX) so a `u32` refactor surfaces here rather than at the 2^31-depth chain. `cached_pca_debug_includes_pca_id_for_operator_log_grep` pins the `Debug`-rendered `pca_id` substring against a manual-impl regression. `cached_pca_carries_large_ops_vec_through_clone` pins the 2048-element deep-copy against a `Cow<[String]>` micro-opt regression.
- [crates/proxy/src/api/mod.rs](../../crates/proxy/src/api/mod.rs) — 3 new tests. `api_error_db_body_carries_fix_and_docs_hints` pins both `curl /healthz` + `troubleshooting` substrings on the 500 envelope. `api_error_not_found_body_includes_fix_and_pca_cache_docs_link` pins the `Trust Plane` + `pca-cache` substrings on the 404 envelope (the operator-onboarding "why isn't my chain here" path). `pca_view_serializes_with_stable_field_names` pins the 7-key wire-shape (`pca_id`, `p_0`, `ops`, `hop`, `predecessor_id`, `pic_profile`, `cbor_hex`) against a serde-rename regression.

proxy bin tests: **495 → 501**.

**2026-05-17 (round 44) — 7 more pure-helper tests on crypto/pkce + error_envelope.** The 6 existing `pkce` tests covered the RFC 7636 §B vector + mismatch + below-minimum length + the 42/43 + 128/129 boundaries + Display-string log filters. The 6 existing `error_envelope` tests covered the constructor + builder composition + selective-serialize on `Some` + into_response status. But the **empty-verifier** fast-fail (must trip VerifierLength before computing SHA-256 — a regression would burn cycles on every malformed request and potentially open a SHA-256-cost timing side channel) was unpinned, the **case-sensitive challenge compare** (a "robustness" lower-case refactor would silently weaken PKCE against a downgrade attack) was unpinned, and the **`std::error::Error` trait-object cast** (load-bearing for `anyhow` chains in adapter call sites) was unpinned. On the envelope side, the **`extras` skip-on-null** wire-shape contract (the rest of the optional fields use `Option::is_none`; `extras` uses `Value::is_null` — a refactor to the uniform predicate would land `extras: null` on every body and break the is-present-implies-parse downstream contract) was unpinned, the **`DOCS_BASE` constant** was unpinned, the **Debug shape** carrying `code` for grep was unpinned, and the **`with_detail(impl Into<String>)`** dual-shape (accepts both `&str` and `String` — adapter sites build dynamic detail strings) was unpinned. Files:

- [crates/proxy/src/crypto/pkce.rs](../../crates/proxy/src/crypto/pkce.rs) — 3 new tests. `empty_verifier_rejected_as_length_not_mismatch` pins fail-closed order. `challenge_comparison_is_case_sensitive` computes the real challenge, then upper-cases it and asserts `Mismatch` (the verifier itself stays 43-chars valid, isolating the case-sensitivity contract on the challenge side). `pkce_error_implements_std_error_trait_for_anyhow_chains` pins the `thiserror` derive's trait-object castability.
- [crates/proxy/src/error_envelope.rs](../../crates/proxy/src/error_envelope.rs) — 4 new tests. `extras_null_omitted_extras_object_included_on_wire` pins both halves of the `skip_serializing_if = "Value::is_null"` predicate. `docs_base_constant_pinned_to_canonical_root_url` pins `https://proxilion.com/docs` against a typo (`.io` vs `.com`). `error_body_debug_includes_code_for_grep` pins the rendered Debug string carrying the `code` field. `with_detail_accepts_string_and_str_via_into` pins both call shapes (`String::from(...)` + `"static"`).

proxy bin tests: **501 → 508**.

**2026-05-17 (round 45) — 7 more pure-helper tests on forwarder/nats + adapters/policy_trace.** The 6 existing `nats` tests covered the subject formatter + 4 sanitize-input shapes + the ConnectError Display. The 7 existing `policy_trace` tests covered the `mark_layer_a_failed` replace + `mark_read_filter` append/replace/blocked paths + the `summary` ok-vs-code rendering + `set_layer` append-when-absent + empty-layers + a multi-label summary. But several adversarial / wire-shape boundaries were unpinned: NATS-subject sanitization had never asserted the **whitespace family** explicitly (`\t` `\n` `\r` — a refactor hardcoding only `' '` would silently let the others through and fork the subject), nor the **wildcard family** (`*` `>` — letting these through would let an attacker craft a publish that overlaps a subscription it shouldn't, e.g. an adversarial filename of `*` matching `actions.google.*`), nor the **dot passthrough** (the subject hierarchy separator — filtering it would collapse multi-token actions into one token), nor the **ConnectError Debug** wire shape. On the policy-trace side, `set_layer` had been tested for replace + append separately but **never both at once with siblings present** (a `swap_remove` refactor would silently scramble the Vec order that `summary` renders against), the comma-and-Vec-order **summary join** contract was inferred but never asserted directly (alphabetical-sort regression: `layer_a` before `layer_b` even when LayerB landed first — the test below puts ReadFilter FIRST in the Vec so an alphabetical sort would surface), and **`emit` don't-panic** on allowed AND denied paths (a panic in the trace emitter would crash the worker mid-request) was unpinned. Files:

- [crates/proxy/src/forwarder/nats.rs](../../crates/proxy/src/forwarder/nats.rs) — 4 new tests. `sanitize_replaces_whitespace_tab_and_newline_with_underscore` pins `\t` `\n` `\r` + space → `_`. `sanitize_replaces_nats_wildcard_chars_star_and_gt` pins the `*` and `>` subject-wildcard collapse. `sanitize_dot_passthrough_preserves_subject_hierarchy` pins `.` AS the separator (passes through with leading/trailing variants). `connect_error_debug_includes_struct_name_for_grep` pins `ConnectError(...)` in the rendered Debug.
- [crates/proxy/src/adapters/policy_trace.rs](../../crates/proxy/src/adapters/policy_trace.rs) — 3 new tests. `set_layer_replaces_only_matching_layer_leaving_siblings_intact` pins in-place replacement against a 3-layer Vec (LayerB-failed replacement leaves LayerA + ReadFilter untouched at their original Vec indices). `summary_join_uses_comma_and_preserves_layer_order` pins the comma-separator + Vec-order rendering against an alphabetical-sort regression (`read_filter=ok,layer_a=ok` — ReadFilter first because LayerB-then-LayerA would also pass an alphabetical sort). `emit_does_not_panic_on_allowed_and_denied_traces` pins the don't-panic invariant on both INFO + WARN paths.

proxy bin tests: **508 → 515**. Cumulative across rounds 1–45: proxy **+349**, cli +17, policy-engine **+62**, shared-types +7 — **414 new pure-helper unit tests** total. Gate floors unchanged (workspace 45/55, per-crate proxy 45 / shared-types 95 / policy-engine 88 / cli 10). `cargo fmt --all --check`, `cargo clippy --workspace --all-targets --locked -- -D warnings -A clippy::type_complexity -A clippy::too_many_arguments -A clippy::result_large_err`, `cargo test --workspace --locked`, and `RUSTFLAGS="-D warnings" cargo build --workspace --release --locked` all green.

**2026-05-17 (round 46) — 6 more pure-helper tests on notifier/handle + notifier/mod.** The 8 existing `handle` tests covered the Arc-shared swap visibility + clone fan-out + bundle clone + per-driver fan-out. The 6 existing `mod` tests covered the schema constant + approve/reject URL construction + None-field passthrough + empty-ops slice. But the **`webhook_burst` + `slack_burst` None defaults** (load-bearing for the "empty bundle" semantic — pre-built defaults would silently activate burst suppression on test fixtures), the **clone fan-out for `replace(None)`** (symmetric to replace-Some — a deep-copy regression on the None path would only fail this assertion), the **`Handle::new(Some(_))` Arc-passthrough** (boot-path contract — the first `current()` call must return the SAME `Arc<T>` the constructor was handed, pinned via `Arc::ptr_eq` to catch a lazy-init refactor), the **trailing-slash double-slash URL** shape (current behavior is wire-visible — a "fix" must move in lockstep with the docs telling operators to omit the slash), the **multi-op slice serialization** (a flatten-to-comma-joined-string refactor would break webhook receivers that iterate the JSON array), and the **`layer = "invariant"`** wire-shape (PIC monotonicity vs policy block — Slack mrkdwn `*Layer:* {{layer}}` byte stability) were unpinned. Files:

- [crates/proxy/src/notifier/handle.rs](../../crates/proxy/src/notifier/handle.rs) — 3 new tests. `empty_bundle_has_none_for_burst_suppressors` pins both Option<BurstSuppressor> default-None fields. `replace_to_none_after_some_clears_for_all_clones` pins the clone fan-out across the Some→None transition. `handle_new_with_initial_some_carries_through_first_current_call` pins `Arc::ptr_eq` between the constructor input and the first `current()` read.
- [crates/proxy/src/notifier/mod.rs](../../crates/proxy/src/notifier/mod.rs) — 3 new tests. `approve_url_handles_trailing_slash_in_proxy_public_url` pins the current `https://proxy.example//api/v1/...` double-slash shape. `from_record_carries_multi_op_slice_through_serialize` pins a 3-element JSON array round-trip. `from_record_invariant_layer_round_trips_through_json` pins `layer = "invariant"` byte-stability on the wire.

proxy bin tests: **515 → 521**.

**2026-05-17 (round 47) — 6 more pure-helper tests on oauth/state + oauth/error.** The 4 existing `state` tests covered the four env-var paths (CLIENT_ID missing, SECRET missing, URL defaults, URL overrides). The 8 existing `error` tests covered the status() classification + body() codes + selective detail + 4 fix substrings + the no-leak invariant on Internal/Db/Crypto. But the **`GoogleClient::Clone`** field passthrough was unpinned (a `Cow<str>` refactor would silently break the per-request mutation-safety the OAuthState design relies on), the **AUTH_URL-only override** (asymmetric to the existing both-URL override — a refactor that "consistently" required both URLs together would surface here), the **`GoogleClient::Debug`** carrying client_id (operator-facing log shape), the **`OAuthError` Display strings** (operator log-filter substrings: "PKCE verification failed" / "session expired or unknown" / "Trust Plane refused PCA" / "invalid or already used"), the **`Upstream` variant status** (a real `reqwest::Error` from a 1ms-timeout against the RFC 5737 black-hole IP → 502 + `upstream_unavailable` code — a refactor that mapped Upstream to 503 would break the agent's retry-policy classification), and the **Db→500 wire shape** (Grafana alert keyed on `code="internal_error" status="500"`) were unpinned. Files:

- [crates/proxy/src/oauth/state.rs](../../crates/proxy/src/oauth/state.rs) — 3 new tests. `google_client_clone_preserves_every_field` pins all four fields. `from_env_respects_only_auth_url_override_with_token_url_default` pins the asymmetric override path. `google_client_debug_carries_client_id` pins the derive's operator-facing field.
- [crates/proxy/src/oauth/error.rs](../../crates/proxy/src/oauth/error.rs) — 3 new tests. `oauth_error_display_strings_pinned_for_log_filters` pins five `#[error(...)]` substrings. `upstream_variant_status_is_502_bad_gateway` constructs a real `reqwest::Error` via a 1ms timeout against `192.0.2.1:1` and pins both the 502 status and the `upstream_unavailable` body code. `into_response_500_branch_uses_internal_error_code_for_db_path` pins the Db→500→`internal_error` triple.

proxy bin tests: **521 → 527**.

**2026-05-17 (round 48) — 7 more pure-helper tests on oauth/routes + api/notifier_slack.** The 7 existing `routes` tests covered `pct` reserved-chars, `oauth_error_class` denied-vs-error on 9 variants, `intersect_scope_with_ops` happy + unknown-filter, `narrowed_ops_for_pca1` happy + empty, and `new_auth_code` shape. The 4 existing `notifier_slack` tests covered `header_str` ascii + non-ascii, `slack_ok_message` happy, and `slack_err` 401. But the **`pct` empty + multibyte** boundaries were unpinned (the URL builder calls `pct(&state)` from paths that can hand it empty input; the per-byte UTF-8 encoding is the wire-safety contract for `é` → `%C3%A9`), the **`oauth_error_class` Upstream/Db classification** (the `error` bucket — moving either into `denied` would bias the dashboard's "upstream is broken" vs "your config is broken" panels), the **`profile` always-keep branch** of `intersect_scope_with_ops` (the OIDC display-name surface — the existing tests pin `openid` + `email` but not `profile`), and the **empty-intersection** edge (no scope matches AND no always-keep — must be `""`, not panic, not fall back to a "safe default") were unpinned. On the Slack side, **case-insensitive header lookup** (Slack's Go SDK sometimes sends `X-Slack-Signature` mixed-case; axum's HeaderMap normalizes — pinning this catches a refactor that iterated raw bytes), **`slack_err` 500-path** (the rare DB-failure case; the helper accepts any status — a hardcoded 401 would silently downgrade), and the **`slack_ok_message("")` empty-text** boundary (no validation of input) were unpinned. Files:

- [crates/proxy/src/oauth/routes.rs](../../crates/proxy/src/oauth/routes.rs) — 4 new tests. `pct_handles_empty_and_multibyte_utf8_inputs` pins `""` → `""` + `é` → `%C3%A9`. `oauth_error_class_categorizes_upstream_and_db_as_error_bucket` pins Db → `error` directly. `intersect_scope_keeps_profile_alongside_openid_and_email` pins all three always-keep matches. `intersect_scope_collapses_to_empty_when_no_scope_matches_and_no_always_keep` pins the empty-intersection contract.
- [crates/proxy/src/api/notifier_slack.rs](../../crates/proxy/src/api/notifier_slack.rs) — 3 new tests. `header_str_is_case_insensitive_per_http_spec` pins the HeaderMap normalization. `slack_err_emits_500_for_internal_error_path` pins the helper accepting any status. `slack_ok_message_carries_empty_text_through_to_wire` pins the no-validation empty-text round-trip.

proxy bin tests: **527 → 534**. Cumulative across rounds 1–48: proxy **+368**, cli +17, policy-engine **+62**, shared-types +7 — **433 new pure-helper unit tests** total. Gate floors unchanged (workspace 45/55, per-crate proxy 45 / shared-types 95 / policy-engine 88 / cli 10). `cargo fmt --all --check`, `cargo clippy --workspace --all-targets --locked -- -D warnings -A clippy::type_complexity -A clippy::too_many_arguments -A clippy::result_large_err`, `cargo test --workspace --locked`, and `RUSTFLAGS="-D warnings" cargo build --workspace --release --locked` all green.

**2026-05-17 (round 49) — 7 more pure-helper tests on oauth/bridge + api/killswitch.** The 8 existing `bridge` tests covered the rejects-expired + 7 issuer classifications + malformed-JWT + bad-base64 + future-issued + iss round-trip + secondary-substrings + accepts-fresh. The 8 existing `killswitch` tests covered populate_kill_cache correct/wrong-length/empty + ApiError 400/500 + KillResponse wire keys + KillBody default + confirm:yes round-trip. But the **clock-skew boundary** (`iat == now + 60` ACCEPTS, `iat == now + 61` REJECTS — a `>=` refactor would silently shrink the allowance by one second), the **`pca_0_cbor_b64` Option round-trip** (load-bearing `#[serde(default)]` — production bridges pre-fetch the bytes; stubs omit them), the **`FederationClaims::Clone`** field passthrough (the router clones into per-request state before persisting to the session row), and the **case-insensitive `infer_idp`** (the `to_ascii_lowercase` step is load-bearing — a metric-label split would appear for the same IdP) were unpinned. On the killswitch side, the **zero-`bearers_revoked` wire shape** (the dashboard "no-op" toast keys on `>= 0` — a refactor to `Option<i64>` would silently break it), the **forward-compat unknown-field acceptance** (no `deny_unknown_fields` — the CLI can add fields without breaking existing proxies), and the **`ApiError::BadRequest` Display verbatim shape** (the CLI's renderer parses the bare message — symmetric ApiError modules carry a `"bad request: {0}"` prefix, this one does not) were unpinned. Files:

- [crates/proxy/src/oauth/bridge.rs](../../crates/proxy/src/oauth/bridge.rs) — 4 new tests. `accepts_token_at_exact_60s_clock_skew_boundary` pins both sides (`+60` ok, `+61` rejected). `federation_claims_round_trip_pca_cbor_b64_optional` pins both Some(_) and absent shapes via two JWTs. `federation_claims_clone_preserves_every_field` pins every Clone-derived field. `infer_idp_case_insensitive_against_mixed_case_issuers` pins the three primary IdP buckets against mixed-case inputs.
- [crates/proxy/src/api/killswitch.rs](../../crates/proxy/src/api/killswitch.rs) — 3 new tests. `kill_response_with_zero_bearers_revoked_serializes_cleanly` pins the bare-integer-0 wire shape. `kill_body_rejects_unknown_field_via_deny_or_passes_through_today` pins forward-compat (a future_field is accepted, parsed fields preserved). `api_error_bad_request_display_carries_inner_message` pins the no-prefix `#[error("{0}")]` shape.

proxy bin tests: **534 → 541**.

**2026-05-17 (round 50) — 6 more pure-helper tests on crypto/token_cipher + crypto/bearer.** The 9 existing `token_cipher` tests covered round-trip + distinct-nonces + wrong-key + short-key + tampered-CT + wrong-nonce-len + Ciphertext::Clone + BadKeyLen Display + empty-key. The 11 existing `bearer` tests covered round-trip format + wrong-prefix + lowercase-body + hash-stable + Debug-no-leak + length boundaries (51/53) + bad digits + as_bytes shape + Debug truncation + distinct generation + PartialEq distinguishing. But several boundaries the production hot path depends on were unpinned: the **empty-plaintext round-trip** (Google OAuth omits refresh_token → empty bytes still must round-trip cleanly via the 16-byte GCM tag), the **4 KB plaintext round-trip** (OAuth ID tokens hit this size; a fixed-size buffer refactor would surface here), the **`CipherError::Aead` Display** (operator log filter substring), the **empty/prefix-only/half-typed bearer parse** boundaries on the fast-path length check (auth middleware sometimes receives an empty header value), the **byte-for-byte parse-returns-input identity** (the SHA-256 hash MUST match the slice the parser was handed — any normalization would invalidate every persisted hash), and the **`BearerHash::Clone`** owning-array semantic (the killswitch path stashes a copy in the kill_cache). Files:

- [crates/proxy/src/crypto/token_cipher.rs](../../crates/proxy/src/crypto/token_cipher.rs) — 3 new tests. `empty_plaintext_round_trips_with_nonzero_ciphertext` pins empty PT + 16-byte tag. `large_plaintext_round_trips_through_encrypt_decrypt` pins a 4096-byte payload. `aead_error_display_is_stable_for_log_filters` pins the exact Display string `"AES-GCM operation failed"`.
- [crates/proxy/src/crypto/bearer.rs](../../crates/proxy/src/crypto/bearer.rs) — 3 new tests. `parse_rejects_empty_string_and_prefix_only` pins the three fast-fail length cases (`""`, `PREFIX`, prefix + 26 chars). `parse_returns_input_slice_byte_for_byte_when_valid` pins byte-for-byte identity. `bearer_hash_clone_yields_independent_array` pins the `[u8; 32]` Copy-via-Clone disjointness.

proxy bin tests: **541 → 547**.

**2026-05-17 (round 51) — 7 more pure-helper tests on policy-engine trace + decision.** The 10 existing `trace` tests covered the LayerOutcome::passed/failed shapes + 4 trace allowed/denied paths + OpsAtomView round-trip + PolicyLayer snake_case + PolicyEvalMode default + JSON null-not-omitted for None fields + trace JSON envelope. The 12 existing `decision` tests covered Pattern literal/regex + Decision serde 4 variants + QuarantineAction Copy + ReadFilter Clone + literal empty-haystack edge + decision kind wire snapshot. But several invariants the engine relies on were unpinned: the **empty-layers + Allow** vacuous-true (engine path always emits Layer A/B stubs but mocks construct empty traces), the **fail-at-third-layer** ordering invariant (`.all(passed)` must NOT short-circuit on the first pass — a single later-layer fail must flip allowed to false), the **duration_micros public-field mutation** (engine post-stamps the field after measuring eval time — a private-field refactor would force a builder method), the **OpsAtomView exact 3-key wire shape** (no extra fields; Trust Plane's parser is strict). On `decision`: the **PartialEq distinguishing Block variants by reason AND override_allowed** (the dashboard's diff-view re-renders on inequality), the **RateLimit burst/per_seconds independent round-trip** (an accidental field-order swap during refactor would silently flip the rate-limit semantics — used distinct values 7 and 42), and the **Pattern::Regex Clone** preserving compiled state (a `Cow<'static, str>` + re-compile-on-match refactor would silently re-pay compile cost per request on the hot path). Files:

- [crates/policy-engine/src/trace.rs](../../crates/policy-engine/src/trace.rs) — 4 new tests. `allowed_returns_true_for_empty_layers_with_allow_decision` pins the vacuous-`all` case. `allowed_false_when_first_layer_passes_but_a_later_layer_fails` pins the must-check-every-layer invariant on a 3-layer trace. `duration_micros_starts_zero_and_supports_post_construction_mutation` pins both zero-on-construct + pub field. `ops_atom_view_serializes_with_all_three_fields_present` pins all three field names + zero extras.
- [crates/policy-engine/src/decision.rs](../../crates/policy-engine/src/decision.rs) — 3 new tests. `decision_partial_eq_distinguishes_block_variants_by_reason_and_override` pins all four equality/inequality permutations. `rate_limit_round_trips_burst_and_per_seconds_separately` pins both integer fields with distinct values. `regex_pattern_clone_preserves_compiled_state` pins the compiled-regex Clone semantic via positive + negative match.

policy-engine lib tests: **120 → 127**. proxy bin tests: **547 (unchanged from round 50)**. Cumulative across rounds 1–51: proxy **+381**, cli +17, policy-engine **+69**, shared-types +7 — **474 new pure-helper unit tests** total. Gate floors unchanged (workspace 45/55, per-crate proxy 45 / shared-types 95 / policy-engine 88 / cli 10). `cargo fmt --all --check`, `cargo clippy --workspace --all-targets --locked -- -D warnings -A clippy::type_complexity -A clippy::too_many_arguments -A clippy::result_large_err`, `cargo test --workspace --locked`, and `RUSTFLAGS="-D warnings" cargo build --workspace --release --locked` all green.

**2026-05-17 (round 52) — 7 more pure-helper tests on auth_middleware + policy-engine/context.** The 10 existing `auth_middleware` tests covered RefreshCoordinator (same-hash + distinct-hash + empty-default) + verify_with_key (untampered + tampered + wrong-key) + AuthFail Display strings + PcaCacheMiss substring + AuthFail::Db `#[from]` + the 401 body byte-stable. The 10 existing `context` tests covered customer_domain + path/user.email + headers + body string/non-string + unknown-head + list-array + non-array + list-with-non-string + path-and-headers-never-list. But several invariants the hot path relies on were unpinned: the **mutex-independence of refresh locks** (a regression that collapsed all bearers to one mutex would deadlock on the second acquire — caught via try_lock while holding A), the **AuthFail::Decrypt no-leak** semantic (the variant has no inner field — pin same-string-across-calls so a refactor that wrapped CipherError surfaces here as a wire-shape change), and the **AuthState::Clone trait bound** (every middleware invocation clones it via axum's State<T>; a !Clone field added later would surface at hundreds of router-build sites rather than at this single trait-bound assertion). On `context`: **missing-key vs null-value distinction** (OpsExpression treats `None` as "fall through to bare template" but `Some("null")` would produce a literal `:null` atom on the wire — load-bearing distinction), **empty list `Some(vec![])`** vs `None` (the substitution path treats Some(empty) as zero atoms, not as fall-through), **`user.email` empty-string returns Some("") not None** (policy authors use `user.email == ""` as a sentinel — a `Option<String>` refactor would flip this), and the **customer_domain Clone-through** survival. Files:

- [crates/proxy/src/auth_middleware.rs](../../crates/proxy/src/auth_middleware.rs) — 3 new tests. `refresh_coordinator_distinct_hashes_produce_independent_locks` pins lock disjointness via try_lock-while-held. `auth_fail_decrypt_does_not_carry_cipher_internals_in_message` pins the fixed-string Display + same-call-twice equality. `auth_state_is_clone_for_axum_state_propagation` pins the Clone trait bound at compile time via `fn require_clone<T: Clone>()` (no instantiation needed).
- [crates/policy-engine/src/context.rs](../../crates/policy-engine/src/context.rs) — 4 new tests. `lookup_body_missing_key_returns_none_distinct_from_null_value` pins both cases of the load-bearing distinction. `lookup_list_empty_string_array_returns_empty_vec_not_none` pins `Some(empty)` against the `null vs []` substitution-path conflation. `lookup_user_email_with_empty_string_returns_some_empty` pins the sentinel-friendly `Some("")` shape. `lookup_customer_domain_carries_through_clone` pins the Clone-derived field passthrough.

proxy bin tests: **547 → 550**. policy-engine lib tests: **127 → 131**.

**2026-05-17 (round 53) — 6 more pure-helper tests on notifier/webhook + api/notifier.** The 10 existing `webhook` tests covered WebhookSecret round-trip + reject-short/odd + per-branch error messages + lowercase-hex sig + with_burst attachment + NotifierBuildError Display + proxy_public_url + 3 wiremock-backed POST shapes. The 11 existing `api/notifier` tests covered redact_url four cases + TestRequest deserialization + SetConfigBody three variants + redact preserve-scheme + scheme-only/trailing-slash/missing-driver/missing-config. But several cryptographic and wire-shape invariants were unpinned: the **HMAC avalanche on single-byte body diff** (a refactor to a checksum would silently accept tampered bodies), the **distinct-key → distinct-MAC** symmetric pin (a hardcoded-key regression would surface as identical signatures), the **WebhookSecret::Clone same-MAC** invariant (the bundle hands a clone into each driver build — a per-instance salt smuggled through Clone would break webhook receivers). On `api/notifier`: **userinfo segment passthrough** (https://user:pass@host — current behavior surfaces it; a future strip would be a conscious change), **scheme-agnostic redaction** (ftp:// + mqtt:// — operators sometimes configure non-https), and the **TestRequest forward-compat empty/extra-field path** were unpinned. Files:

- [crates/proxy/src/notifier/webhook.rs](../../crates/proxy/src/notifier/webhook.rs) — 3 new tests. `secret_sign_distinguishes_single_byte_difference_in_body` pins the avalanche property on a single-byte flip (+ same-length pin). `secret_distinct_keys_produce_distinct_signatures_for_same_body` pins the cross-key symmetry. `secret_clone_yields_same_signature_as_original` pins no per-instance salt smuggled into Clone.
- [crates/proxy/src/api/notifier.rs](../../crates/proxy/src/api/notifier.rs) — 3 new tests. `redact_url_handles_userinfo_segment_without_panic` pins current behavior (passes the `user:pass@host` segment through to the redacted form). `redact_url_keeps_ftp_and_unusual_schemes_intact` pins scheme-agnostic redaction across ftp:// + mqtt://. `test_request_defaults_via_default_impl_on_optional_driver` pins empty-body → None + forward-compat unknown-field acceptance.

proxy bin tests: **550 → 556**.

**2026-05-17 (round 54) — 6 more pure-helper tests on blocked.rs + policy-engine/loader.** The 12 existing `blocked` tests covered canonical_request_json shape + body-only-what-exposed + truncation + OwnedBlockedNotification round-trip + Clone + deterministic-ness + truncation-elides-body + just-below-cap + alphabetical sort + empty-maps-as-objects + Record::Clone. The 10 existing `loader` tests covered FilePolicyLoader round-trip + mtime-change + not-found + StaticPolicyLoader set_yaml + PolicyLoadError Display + PolicyBundle equality + path/source_label + sync-async token match + sync not-found + with_label. But the **nested object body field** rendering (gmail headers map — a stringification refactor would break audit shape), the **empty method/path defensive** boundary (no panic + parseable JSON), and the **`CANONICAL_REQUEST_MAX_LEN = 4096`** literal pin (spec.md §2.1 dev 3 wire contract — a widening would break downstream SIEM ingestors) were unpinned. On `loader`: the **initial revision token `"rev:0"`** (proxy bootstrap pins this), the **`changed_since` default-trait-impl path on StaticPolicyLoader** (load-and-compare — pin via initial-no-change + set_yaml-yields-change), and the **atomic fetch_add monotonicity** of `set_yaml` across 5 successive bumps (a swap/store refactor would surface here) were unpinned. Files:

- [crates/proxy/src/blocked.rs](../../crates/proxy/src/blocked.rs) — 3 new tests. `canonical_request_json_renders_nested_object_body_field` pins nested-object passthrough. `canonical_request_json_with_empty_method_and_path_still_serializes` pins defensive empty-string boundaries. `canonical_request_max_len_constant_pinned_at_4_kib` pins the literal 4096 wire contract.
- [crates/policy-engine/src/loader.rs](../../crates/policy-engine/src/loader.rs) — 3 new tests. `static_loader_initial_version_is_rev_zero` pins `"rev:0"`. `static_loader_changed_since_uses_default_impl_correctly` pins the default trait impl path via initial→no-change + set_yaml→change. `static_loader_set_yaml_bumps_revision_monotonically` pins 5 successive atomic increments.

proxy bin tests: **556 → 559**. policy-engine lib tests: **131 → 134**. Cumulative across rounds 1–54: proxy **+393**, cli +17, policy-engine **+76**, shared-types +7 — **493 new pure-helper unit tests** total. Gate floors unchanged (workspace 45/55, per-crate proxy 45 / shared-types 95 / policy-engine 88 / cli 10). `cargo fmt --all --check`, `cargo clippy --workspace --all-targets --locked -- -D warnings -A clippy::type_complexity -A clippy::too_many_arguments -A clippy::result_large_err`, `cargo test --workspace --locked`, and `RUSTFLAGS="-D warnings" cargo build --workspace --release --locked` all green.

**2026-05-17 (round 55) — 6 more pure-helper tests on pic/cat_key + shared-types/operator_scopes.** The 6 existing `cat_key` tests covered Status Display + InfoResp deserialize + unreachable-trust-plane + Status no-body + Registry Clone Arc-share + forward-compat unknown fields. The 6 existing `operator_scopes` tests covered no-duplicates + wildcard-present + non-empty descriptions + scope_strings length + known-scope-set + kebab-or-colon format. But several invariants were unpinned: the **Decode variant's inner-detail passthrough** across three distinct messages (a future Display collapse would surface here), the **trailing-slash URL constructor** boundary (current behavior doesn't reject — a future strip would be a conscious change), the **Status variant's raw-code passthrough** across 4xx + 5xx codes (a refactor to named-arm bucketing would lose precision). On `operator_scopes`: the **catalogue length floor** (≥12 — catches an accidental deletion that all per-scope tests would still pass), the **`/api/v1/` endpoint substring on every non-wildcard entry** (CLI rendering consistency), and the **scope_strings index-by-index order preservation** (the existing test only pinned `*` first — alphabetical-sort regression would surface here). Files:

- [crates/proxy/src/pic/cat_key.rs](../../crates/proxy/src/pic/cat_key.rs) — 3 new tests. `cat_key_error_decode_display_carries_inner_detail` pins three distinct messages. `cat_key_registry_url_with_trailing_slash_still_constructs` pins constructor + Arc::ptr_eq via Clone. `cat_key_error_status_variants_distinguish_4xx_and_5xx_codes` pins raw-code passthrough across four codes.
- [crates/shared-types/src/operator_scopes.rs](../../crates/shared-types/src/operator_scopes.rs) — 3 new tests. `catalogue_carries_at_least_twelve_entries_per_documented_spec` pins length ≥ 12. `every_non_wildcard_endpoint_field_mentions_api_v1_route` pins `/api/v1/` substring on every non-wildcard endpoint. `scope_strings_preserves_catalogue_order_end_to_end` pins index-by-index order preservation.

proxy bin tests: **559 → 562**. shared-types tests: **12 → 15**.

**2026-05-17 (round 56) — 6 more pure-helper tests on pic/verifier + pic/executor.** The 9 existing `verifier` tests covered the 3-deep chain decode + tampered-payload + 3 invariant checks (monotonicity, continuity, p0) + invariant_kind labels + err_to_result broken_at + Display field substitution. The 9 existing `executor` tests covered audit-fallback + runtime-gate-error + dev-ephemeral-kids + new-kid-roundtrip + Display field values + IssuePcaResponse exp Optional + ProcessPocResponse round-trip + RegisterExecutorRequest wire shape + Clone Arc-share. But several wire-shape boundaries were unpinned: `VerificationResult`'s **seven-key serialize shape** (the dashboard chain-walker keys on every field), the **distinct-label invariant on the out-of-contract bucket triple** (`missing`/`decode`/`cat_key` must NOT alias — a copy-paste collapsing them would lose triage signal), the **`err_to_result` fallback-to-leaf for HopOrder** (no Uuid in the variant — a future `pca_id` field addition surfaces as a wire-shape change). On `executor`: the **`SuccessorOutcome::Debug` carries `detail`** (audit-trail logging contract), the **`ProcessPocRequest` single-field wire shape** (`poc` only — sibling-field additions break Trust Plane contract), and the **`IssuePcaRequest` four-field wire shape** (a `cred_type` shortening of `credential_type` would surface here). Files:

- [crates/proxy/src/pic/verifier.rs](../../crates/proxy/src/pic/verifier.rs) — 3 new tests. `verification_result_serializes_with_stable_wire_field_names` pins all 7 wire keys. `invariant_kind_for_decode_and_missing_matches_bounded_label_set` pins the three out-of-contract buckets pair-wise distinct. `err_to_result_for_hop_order_pins_broken_at_to_leaf_id` pins HopOrder→leaf fallback + reason carries hop integers.
- [crates/proxy/src/pic/executor.rs](../../crates/proxy/src/pic/executor.rs) — 3 new tests. `successor_outcome_debug_carries_variant_name_and_detail` pins Debug shape on AuditFallback. `process_poc_request_serializes_to_single_wire_field` pins the single-field invariant. `issue_pca_request_serializes_with_four_wire_fields` pins the four-field invariant + `credential_type` name.

proxy bin tests: **562 → 568**.

**2026-05-17 (round 57) — 6 more pure-helper tests on shared-types/error_code + proxy/adapters/error.** The 6 existing `error_code` tests covered wire-strings + serde round-trip + status snapshot + Display + Copy/Hash + unknown-rejected. The 12 existing `adapters/error` tests covered upstream_error_kind classifications + 9 body/status/extras paths. But several wire contracts were unpinned: the **load-bearing DatabaseError/InternalError two-into-one wire-string aliasing** (the proxy intentionally hides "your postgres is sick" from the agent — a refactor splitting them would silently leak operational state), the **two upstream variants sharing 502** (UpstreamTooLarge bumping to 413 would change agent retry semantics), and the **per-variant bidirectional `Value` round-trip** (one-way snapshots don't catch a `rename_all` drift on deserialize). On `adapters/error`: the **PicInvariantViolation detail+code+docs triple** (the Layer-A break is operator-actionable; pin all three), the **RateLimit 429 + no-detail** (intentionally hides current-window count from abusers), and the **RequireConfirmation 428 + `X-Proxilion-Confirmation` fix hint** (agent SDK reads the hint to surface the retry shape). Files:

- [crates/shared-types/src/error_code.rs](../../crates/shared-types/src/error_code.rs) — 3 new tests. `database_error_and_internal_error_share_wire_string_by_design` pins the load-bearing aliasing. `default_status_for_two_upstream_variants_share_502_class` pins both 502s. `error_code_serde_round_trip_via_value_for_every_variant` pins bidirectional round-trip via `serde_json::Value` for 8 variants (the alias pair handled separately).
- [crates/proxy/src/adapters/error.rs](../../crates/proxy/src/adapters/error.rs) — 3 new tests. `app_error_pic_invariant_violation_carries_detail_through_to_wire` pins detail + code + docs. `app_error_rate_limit_status_is_429_and_carries_no_detail` pins 429 + no-detail. `app_error_require_confirmation_status_is_428_with_token_hint` pins 428 + `X-Proxilion-Confirmation` fix substring.

proxy bin tests: **568 → 571**. shared-types tests: **15 → 18**. Cumulative across rounds 1–57: proxy **+411**, cli +17, policy-engine **+76**, shared-types **+13** — **517 new pure-helper unit tests** total. Gate floors unchanged (workspace 45/55, per-crate proxy 45 / shared-types 95 / policy-engine 88 / cli 10). `cargo fmt --all --check`, `cargo clippy --workspace --all-targets --locked -- -D warnings -A clippy::type_complexity -A clippy::too_many_arguments -A clippy::result_large_err`, `cargo test --workspace --locked`, and `RUSTFLAGS="-D warnings" cargo build --workspace --release --locked` all green.

**2026-05-17 (round 58) — 7 more pure-helper tests on demo + server.** The 6 existing `demo` tests covered synth_event field passthrough + USERS membership + `s`-suffix path verbatim + 6-char suffix on other templates + distinct UUIDs + SCENARIOS decision variety. The 6 existing `server` tests covered hex_decode_32 all-zero/all-ff + mixed-case + wrong-length + non-hex + ensure_dev_cert no-op + ensure_dev_cert generate. But several invariants the operator-facing surfaces depend on were unpinned: the **`extra.demo = true` marker presence** (load-bearing for audit-query exclusion — every real dashboard filters on this; a regression would silently pollute production audit data), the **USERS triple distinctness + demo.local suffix** (the multi-user demo timeline contract), and the **`/`-trailing-slash path-template branch** (the existing test pinned `s`, not the OR-pair). On `server`: the **leading-zero preservation within hex pairs** (a regression on `00` vs `0` decoding would silently zero out high nibbles), the **`Check` and `Healthz` wire-shape stability** (Cloudflare uptime alerts key on `checks.trust_plane.ok` — a wire rename breaks them), and the **one-valid + one-invalid char rejection at both pair positions** (a single-char prevalidator regression would let `0Z00...` slip through). Files:

- [crates/proxy/src/demo.rs](../../crates/proxy/src/demo.rs) — 3 new tests. `synth_event_extra_carries_demo_marker_for_audit_filter_exclusion` pins the `demo: true` marker. `users_constant_has_three_distinct_demo_principals` pins length 3 + distinctness + `user:`/`@demo.local` shape. `synth_event_path_template_ending_in_slash_is_used_verbatim` pins the `/` branch of the OR-check separately from the existing `s`-branch.
- [crates/proxy/src/server.rs](../../crates/proxy/src/server.rs) — 4 new tests. `hex_decode_32_preserves_leading_zeros_within_pairs` pins `0a` → 0x0a across the 0..32 byte sequence. `check_serializes_with_stable_three_field_shape` pins the three wire keys + their concrete values. `healthz_envelope_serializes_with_ready_version_and_checks` pins the envelope's three top-level fields. `hex_decode_32_rejects_pair_with_one_valid_and_one_invalid_char` pins rejection at BOTH pair positions (0 and 62).

proxy bin tests: **571 → 578**.

**2026-05-17 (round 59) — 6 more pure-helper tests on session + adapters/google_drive.** The 7 existing `session` tests covered Debug-redaction of google_access_token + 401 status + extractor-missing-err + Debug-omits-bearer_hash+cbor + Clone-Arc-share + 12-byte body + extractor-Ok-when-present. The 9 existing `google_drive` tests covered enforce-allow + 3 enforce-deny paths + proxy_headers present + omitted-when-no-match + invalid-header skip + 2 wire-shape paths for AppError responses. But several invariants the bearer middleware + adapter relies on were unpinned: the **multi-op granted_ops Debug rendering** (the policy engine's Layer-A handoff carries every op; a truncate-for-brevity refactor would silently lose audit signal), the **empty granted_ops branch** (openid-only OAuth flows hit this), the **SessionExtractError 401 status named-constant** (refactor-safe vs integer literal), the **`matched_policy_id: None` preservation through PolicyBlocked construction** (the dashboard's "show policy" filter keys on is_some — placeholder injection would mis-bucket), the **lowercase-hyphenated Uuid header rendering** (operator grep contract — Uuid::simple would silently break it), and the **OpsExpression::default zero-required-atoms semantic** (a "deny *" sentinel would flip every test fixture's meaning). Files:

- [crates/proxy/src/session.rs](../../crates/proxy/src/session.rs) — 3 new tests. `session_context_carries_multi_op_granted_set_through_construction` pins 3-op set + Debug rendering. `session_context_with_empty_granted_ops_still_constructs` pins empty-set construction + Debug `[]`. `session_extract_error_status_helper_returns_unauthorized_constant` pins the 401 status via named constant + integer pin.
- [crates/proxy/src/adapters/google_drive.rs](../../crates/proxy/src/adapters/google_drive.rs) — 3 new tests. `enforce_pre_request_decision_preserves_none_policy_id_on_block` pins the no-placeholder contract. `insert_proxy_headers_renders_uuid_as_lowercase_hyphenated_form` pins both hyphen count (4) and lowercase-only. `outcome_helper_round_trips_required_ops_default` pins the zero-atom default + PicMode::Audit.

proxy bin tests: **578 → 584**.

**2026-05-17 (round 60) — 7 more pure-helper tests on api/blocked + adapters/read_filter.** The 12 existing `api/blocked` tests covered the 6 ApiError → Response paths + ApproveBody/RejectBody/IssueLinkBody round-trips + missing-justification + BlockedRow skip-on-None + include-when-Some. The 12 existing `read_filter` tests covered 4 apply scenarios + binary-skip + overlap-merge + p99 perf + truncate boundaries + content-type allow-list + merge_overlapping shapes + splice + regexset short-circuit. But several adversarial / boundary inputs the operator triage path depends on were unpinned: the **`re-root chain at broader PCA_0` substring** on the 422 PicRefused fix (spec.md §6.6 escape-hatch guidance), the **`ttl_minutes: 0` sentinel** acceptance (distinct from None — handler interprets 0 as "no TTL"), the **negative-ttl_minutes parse acceptance** (`i64` not `u64` — semantic validation moved out of parse boundary). On `read_filter`: the **three binary top-level Content-Type rejects** (image/video/audio — operator-facing waste-of-cycles regression source), the **empty replacement string** in splice (Strip-Silently mode hits this), the **`merge_overlapping` order-preservation for already-sorted disjoint input** (caller-side sort invariant is masked if function re-sorts internally), and the **`n=0` truncate** boundary (returns just the ellipsis marker; refactor to special-case `""` would lose the marker). Files:

- [crates/proxy/src/api/blocked.rs](../../crates/proxy/src/api/blocked.rs) — 3 new tests. `api_error_pic_refused_fix_mentions_re_root_chain_hint` pins the operator-onboarding substring. `approve_body_accepts_zero_ttl_minutes_as_explicit_no_ttl_marker` pins the 0 sentinel. `issue_link_body_with_negative_ttl_still_deserializes` pins `i64`-domain parse.
- [crates/proxy/src/adapters/read_filter.rs](../../crates/proxy/src/adapters/read_filter.rs) — 4 new tests. `should_scan_rejects_image_video_audio_top_level_types` pins three binary top-levels + multipart/form-data. `splice_handles_empty_replacement_string` pins Strip-Silently mode. `merge_overlapping_preserves_input_order_for_disjoint_sorted_input` pins the caller-invariant no-resort contract. `truncate_zero_length_limit_returns_just_the_ellipsis` pins the marker preservation on n=0.

proxy bin tests: **584 → 591**. Cumulative across rounds 1–60: proxy **+431**, cli +17, policy-engine **+76**, shared-types **+13** — **537 new pure-helper unit tests** total.

**2026-05-17 (round 61) — 7 more pure-helper tests on kill_cache + forwarder/tee + notifier/handle + adapters/state + api/notifier_slack.** Five small modules whose existing tests covered the typical happy path but left one or two operator-observable boundaries unpinned each. Files:

- [crates/proxy/src/kill_cache.rs](../../crates/proxy/src/kill_cache.rs) — 2 new tests. `mark_repeated_same_hash_remains_killed_no_panic` pins idempotency on the happy path — the killswitch handler can land the same row twice on a quick retry; a regression to a Vec-backed duplicate-rejecting impl would surface here. `neighbor_hash_differing_by_one_byte_is_not_killed` pins byte-for-byte hash equality — a regression that truncated the hash key to 16 bytes "for faster lookup" would let one revoked bearer also kill a neighbor sharing a prefix.
- [crates/proxy/src/forwarder/tee.rs](../../crates/proxy/src/forwarder/tee.rs) — 1 new test. `no_sinks_publish_skips_join_all_branch_without_panic` pins the `if self.sinks.is_empty() { return; }` early-return — three sequential publishes on a zero-sink Tee assert that the primary still gets every event and no panic surfaces (`Vec::with_capacity(0)`-then-join_all of an empty future set is the natural shape if a refactor dropped the guard).
- [crates/proxy/src/notifier/handle.rs](../../crates/proxy/src/notifier/handle.rs) — 1 new test. `handle_new_with_initial_none_first_current_is_none` is the symmetric pair to the Some-init test from round 46 — pins that `Handle::new(None)` surfaces `None` on first read (not a default-built notifier from a refactor that pre-populated an empty `Arc<T>` for ergonomics) plus that repeat reads stay None (defends against a lazy-init refactor that materializes on first call).
- [crates/proxy/src/adapters/state.rs](../../crates/proxy/src/adapters/state.rs) — 1 new test. `google_api_base_fallback_uses_https_scheme_not_http` is defense-in-depth on the production-fallback URL: a regression that fat-fingered `http://` (or dropped the scheme entirely) would route adapter calls to plaintext on the production googleapis hostname — every request would then fail TLS but the failure mode is a 30s connect timeout rather than a clean error, masking the misconfiguration in operator alerts.
- [crates/proxy/src/api/notifier_slack.rs](../../crates/proxy/src/api/notifier_slack.rs) — 2 new tests. `slack_err_carries_application_json_content_type_header` pins the `content-type: application/json` header on the error helper — Slack's client renders the inline-message body iff the header is application/json; a copy-paste regression that dropped it (or emitted `text/plain`) would silently turn every operator-facing button-click error into an unrendered raw-text bubble. `slack_ok_message_carries_application_json_content_type_header` pins the symmetric invariant on the success helper — the two helpers share the same Slack-rendering contract and must move in lockstep.

proxy bin tests: **591 → 598**. Cumulative across rounds 1–61: proxy **+438**, cli +17, policy-engine **+76**, shared-types **+13** — **544 new pure-helper unit tests** total. Gate floors unchanged (workspace 45/55, per-crate proxy 45 / shared-types 95 / policy-engine 88 / cli 10). `cargo fmt --all --check`, `cargo clippy --workspace --all-targets --locked -- -D warnings -A clippy::type_complexity -A clippy::too_many_arguments -A clippy::result_large_err`, `cargo test --workspace --locked`, and `RUSTFLAGS="-D warnings" cargo build --workspace --release --locked` all green. Gate floors unchanged (workspace 45/55, per-crate proxy 45 / shared-types 95 / policy-engine 88 / cli 10). `cargo fmt --all --check`, `cargo clippy --workspace --all-targets --locked -- -D warnings -A clippy::type_complexity -A clippy::too_many_arguments -A clippy::result_large_err`, `cargo test --workspace --locked`, and `RUSTFLAGS="-D warnings" cargo build --workspace --release --locked` all green.

**2026-05-17 (round 62) — 7 more pure-helper tests on oauth/routes calendar mappings + api/notifier_slack envelope boundaries.** Two modules whose existing rounds (48 + 61 respectively) had drilled most of the surface but left specific operator-actionable boundaries unpinned: `narrowed_ops_for_pca1` had never been exercised on either calendar scope arm (existing tests pinned `drive:` + `gmail:send:` + unknown-scope-empty but not the two calendar mappings — a copy-paste regression that pointed the full-calendar arm at `calendar:read:` would silently narrow every calendar PCA to read-only, a privilege-escalation surface in the opposite direction); the slack helpers had been pinned for content-type + status + the in-channel keys + empty-text, but the cross-helper envelope distinction (slack_err must NOT carry `response_type` / `replace_original` or Slack would attempt to replace the operator's channel button-click with the raw error JSON), the serde-vs-format-template safety (special chars in the message must escape through serde rather than concatenate into invalid JSON), the exact three-key contract on the success envelope, and the empty-header-value boundary (present-but-empty vs absent) were all unpinned. Files:

- [crates/proxy/src/oauth/routes.rs](../../crates/proxy/src/oauth/routes.rs) — 3 new tests. `narrowed_ops_for_pca1_calendar_full_scope_keeps_all_calendar_prefixes` pins that the bare `calendar:` prefix mapping passes read/write/delete ops through unchanged (the existing test only covered drive + gmail.send). `narrowed_ops_for_pca1_calendar_readonly_keeps_only_calendar_read_subset` pins the asymmetry — `calendar.readonly` maps to the narrower `calendar:read:` prefix only, so write/delete ops MUST be filtered out even though they share the `calendar:` head. A regression that mapped readonly to the bare `calendar:` prefix (matching the drive case) would silently grant write access via a readonly OAuth scope. `narrowed_ops_for_pca1_empty_pca0_ops_returns_empty_vec_for_any_scope` pins the empty-input boundary across three distinct scope shapes (single known, multi known, empty string) — all must collapse to an empty Vec without panicking on the `.iter().any(...)` against an empty slice.
- [crates/proxy/src/api/notifier_slack.rs](../../crates/proxy/src/api/notifier_slack.rs) — 4 new tests. `slack_err_body_omits_slack_shaped_response_keys` pins the cross-helper envelope distinction — slack_err must NOT carry `response_type` or `replace_original` (Slack would otherwise attempt to replace the operator's button click with the raw error JSON as the channel message body). `slack_err_escapes_special_chars_in_msg_via_serde_json` pins that double-quotes + newlines in the error message round-trip through serde escape rules (`\"` + `\n`) rather than concatenating into invalid JSON — a refactor that switched to a `format!` template "for simplicity" would surface here as a parse error. `slack_ok_message_body_has_exactly_three_keys` pins the documented three-key shape (`response_type` / `replace_original` / `text`) — a refactor that appended a `ts` or `blocks` array unconditionally would silently change every operator-facing button-click response shape. `header_str_returns_some_empty_for_empty_header_value` pins the empty-header-value boundary (`Some("")`, NOT `None`) — the bearer / signature middleware distinguishes header-absent from header-present-but-empty, so collapsing them would silently bypass signature checks on a signed-request attempt with an empty signature field.

proxy bin tests: **598 → 605**.

**2026-05-17 (round 63) — 7 more pure-helper tests on api/notifier redact_url + SetConfigBody + api/setup envelope/From-impl boundaries.** Two modules where the prior rounds (53 + 41 respectively) had pinned the typical happy path + several edges, but several operator-actionable boundaries remained unpinned: `redact_url` had never been exercised on empty input (the show / setup-status endpoints render every configured driver's URL through this helper before logging — a panic on one notifier's empty URL would crash the entire setup-status response) or on host:port authorities (a refactor that split on `:` to strip credentials would silently drop the port), `SetConfigBody`'s forward-compat field acceptance was only pinned for the sibling `TestRequest` envelope, and the slack-driver round-trip (distinct from the existing webhook-driver round-trip) was unpinned despite each driver carrying a different `config` payload shape. On `api/setup`: the `SetupStatus.items` empty-Vec wire shape (`[]` not `null` — the admin UI's `.length` access would crash on null), the no-truncation guarantee on the `detail` String field (the PCA-cache panel can render multi-KB count strings on long-running installs), and the `#[from] sqlx::Error` blanket-impl exercise across distinct sqlx variants (verifies the `?`-operator path works for every variant, not just the one the existing tests construct) were all unpinned. Files:

- [crates/proxy/src/api/notifier.rs](../../crates/proxy/src/api/notifier.rs) — 4 new tests. `redact_url_empty_string_returns_empty_without_panic` pins the empty-input boundary — operator-cli paths that pass an `Option::unwrap_or_default()` URL on a non-required field land here as `""`, and a panic on `split_once` would crash the setup-status endpoint. `redact_url_preserves_host_port_in_authority_segment` pins that `:port` survives the redaction (`https://siem.local:8080/...` not `https://siem.local/...`) — operators distinguish services on the same host by port in the redacted log line. `set_config_body_accepts_extra_unknown_fields_for_forward_compat` is the symmetric forward-compat pin to the existing `TestRequest` test — a regression that added `#[serde(deny_unknown_fields)]` would force every CLI shape change to be a coordinated proxy upgrade. `set_config_body_with_slack_driver_round_trips_distinct_from_webhook` pins that the envelope is driver-agnostic — a refactor that hardcoded a webhook-style `url` field expectation inside `config` (rather than slack's `bot_token` + `signing_secret`) would surface here.
- [crates/proxy/src/api/setup.rs](../../crates/proxy/src/api/setup.rs) — 3 new tests. `setup_status_with_zero_items_serializes_as_empty_array_not_null` pins the empty-Vec → `[]` (not `null`) serde guarantee against the admin UI's `.length` access. `check_item_long_detail_string_serializes_intact_with_no_truncation` pins a 4 KiB detail round-trip against a future "for log hygiene" truncation refactor — the `first_pca` check renders `format!("{count} PCA(s) in cache")` which can carry long counts on long-running installs. `setup_error_from_impl_works_for_distinct_sqlx_variants` exercises the `#[from] sqlx::Error` blanket-impl path across three distinct sqlx variants (`PoolClosed`, `RowNotFound`, `WorkerCrashed`) so the `?`-operator conversion is verified on each — a refactor that dropped `#[from]` would surface here as a compile error.

proxy bin tests: **605 → 612**. Cumulative across rounds 1–63: proxy **+452**, cli +17, policy-engine **+76**, shared-types **+13** — **558 new pure-helper unit tests** total. Gate floors unchanged (workspace 45/55, per-crate proxy 45 / shared-types 95 / policy-engine 88 / cli 10). `cargo fmt --all --check`, `cargo clippy --workspace --all-targets --locked -- -D warnings -A clippy::type_complexity -A clippy::too_many_arguments -A clippy::result_large_err`, `cargo test --workspace --locked`, and `RUSTFLAGS="-D warnings" cargo build --workspace --release --locked` all green.

**2026-05-17 (round 64) — 7 more pure-helper tests on notifier/burst urlencoding + resolver shapes + drain boundary + forwarder/siem builder invariants.** Two modules where prior rounds (27 + 24 respectively) had pinned the typical happy path and several edges, but several operator-actionable boundaries remained unpinned: `urlencoding_encode` had never been tested directly (only via the full `details_url_round_trip`), the third resolver shape `(None, Some(window))` — threshold-keep + window-override — was unpinned despite being operationally useful, `drain_summaries` on a fresh suppressor had no direct coverage (the b.suppressed==0 skip branch is the only thing standing between an idle flush tick and a spurious notification spam), and the `exemplar` field's nested struct keys were never asserted on the wire (webhook/Slack receivers render the 5-field preview). On `forwarder/siem`: the `with_max_retries` fluent setter shape, the load-bearing `assert!(max_batch_size > 0)` invariant on `with_batching`, and the `batching_enabled()` predicate's both axes (false-by-default + true-after-with-batching) were all unpinned. Files:

- [crates/proxy/src/notifier/burst.rs](../../crates/proxy/src/notifier/burst.rs) — 4 new tests. `urlencoding_encode_percent_encodes_non_alphanumeric_bytes_per_byte` pins all three boundary shapes (alphanumeric passthrough, common policy-id punctuation `-` `.` `_` `:` `@` all encoded per NON_ALPHANUMERIC, multibyte UTF-8 per-byte encoding `é` → `%C3%A9`). `resolver_threshold_none_keeps_default_window_override` pins the third resolver-shape `(None, Some(w))` — fires 60 events at the global default threshold (50) with window=0 from the resolver; all admit because each prior timestamp is immediately pruned. `drain_summaries_initially_empty_returns_empty_vec` pins the fresh-suppressor boundary so a regression that emitted a synthetic "(no events)" summary on every flush would surface here. `burst_summary_exemplar_some_serializes_with_stable_struct_keys` pins the full 5-key wire shape (`policy_id` / `p_0` / `vendor` / `action` / `layer`) against a field-rename regression that would silently break the webhook/Slack template.
- [crates/proxy/src/forwarder/siem.rs](../../crates/proxy/src/forwarder/siem.rs) — 3 new tests. `with_max_retries_is_consuming_self_fluent_setter` pins the `(mut self, n) -> Self` builder shape — a refactor to `&mut self` would force every call site to bind the forwarder to a local before chaining. `with_batching_panics_on_zero_max_batch_size` pins the fail-fast `assert!(max_batch_size > 0)` invariant (a `Vec::with_capacity(0)` buffer that then size-triggered on `len >= 0` would flush after every publish, defeating the batching purpose silently). `batching_enabled_is_false_by_default_and_true_after_with_batching` pins both axes of the predicate that server.rs uses to decide whether to spawn the flush loop — a refactor that inverted the default (e.g. "always batch") would silently change every operator's default delivery shape on next restart.

proxy bin tests: **612 → 619**.

**2026-05-17 (round 65) — 7 more pure-helper tests on api/actions CSV/PurgeResponse/ListParams + audit_body hash/base64/CC redactor boundaries.** Two modules where prior rounds (23 + 34 respectively) had pinned much of the surface, but several wire-shape and primitive-identity boundaries remained unpinned: the CSV exporter's 16-field contract had been pinned via comma count but never the negative — `extra` jsonb is intentionally NOT in the wire shape (a "for completeness" refactor that appended it would silently break every operator's spreadsheet template); the numeric-field branches of `row_to_csv_line` (`read_filter_triggered: bool`, `quarantined_count: u32`) had only been exercised at the default-zero values, never the active branch where a refactor that pushed them through the string `esc(...)` would silently quote them; `PurgeResponse` had only been round-tripped with `dry_run: true`, leaving the `false`-must-still-serialize-not-elide axis (the operator-dashboard "DESTRUCTIVE" banner keys on key-presence) unpinned; `ListParams` partial-query-string parsing (the natural shape of operator-cli `--filter` flags) was unpinned despite the existing empty-query-string test. On `audit_body`: `sha256_hex`'s distinct-input-yields-distinct-digest contract had only been pinned indirectly (the bearer.rs tests' `hash_differs_across_tokens` test covers a different helper), `base64_encode`'s STANDARD-vs-URL_SAFE alphabet identity (the audit pipeline persists base64 bodies; replay tools downstream invoke STANDARD decoders — a switch would silently corrupt every existing replay), and the credit-card redactor's surrounded-by-text shape (the most common real-world PII leak: "card: 4111-1111-1111-1111 ok") were all unpinned. Files:

- [crates/proxy/src/api/actions.rs](../../crates/proxy/src/api/actions.rs) — 4 new tests. `row_to_csv_line_excludes_extra_jsonb_field_from_wire` pins the negative — even when `extra` carries a sentinel string, it doesn't leak into the CSV line (a "for completeness" refactor that appended it would silently break the documented 16-field export schema). `row_to_csv_line_renders_read_filter_triggered_and_quarantined_count_when_nonzero` pins the unquoted numeric/boolean shapes (`,true,` not `,"true",`; `,17,` not `,"17",`) so a regression that routed them through the string `esc(...)` would surface here. `purge_response_serializes_dry_run_false_as_explicit_false_not_absent` pins the symmetric `false`-axis to the existing `true`-round-trip — a future `#[serde(skip_serializing_if = "is_false")]` micro-opt would silently flip the semantics. `list_params_partial_query_string_sets_only_named_filters_keeping_others_none` pins that setting `vendor=google&limit=25` leaves the other 5 filter fields as None (not Some("")) — a serde drift to `String` with `#[serde(default)]` would silently change every "filter by vendor only" query into a filter-by-vendor-AND-empty-string composite that matches no rows.
- [crates/proxy/src/audit_body.rs](../../crates/proxy/src/audit_body.rs) — 3 new tests. `sha256_hex_distinct_inputs_yield_distinct_digests` pins the collision-separation contract directly so a mock-leaks-into-production refactor that hashed a constant would surface here (the `request_hash` column is used as a JOIN key across replays — a constant-output regression would silently collapse every replay onto the same group). `base64_encode_alphabet_includes_plus_and_slash_not_url_safe_chars` pins STANDARD vs URL_SAFE identity via two payloads that force each alphabet's distinctive bytes on the wire (`>>>` → `Pj4+` for `+`; `\xFF\xFF\xFF` → `////` for `/`) and asserts URL_SAFE's `-`/`_` do NOT appear. `redact_pii_text_credit_card_in_text_surroundings_still_redacts` pins the `\b` word-boundary semantic on the most common real-world shape (CC adjacent to non-digit context) and asserts both that the CC is redacted and that the surrounding prefix/suffix bytes survive — a regression that swapped `\b` for `(?:^|\s)` would surface here.

proxy bin tests: **619 → 626**. Cumulative across rounds 1–65: proxy **+466**, cli +17, policy-engine **+76**, shared-types **+13** — **572 new pure-helper unit tests** total. Gate floors unchanged (workspace 45/55, per-crate proxy 45 / shared-types 95 / policy-engine 88 / cli 10). `cargo fmt --all --check`, `cargo clippy --workspace --all-targets --locked -- -D warnings -A clippy::type_complexity -A clippy::too_many_arguments -A clippy::result_large_err`, `cargo test --workspace --locked`, and `RUSTFLAGS="-D warnings" cargo build --workspace --release --locked` all green.

**2026-05-17 (round 66) — 6 more pure-helper tests on enforce_pre_request_decision parity across adapters/google_calendar + adapters/google_gmail.** Cross-adapter parity guard on a 4-arm match expression that exists byte-identically in three places: drive.rs had full coverage (Allow / Block-with-Some / Block-with-None / RequireConfirmation / RateLimit — 5 tests across rounds 56 + 59), gmail.rs had two arms (Block + RequireConfirmation), and calendar.rs had ZERO. The three helpers are deliberately mirrored — without coverage on the missing arms they can drift independently on the next refactor that touched only one adapter (e.g. a "leak the rate-limit reason into AppError" change that landed on calendar alone would spec.md §3.4 the agent error body for calendar requests but not the others). Files:

- [crates/proxy/src/adapters/google_calendar.rs](../../crates/proxy/src/adapters/google_calendar.rs) — 4 new tests filling every arm. `enforce_pre_request_decision_allow_returns_ok_for_calendar` pins `Allow → Ok(())` (a regression to `AppError::Internal` would 500 every calendar fetch). `enforce_pre_request_decision_block_carries_policy_id_reason_and_override_for_calendar` pins all three fields surviving the `Block → PolicyBlocked` translation — a copy-paste bug that forgot `override_allowed` would silently break the dashboard's "this block is overridable" indicator on every calendar deny. `enforce_pre_request_decision_require_confirmation_carries_reason_for_calendar` pins the inner-String round-trip (a redact-for-privacy refactor would strip operator reasons from confirm prompts). `enforce_pre_request_decision_rate_limit_returns_app_error_for_calendar` pins the bare `AppError::RateLimit` shape — a refactor that passed through the rate-limit reason would silently start leaking server state per spec.md §3.4.
- [crates/proxy/src/adapters/google_gmail.rs](../../crates/proxy/src/adapters/google_gmail.rs) — 2 new tests filling the two gmail-side gaps. `enforce_allow_returns_ok_for_gmail_adapter` is the symmetric Allow guard (drive has `enforce_allow_is_ok`; gmail had nothing — a regression that mapped Allow → Internal would 500 every gmail send). `enforce_rate_limit_returns_app_error_rate_limit_for_gmail` is the RateLimit parity guard (drive has `enforce_rate_limit`; gmail had nothing).

proxy bin tests: **626 → 632**.

**2026-05-17 (round 67) — 6 more pure-helper tests on notifier/email parse_or_fallback fail-fast + adapters/state override boundaries.** Two modules where prior rounds (33 + 18+61 respectively) had pinned much of the surface but specific operational shapes remained unpinned: `parse_or_fallback` had the "any-malformed kicks back to fallback" pin on a 2-element list (good, bad) — the mid-list case where the FIRST TWO succeed but the third fails was unpinned despite this being where a `return fallback.to_vec()` → `out.extend(fallback.clone())` regression would surface (silently doubling the recipient list on every transient YAML typo). The `field` parameter's no-effect-on-output contract was unpinned despite future-driver-routing regressions being the natural shape of a refactor that started keying on it. The empty-fallback boundary (operators who require per-policy recipients with no proxy-wide default) was unpinned despite being the spec.md §5.7 escalation pipeline's contract. On `adapters/state`: the `Option<String>::Some(String::new())` wire-distinct shape, the trailing-slash passthrough, and the multi-segment path passthrough (the wiremock sub-path fixture shape) were all unpinned. Files:

- [crates/proxy/src/notifier/email.rs](../../crates/proxy/src/notifier/email.rs) — 3 new tests. `parse_or_fallback_fail_fast_on_mid_list_malformed_returns_fallback_not_partial` pins a 4-element list (valid, valid, malformed, valid) where the helper must return ONLY the fallback (not `parsed-prefix + fallback` — that hybrid would silently double recipients on every transient YAML typo). `parse_or_fallback_field_param_does_not_affect_output_only_log_label` pins three distinct field labels (`"to"` / `"cc"` / `"bcc"`) producing byte-identical output for the same input — a regression that started routing on `field` would silently corrupt per-policy BCC overrides. `parse_or_fallback_works_with_empty_fallback_when_input_is_valid` pins both branches of the empty-fallback case: happy-path parses skip the fallback (surface the parsed list); malformed-input path surfaces the empty fallback verbatim (which is the operator's explicit choice — fail closed on the per-policy override).
- [crates/proxy/src/adapters/state.rs](../../crates/proxy/src/adapters/state.rs) — 3 new tests. `google_api_base_override_with_empty_string_returns_empty_not_fallback` pins `Some("")` as wire-distinct from `None` — `as_deref().unwrap_or(...)` returns `Some("")` verbatim, NOT the fallback URL. A refactor to `.filter(|s| !s.is_empty()).unwrap_or(...)` would silently change the operator's explicit-empty choice into the production fallback. `google_api_base_override_preserves_trailing_slash_unchanged` pins the no-trim contract (adapter call sites append their own path segments — a `.trim_end_matches('/')` regression would change every adapter request URL by one byte). `google_api_base_override_preserves_multi_segment_path_verbatim` pins the wiremock sub-path fixture shape (`http://127.0.0.1:9000/google/v1`) — a `url::Url::host_str` refactor would strip the sub-path and re-route every adapter call to the wiremock root.

proxy bin tests: **632 → 638**. Cumulative across rounds 1–67: proxy **+478**, cli +17, policy-engine **+76**, shared-types **+13** — **584 new pure-helper unit tests** total. Gate floors unchanged (workspace 45/55, per-crate proxy 45 / shared-types 95 / policy-engine 88 / cli 10). `cargo fmt --all --check`, `cargo clippy --workspace --all-targets --locked -- -D warnings -A clippy::type_complexity -A clippy::too_many_arguments -A clippy::result_large_err`, `cargo test --workspace --locked`, and `RUSTFLAGS="-D warnings" cargo build --workspace --release --locked` all green.

**2026-05-17 (round 68) — 7 more pure-helper tests on api/notifier_public render_result/fill_template branches + kill_cache documented-constant pins.** Two modules where prior rounds (39 + 13+61 respectively) had pinned the typical happy path but specific operator-visible branches and the load-bearing const values remained unpinned: `render_result` had been exercised indirectly only via `render_already_used` (always `ok=true`), leaving the symmetric `ok=false`/`banner-err` branch + the `banner-ok` CSS class identity unpinned despite both being the operator-visible green-vs-red distinction in the approver UI. `fill_template`'s 3-arm action_title match had no test directly asserting the per-arm strings (only the placeholders), so a refactor that collapsed the unknown-action catch-all into the reject branch would silently change the page title. The `requested_ops.join(", ")` separator (comma + space) had no direct pin — a regression to `","` (no space) or `\n` would silently change every approver UI render. On `kill_cache`: the documented `ENTRY_TTL = 3600s` per spec.md §3.2 + `MAX_CAPACITY = 100_000` per the file docstring's operational sizing were unpinned despite both being load-bearing operational decisions; and `Clone`'s shared-state semantic (a mark on one clone must be visible through the other) was unpinned despite this being the contract every AppState clone path depends on for killswitch enforcement to work. Files:

- [crates/proxy/src/api/notifier_public.rs](../../crates/proxy/src/api/notifier_public.rs) — 4 new tests. `render_result_with_ok_true_uses_banner_ok_css_class` pins the success-path CSS class + asserts no leak of `banner-err`. `render_result_with_ok_false_uses_banner_err_css_class` is the symmetric failure-path pin (the existing `render_form_unknown_action_shows_banner_err` test hits a different code path — inline banner string, not the `render_result` helper). `fill_template_action_title_renders_three_distinct_strings_for_approve_reject_unknown` pins all three arms with both positive (correct title appears) and negative (other titles don't leak) assertions so a "simplify the match" refactor that collapsed the catch-all surfaces here. `fill_template_requested_ops_joins_with_comma_space_separator` pins the `, ` separator via a 3-op list — a regression to `","` or `"\n"` would silently change the operator-visible rendering.
- [crates/proxy/src/kill_cache.rs](../../crates/proxy/src/kill_cache.rs) — 3 new tests. `entry_ttl_constant_pinned_at_one_hour_per_spec` pins `ENTRY_TTL == Duration::from_secs(3600)` (spec.md §3.2). `max_capacity_constant_pinned_at_one_hundred_thousand` pins `MAX_CAPACITY == 100_000` (the docstring-documented operational sizing). `clone_shares_underlying_moka_cache_marks_visible_across_clones` pins the load-bearing Arc-share semantic of moka's `Cache::clone` via bidirectional mark/observe across two clones — a refactor that deep-copied the cache would silently break killswitch enforcement on every replica with > 1 in-flight request.

proxy bin tests: **638 → 645**.

**2026-05-17 (round 69) — 6 more pure-helper tests on adapters/read_filter should_scan content-type parsing + session.rs extractor + 401 wire shape.** Two modules where prior rounds (11+60 + 35+59 respectively) had pinned the typical happy path but specific real-world wire shapes and consume-vs-clone extractor semantics remained unpinned: `should_scan` is called with the EXACT Content-Type header bytes Google's APIs return (`application/json; charset=utf-8` — the parameterized shape) but no test exercised the `;` split branch on a real production header. The case-insensitive normalization was unpinned despite RFC 9110 §8.3.1 requiring it. The `Some("")` boundary (wire-distinct from None, lands as false after split/trim) was unpinned. On `session.rs`: the 401 response's content-type default (`text/plain; charset=utf-8`) was unpinned despite operator log parsers splitting "agent session lost" 401s from middleware-rejected structured 401s on this axis. The extractor's `cloned()`-not-`take()` semantic — letting a second extractor call against the same parts still succeed — was unpinned despite this being the contract any multi-extractor handler chain depends on. The typed-lookup-misses-on-wrong-type contract (a different extension type stuffed in must NOT satisfy `Arc<SessionContext>` lookup) was unpinned. Files:

- [crates/proxy/src/adapters/read_filter.rs](../../crates/proxy/src/adapters/read_filter.rs) — 3 new tests. `should_scan_strips_parameters_after_semicolon_before_matching` pins all three production shapes (`application/json; charset=utf-8` with space, `;charset=utf-8` without, `text/html; charset=utf-8`) — Google's APIs return the parameterized form and a regression that compared the raw header would silently no-op scanning on every JSON response. `should_scan_with_uppercase_content_type_normalizes_case_before_matching` pins the RFC 9110 §8.3.1 case-insensitive contract via `APPLICATION/JSON` + `Application/Json` + `TEXT/HTML`. `should_scan_with_empty_content_type_string_does_not_scan` pins the `Some("")` boundary as wire-distinct from `None` (Some empty → false, mirroring "no recognized type" rather than the default-scan None path).
- [crates/proxy/src/session.rs](../../crates/proxy/src/session.rs) — 3 new tests. `session_extract_error_response_content_type_defaults_to_text_plain` pins the `text/plain; charset=utf-8` shape that axum's `&'static str → Response` impl produces — a refactor that swapped to a Json envelope would surface here, distinct from operator dashboards that split structured-401 vs plain-401 paths. `extractor_does_not_consume_extension_extractor_safe_for_repeat_calls` pins the `cloned()`-not-`take()` semantic via two sequential extractions against the same parts that both succeed + share the same `Arc::ptr_eq` inner context — a `remove()` regression would silently break handler chains running the extractor twice. `extractor_rejects_when_only_a_different_extension_type_is_present` pins the typed-lookup contract by stuffing an unrelated extension type into parts.extensions and asserting the extractor still surfaces `SessionExtractError` — a refactor that loosened the lookup would surface as an unexpected Ok.

proxy bin tests: **645 → 651**. Cumulative across rounds 1–69: proxy **+491**, cli +17, policy-engine **+76**, shared-types **+13** — **597 new pure-helper unit tests** total. Gate floors unchanged (workspace 45/55, per-crate proxy 45 / shared-types 95 / policy-engine 88 / cli 10). `cargo fmt --all --check`, `cargo clippy --workspace --all-targets --locked -- -D warnings -A clippy::type_complexity -A clippy::too_many_arguments -A clippy::result_large_err`, `cargo test --workspace --locked`, and `RUSTFLAGS="-D warnings" cargo build --workspace --release --locked` all green.

**2026-05-17 (round 70) — 6 more pure-helper tests on oauth/bridge validate boundaries + pic/executor error/debug/trait coverage.** Two modules where prior rounds (4+10+49 + 14+19+56 respectively) had pinned the typical happy path but specific boundaries and remaining variants were unpinned: the exp boundary on `validate_federation_token` is `if now > claims.exp` (strict greater) — the inclusive `now == exp` case (must accept) had no test; the third JWT failure mode (b64 decodes cleanly but JSON parsing fails on missing required fields) was unpinned even though operator log filters key on the `"bad claims"` substring; the `oidc` fallback bucket on `infer_idp` had a single test (`id.example.org`) and never asserted the bucket survives across diverse non-named-IdP shapes (Keycloak, Auth0-style). On `pic/executor`: the `Transport` and `Base64` `#[from]` variants of `ExecutorError` had no Display pin (the existing test covered Upstream + Invariant + Core only); `SuccessorOutcome::Debug` had only the `AuditFallback` arm pinned (the `Issued` arm + inner `ProcessPocResponse` Debug pass-through was unpinned); the `std::error::Error` trait-object castability the anyhow chain depends on was unpinned. Files:

- [crates/proxy/src/oauth/bridge.rs](../../crates/proxy/src/oauth/bridge.rs) — 3 new tests. `validate_federation_token_at_exact_exp_boundary_accepts_inclusive` pins `now == exp` accepts (strict-greater check, not `>=`) — a refactor that flipped the comparison would silently shrink the validity window by one second and make flows flaky around the wall-clock boundary. `validate_federation_token_with_b64_valid_but_json_invalid_carries_bad_claims_msg` pins the third failure mode's operator-facing `"bad claims"` substring (the OAuth callback logs key on it). `infer_idp_returns_oidc_bucket_for_non_well_known_https_issuer_variants` pins the `oidc` fallback across three distinct generic IdP shapes (Keycloak realm path, custom `login.acme.dev`, internal Keycloak) — a refactor that tightened the fallback would silently hide non-named-IdP traffic under "unknown" on operator dashboards.
- [crates/proxy/src/pic/executor.rs](../../crates/proxy/src/pic/executor.rs) — 3 new tests. `executor_error_transport_and_base64_display_strings_carry_distinct_prefixes` pins the two `#[from]`-derived variants' prefixes (`"Trust Plane request failed:"` for Transport, `"base64:"` for Base64) — operator log filters split transport flake from encoded-bytes corruption on these substrings. `successor_outcome_debug_includes_issued_variant_name_for_audit_trail` is the symmetric Debug pin to `successor_outcome_debug_carries_variant_name_and_detail` (which only covered AuditFallback) — pins the Issued variant name + inner `ProcessPocResponse` fields surface through. `executor_error_implements_std_error_trait_for_anyhow_chains` pins the trait-object castability via `&dyn std::error::Error` against two distinct construction paths (Invariant via direct construction, Upstream via named-field initializer) so a refactor that dropped `#[derive(Error)]` would surface here as a compile error.

proxy bin tests: **651 → 657**.

**2026-05-17 (round 71) — 6 more pure-helper tests on demo.rs SCENARIOS invariants + notifier/handle email branch + api/blocked DTO forward-compat.** Three modules where prior rounds (58 + 15+46+61 + 5+6+60 respectively) had pinned the typical happy path but specific invariants and the third OR-chain branch remained unpinned: the SCENARIOS decision/status pairings (allow→200, block→403, require_confirmation→428) form a wire contract operator dashboards alert on, but no test directly pinned the per-arm pairing across the array; the `leaf_pca_id: Some(_)` invariant for the demo chain-walker dashboard was unpinned; the status round-trip was unpinned (any normalization would silently break the demo's exact-status display). On `notifier/handle`: the OR-chain in `any_configured()` had branches pinned for webhook + slack, but the email branch was unpinned despite being symmetric — a copy-paste typo on the email line could only surface here. On `api/blocked`: `ApproveBody`'s forward-compat field acceptance (no `#[serde(deny_unknown_fields)]`) was unpinned for that DTO (other DTOs in the module have it); `RejectBody`'s Some-approver-subject path (operator-driven vs system-driven reject — audit attribution depends on this) was unpinned. Files:

- [crates/proxy/src/demo.rs](../../crates/proxy/src/demo.rs) — 3 new tests. `scenario_decision_status_pairs_match_http_class_per_arm` walks every SCENARIO and pins the decision/status pairing (allow→200, block→403, require_confirmation→428) — a copy-paste typo that mistyped a status code would surface here rather than as data corruption on the demo dashboard. `synth_event_leaf_pca_id_is_always_some_for_demo_chain_visibility` pins `leaf_pca_id.is_some()` across every scenario — required for the chain-walker UX to surface non-empty data on every demo row. `synth_event_status_field_round_trips_scenario_status_verbatim` pins no-transformation on the status passthrough across all four SCENARIOS.
- [crates/proxy/src/notifier/handle.rs](../../crates/proxy/src/notifier/handle.rs) — 1 new test. `any_configured_triggers_on_email_alone_as_third_or_chain_branch` is the symmetric coverage for the email branch (webhook + slack had their own tests). Builds a real `EmailNotifier` so the type-system also checks the `Handle<EmailNotifier>` wiring at the call site — a copy-paste typo that duplicated the slack check would surface here as the email branch never triggering.
- [crates/proxy/src/api/blocked.rs](../../crates/proxy/src/api/blocked.rs) — 2 new tests. `approve_body_accepts_unknown_extra_fields_for_forward_compat` pins the CLI forward-compat path — a refactor that tightened to `#[serde(deny_unknown_fields)]` would force every CLI shape change to be a coordinated proxy upgrade. `reject_body_accepts_optional_approver_subject_when_set` pins the with-subject deserialize path (the existing test only covered the no-subject minimal shape) — the handler distinguishes operator-driven reject (Some) from system-driven reject (None) for audit attribution.

proxy bin tests: **657 → 663**. Cumulative across rounds 1–71: proxy **+503**, cli +17, policy-engine **+76**, shared-types **+13** — **609 new pure-helper unit tests** total. Gate floors unchanged (workspace 45/55, per-crate proxy 45 / shared-types 95 / policy-engine 88 / cli 10). `cargo fmt --all --check`, `cargo clippy --workspace --all-targets --locked -- -D warnings -A clippy::type_complexity -A clippy::too_many_arguments -A clippy::result_large_err`, `cargo test --workspace --locked`, and `RUSTFLAGS="-D warnings" cargo build --workspace --release --locked` all green.

**2026-05-17 (round 72) — 6 more pure-helper tests on policy-engine yaml schema + notifier/burst Bucket+config_for boundaries.** Two modules where prior rounds (32+33 + 27+64 respectively) had pinned much of the schema and resolver surface but specific operator-actionable fields and the private `Bucket::prune` boundary remained unpinned: `RecipientsCfg::escalation_after_minutes` (the ui-less §5.7 dev 2 per-policy escalation deadline the expiry sweeper reads to compute `escalation_at = blocked_at + N min`) had no direct round-trip — a regression that dropped `#[serde(default)]` would force every existing policy to fail-parse on reload; the empty-object boundary on `BurstCfg` (`notifier_burst: {}` — the operator declares intent without committing to dials) had no test; and the `default_quarantine_action` helper itself (the spec.md §6.4 safe-posture default) was only exercised indirectly via a YAML round-trip. On `notifier/burst`: the private `Bucket::prune` helper's `<=` boundary (load-bearing for window-inclusive event retention — a strict-`<` regression would silently shrink every window by one tick), the no-resolver `config_for` branch (the most-common production shape), and the composed `with_details_url` percent-encoding on reserved chars in BOTH `policy_id` AND `p_0` (round 64 pinned `urlencoding_encode` directly but never the composed path) were all unpinned. Files:

- [crates/policy-engine/src/yaml.rs](../../crates/policy-engine/src/yaml.rs) — 3 new tests. `recipients_cfg_round_trips_escalation_after_minutes_field` pins all three shapes (Some(240), absent → None, explicit zero → Some(0) — the operator-driven "fire immediately" sentinel must NOT collapse to None). `burst_cfg_empty_object_deserializes_with_both_fields_none` pins that `notifier_burst: {}` lands as both-None rather than pre-filled defaults — a regression that added `#[serde(default = "...")]` to either dial would silently start suppressing on next reload before the operator committed to the override. `default_quarantine_action_helper_returns_replace_with_marker` pins the function-pointer return directly so a one-character typo (`StripSilently`) surfaces here as the canonical posture failing rather than at the first operator's policy reload.
- [crates/proxy/src/notifier/burst.rs](../../crates/proxy/src/notifier/burst.rs) — 3 new tests. `bucket_prune_drops_only_timestamps_outside_window_keeps_boundary_inclusive` pins the `now - t <= window` inclusive boundary by constructing Instants at -120s / -61s / -60s / -30s / now and asserting prune retains exactly 3 (60s + inside + present). `config_for_with_no_resolver_returns_default_unchanged` pins the no-override happy path (a regression that pre-populated a "safe" override would silently shadow operator-configured defaults). `with_details_url_url_encodes_reserved_chars_in_policy_id_and_p_0` pins the composed wire URL against a `team-a/share?audit` policy id + `alice+suffix@acme.com` p_0 pair (every reserved char must percent-encode) — a refactor that bypassed the encoder for either segment for "readability" would silently break every deep link with `&` or `?` in the policy id.

proxy bin tests: **663 → 666**. policy-engine lib tests: **134 → 137**.

**2026-05-17 (round 73) — 6 more pure-helper tests on adapters/read_filter helper boundaries + notifier/slack truncate+plural + notifier/handle email default.** Three modules where prior rounds (11+60+69 + 26 + 15+46+61+71 respectively) had pinned much of the surface but specific helper boundaries and the third Handle-field default remained unpinned: `truncate`'s exact-equal boundary (`<= n` is the no-truncate predicate — at exactly n chars the input must pass through verbatim with NO ellipsis appended), `merge_overlapping`'s empty-input boundary (the `out.last_mut()` initial-`None` arm — a refactor that pre-allocated would surface here as a panic), and the `text/plain` branch of `should_scan` (the most common log-fixture content type — only `text/html` was pinned in earlier rounds). On `notifier/slack`: the sibling `truncate` helper uses `> n` for the append-ellipsis predicate (different shape from the read_filter sibling — a "harmonize the two helpers" refactor would only surface with both pinned independently), and `plural`'s naive-pluralization shape (it does NOT know about English plurals — a future "smart pluralizer" refactor would silently change every burst-summary subject line). On `notifier/handle`: the `email` field's default-None on `Notifiers::empty` was only collectively asserted via the OR-chain in `bundle_starts_empty` — a regression that pre-populated email alone wouldn't show up there because the OR short-circuits on the webhook check first. Files:

- [crates/proxy/src/adapters/read_filter.rs](../../crates/proxy/src/adapters/read_filter.rs) — 3 new tests. `truncate_at_exact_n_returns_unchanged_no_ellipsis_appended` pins both ascii (10 chars → 10) and multibyte (5 Greek codepoints → 5) at the equal boundary + the just-over (n+1) case where the ellipsis appears. `merge_overlapping_empty_input_returns_empty_vec` pins the zero-element case directly so a `.first().unwrap()` regression would surface here. `should_scan_explicitly_accepts_text_plain_via_text_prefix` pins three concrete `text/*` shapes (plain, csv, markdown) against a "closed accept-list for performance" refactor.
- [crates/proxy/src/notifier/slack.rs](../../crates/proxy/src/notifier/slack.rs) — 2 new tests. `truncate_at_exact_n_passes_through_without_appending_ellipsis` pins the strict-`>` boundary on 140-char input (the context-block budget the `block_kit_payload` site depends on relies on this exact boundary not eating the final visible char) + the just-over n+1 case. `plural_pluralizes_word_that_already_ends_in_s_naively` pins the current naive `+ "s"` behavior across `1 process` (singular), `2 processs` (intentionally awkward — a regression that "fixed" this would silently change every operator-facing message render), and `0 blocks`.
- [crates/proxy/src/notifier/handle.rs](../../crates/proxy/src/notifier/handle.rs) — 1 new test. `empty_bundle_starts_with_none_for_email_handle_too` pins all three Handle fields' initial-None directly (not via `any_configured` which OR-short-circuits) so a regression that pre-populated any single field via `Arc::new(default)` in `empty()` would surface here as the wrong default rather than as a phantom driver firing at boot.

proxy bin tests: **666 → 672**. Cumulative across rounds 1–73: proxy **+515**, cli +17, policy-engine **+79**, shared-types **+13** — **624 new pure-helper unit tests** total. Gate floors unchanged (workspace 45/55, per-crate proxy 45 / shared-types 95 / policy-engine 88 / cli 10). `cargo fmt --all --check`, `cargo clippy --workspace --all-targets --locked -- -D warnings -A clippy::type_complexity -A clippy::too_many_arguments -A clippy::result_large_err`, `cargo test --workspace --locked`, and `RUSTFLAGS="-D warnings" cargo build --workspace --release --locked` all green.

**2026-05-17 (round 74) — 6 more pure-helper tests on policy-engine match_expr private helpers.** The match-expression interpreter (`policy-engine/src/match_expr.rs`) had 33 tests covering the operator-vocabulary happy paths + most error branches, but several private-helper invariants the docs page and operator log filters depend on were unpinned: `as_str_subst`'s Null arm (load-bearing for "missing field" detection through equals/not_equals — a regression collapsing Null→Some("") would silently make every `field IS NULL`-shape policy fire against the empty string) was untested directly; the Number arm's `n.to_string()` shape (a refactor to `{n:?}` would render integers with a trailing `.0` and break operator-authored thresholds); the `greater_than` rhs's float-branch path (only int rhs was pinned despite YAML floats being the natural shape for fractional thresholds like spam-score gates); the `less_than` non-numeric-lhs fall-through (symmetric pin to the existing `greater_than` test — a refactor that special-cased less_than alone to error rather than return false would slip past); the `type_name` enumeration's full variant set (operator log filters key on the exact strings `null`/`bool`/`number`/`string`/`sequence`/`mapping` — a pluralization or capitalization regression would silently break docs cross-references); and `as_str_seq`'s non-sequence rhs error with op-label round-trip (the operator surface for malformed `in: 5` / `in: "x"`). Files:

- [crates/policy-engine/src/match_expr.rs](../../crates/policy-engine/src/match_expr.rs) — 6 new tests. `as_str_subst_renders_yaml_null_as_ok_none` pins the Null arm. `as_str_subst_renders_yaml_number_and_bool_as_their_string_forms` pins the integer-without-trailing-`.0` shape + the bool true/false strings. `greater_than_with_float_rhs_round_trips_through_as_f64` pins the float-rhs branch via `recipient_count: greater_than: 6.5` (7>6.5 true, 7>7.5 false). `less_than_with_non_numeric_lhs_returns_false_gracefully` pins the symmetric fall-through. `type_name_distinguishes_every_yaml_variant_via_explicit_input` walks all six variants. `as_str_seq_rejects_non_sequence_rhs_with_bad_shape_and_op_label` pins both `in` and `not_in` op-label round-trips through BadShape's `op` field.

policy-engine lib tests: **137 → 143**.

**2026-05-17 (round 75) — 6 more pure-helper tests on oauth/routes scope mappings + api/setup field types + api/policy source-order.** Three modules where prior rounds (48+62+intersect_scope earlier + 41+63 + 37+71 respectively) had pinned the typical happy path but specific cross-prefix scope interactions, field-type invariants, and source-order preservation remained unpinned: `intersect_scope_with_ops` had been tested for single-prefix vendors (drive) but never for split-prefix vendors (gmail.readonly vs gmail.send — the prefix-distinction is the only thing standing between gmail.send-only ops silently granting gmail.readonly downstream); `narrowed_ops_for_pca1`'s drive.readonly variant (which maps to the bare `drive:` prefix, unlike calendar.readonly which splits to `calendar:read:` — the asymmetry has been a footgun in prior code reviews) was unpinned; the mixed known+unknown scope forward-compat shape was unpinned; `new_auth_code`'s alphabet exclusion contract (must never emit `=` padding or `0`/`1`/`8`/`9` digits — a swap to Crockford's base32 would silently break receivers) was only positively asserted. On `api/setup`: the `CheckItem.id` field's `&'static str` type contract (operator Grafana panels key on `item.id == "database"` and log aggregators intern these — a refactor to `String` for "consistency with detail" would silently double per-item memory + add an allocation per response). On `api/policy`: `parse_listing` source-order preservation across multiple policies (operators author them in deliberate non-alphabetical intent order; a HashMap intermediate would silently sort and break the operator's mental model). Files:

- [crates/proxy/src/oauth/routes.rs](../../crates/proxy/src/oauth/routes.rs) — 4 new tests. `intersect_scope_filters_gmail_readonly_when_only_send_ops_present` pins the gmail prefix split (gmail.send kept, gmail.readonly filtered) — the prior `drive`-only test couldn't catch a regression that conflated the two gmail prefixes. `narrowed_ops_for_pca1_drive_readonly_keeps_all_drive_prefix_ops` pins drive.readonly's mapping to the bare `drive:` prefix (vs calendar.readonly which splits) — and that gmail ops in a mixed PCA-0 set are still filtered. `narrowed_ops_for_pca1_unknown_scope_in_mix_falls_through_filter` pins forward-compat (a future Google scope addition silently drops rather than panicking). `new_auth_code_alphabet_excludes_padding_char_and_digits_0_1_8_9` pins the RFC 4648 alphabet exclusion across 8 random samples (256 random bytes) — catches a swap to Crockford's base32 or to padded standard base32.
- [crates/proxy/src/api/setup.rs](../../crates/proxy/src/api/setup.rs) — 1 new test. `check_item_id_field_is_static_str_for_zero_alloc_logging` pins the literal `&'static str` field type (enforced at compile time by the field) + the wire JSON round-trip carrying the literal verbatim — a refactor to `String` would surface as a compile error rather than as silent per-request allocation regression.
- [crates/proxy/src/api/policy.rs](../../crates/proxy/src/api/policy.rs) — 1 new test. `parse_listing_preserves_source_order_across_multiple_policies` pins source-order across three deliberately-non-alphabetical policy ids (`zeta`, `alpha`, `mu`) — a HashMap/BTreeMap intermediate-state refactor would silently sort and surface here.

proxy bin tests: **672 → 678**. Cumulative across rounds 1–75: proxy **+521**, cli +17, policy-engine **+85**, shared-types **+13** — **636 new pure-helper unit tests** total. Gate floors unchanged (workspace 45/55, per-crate proxy 45 / shared-types 95 / policy-engine 88 / cli 10). `cargo fmt --all --check`, `cargo clippy --workspace --all-targets --locked -- -D warnings -A clippy::type_complexity -A clippy::too_many_arguments -A clippy::result_large_err`, `cargo test --workspace --locked`, and `RUSTFLAGS="-D warnings" cargo build --workspace --release --locked` all green.

**2026-05-17 (round 76) — 10 more pure-helper tests on operator_auth + blocked_expiry + error_envelope.** Three modules where prior rounds (9+16 + 31 + 44 respectively) had pinned the typical happy path but specific operator-actionable surfaces remained unpinned. On `operator_auth`: the `PREFIX` / `BODY_LEN` / `TOKEN_LEN` triple (the module docstring + CLI `tokens issue` render commit to `pxl_operator_<52 base32>` verbatim — a one-character drift in any of the three constants would silently produce tokens the CLI's own SHA-256 round-trip accepts but no operator-facing documentation describes), the `unauthorized()` `content-type: text/plain` header (operator log parsers split "operator auth lost" 401s from handler-emitted `application/json` 401s on this axis — a refactor to a JSON envelope "for consistency" would silently merge the buckets), the symmetric `require_scope` 403 `content-type: application/json` (the CLI's renderer parses the body and fails loudly on mismatch — a regression would break operator dashboards on every scope-denied request), the `OperatorPrincipal::Clone` Arc-share via `Arc::ptr_eq` on the `scopes` field (every successful auth clones the principal into request extensions — a refactor to `scopes: Vec<String>` would silently allocate one Vec per scope per request without surfacing as a wire-shape change), and the `OperatorAuthState: Clone` trait bound at compile time (axum's `State<T>` extractor requires Clone — a `!Clone` field landing on the state struct would surface at this single trait-bound assertion rather than at hundreds of router-build sites in `server.rs`). On `blocked_expiry`: the `ExpirySweepReport::Clone` independence at NON-zero values (the round-31 pin covered zero only — a refactor to `Arc<AtomicU64>` for "concurrent updates across multiple sweeps" would silently break the snapshot-per-tick contract), the `EscalationSweepReport::Debug` carrying the struct name (operator log aggregators key on `tracing::debug!(?r, ...)` substrings — a manual Debug impl that hid the type name would silently collapse expiry-sweep log lines onto escalation-sweep ones since both reports carry one u64), and the `DEFAULT_TICK_INTERVAL.subsec_nanos() == 0` invariant (a refactor to `Duration::from_secs_f64(...)` would still equal `from_secs(60)` for whole-second values but could silently introduce subsec drift). On `error_envelope`: the `with_extras` last-write-wins semantic on repeated call (adapter sites occasionally build the envelope in two stages — engine default then adapter override — and a refactor that collapsed the slot into a merge/append shape would silently change which value surfaces on the wire), and the array / string `extras` shapes surviving the `Value::is_null` skip predicate (operators occasionally surface arrays directly; a refactor that tightened the predicate to "only objects are kept" would silently drop these alternate shapes). Files:

- [crates/proxy/src/operator_auth.rs](../../crates/proxy/src/operator_auth.rs) — 5 new tests. `token_layout_constants_pinned_per_module_docstring` pins `PREFIX="pxl_operator_"`, `BODY_LEN=52`, `TOKEN_LEN=65` directly. `unauthorized_response_content_type_is_text_plain` pins the `text/plain` header. `require_scope_helper_403_response_is_application_json` pins the symmetric structured-403 content-type. `operator_principal_scopes_cloned_through_arc_share` pins `Arc::ptr_eq` on `OperatorPrincipal::clone`. `operator_auth_state_is_clone_for_axum_state_propagation` pins the Clone trait bound via compile-time `fn require_clone<T: Clone>()`.
- [crates/proxy/src/blocked_expiry.rs](../../crates/proxy/src/blocked_expiry.rs) — 3 new tests. `expiry_sweep_report_clone_at_nonzero_value_yields_independent_copy` pins the snapshot-per-tick contract at a non-zero starting value (round-31 only covered zero). `escalation_sweep_report_debug_renders_struct_name_for_grep` pins the rendered Debug carries the type name so the two sweeps' log lines stay grep-distinguishable. `default_tick_interval_carries_no_subsecond_component` pins `subsec_nanos() == 0` against a `from_secs_f64(...)` refactor.
- [crates/proxy/src/error_envelope.rs](../../crates/proxy/src/error_envelope.rs) — 2 new tests. `with_extras_overrides_prior_value_on_repeated_call` pins last-write-wins on repeated `.with_extras(...)` chaining (no implicit merge). `extras_with_array_or_string_value_serialize_as_non_null_and_present` pins that array + scalar `Value` shapes both survive the `Value::is_null` skip predicate (the predicate is "is null", not "is non-object").

proxy bin tests: **678 → 688**. Cumulative across rounds 1–76: proxy **+531**, cli +17, policy-engine +85, shared-types +13 — **646 new pure-helper unit tests** total. Gate floors unchanged (workspace 45/55, per-crate proxy 45 / shared-types 95 / policy-engine 88 / cli 10). `cargo fmt --all --check`, `cargo clippy --workspace --all-targets --locked -- -D warnings -A clippy::type_complexity -A clippy::too_many_arguments -A clippy::result_large_err`, `cargo test --workspace --locked`, and `RUSTFLAGS="-D warnings" cargo build --workspace --release --locked` all green.

**2026-05-17 (round 77) — 6 more pure-helper tests on config.rs file-layer + ConfigError::BindAddr boundaries.** Prior rounds (20 + 63's setup follow-up) had pinned `check_http_url` and `build`'s validation arms, but several file-layer ingest paths the `Config::load` production entry point relies on remained unpinned: `ConfigError::BindAddr`'s Display (the bootstrap log line operators read to triage a typo'd `PROXILION_BIND_ADDR` — the thiserror `{0:?}: {1}` template renders both the raw input AND the underlying `AddrParseError` Display, and a "simplify the wrapper" refactor that dropped the second field would silently strip the actionable triage half), the `from_file` invalid-bind-addr path's *variant identity* (the parse error must surface `ConfigError::BindAddr` — not collapse to `FileLoad` for "consistency" — because operator dashboards split bootstrap alerts on variant, and a `FileLoad`-wrapped error would silently merge "bad TOML syntax" with "bad address value"), the `eq_ignore_ascii_case("pretty")` branch on `log_format` (only the JSON happy path was indirectly exercised — never `Pretty` directly, and never the case-insensitive contract operators rely on for mixed-case TOML), the else-branch on `log_format` (an unknown value must fall back to JSON *without surfacing an error* — a refactor that tightened this into a closed enum would silently start rejecting deployments with typo'd values), the `.filter(|n| *n > 1)` predicate on `siem_batch_size` (the `1`→`None` collapse is the documented sentinel for "disable batching" — a regression to `>= 1` would spawn the flush loop on every install with the disable marker), and the `.max(1)` clamp on `siem_batch_max_age_secs` (a 0-second flush interval would busy-loop the forwarder at 100% CPU; pin both the clamp at 0→1 and the passthrough at 30→30). Files:

- [crates/proxy/src/config.rs](../../crates/proxy/src/config.rs) — 6 new tests. `config_error_bind_addr_display_carries_raw_input_and_parse_message` pins both halves of the `{0:?}: {1}` template (raw input + parse-error Display substring `"invalid"`). `from_file_invalid_bind_addr_surfaces_bind_addr_error_variant` pins variant identity on the file-layer parse path (must be `BindAddr`, not `FileLoad`). `from_file_log_format_pretty_value_selects_pretty_variant` walks `"pretty"` / `"Pretty"` / `"PRETTY"` to pin the `eq_ignore_ascii_case` case-insensitive contract. `from_file_log_format_unknown_value_falls_back_to_json_no_error` pins both halves of the else-branch (no parse error + variant is JSON). `from_file_siem_batch_size_one_collapses_to_none_via_gt_one_filter` walks `1→None` (collapsed by `> 1` filter) and `5→Some(5)` (kept). `from_file_siem_batch_max_age_secs_zero_clamps_to_one` walks `0→1` (clamped) and `30→30` / `1→1` (passthrough) — a `.max(0)` regression or a dropped clamp would surface here.

proxy bin tests: **688 → 694**. Cumulative across rounds 1–77: proxy **+537**, cli +17, policy-engine +85, shared-types +13 — **652 new pure-helper unit tests** total. Gate floors unchanged (workspace 45/55, per-crate proxy 45 / shared-types 95 / policy-engine 88 / cli 10). `cargo fmt --all --check`, `cargo clippy --workspace --all-targets --locked -- -D warnings -A clippy::type_complexity -A clippy::too_many_arguments -A clippy::result_large_err`, `cargo test --workspace --locked`, and `RUSTFLAGS="-D warnings" cargo build --workspace --release --locked` all green.

**2026-05-17 (round 78) — 6 more pure-helper tests on api/mod.rs ApiError::Verifier arm + ApiError Display + PcaView None-passthrough.** Prior rounds (14 + 44's follow-up) had pinned the `NotFound` + `Db` arms of `ApiError::into_response` and the `PcaView` Some-wire-shape, but the `Verifier` arm — the third of three variants — was the only one never directly exercised, and several operator-actionable surfaces remained unpinned: the `Verifier` 500 status + `code="verifier_error"` envelope (operator dashboards split chain-walker faults from generic DB faults on this axis; a refactor collapsing `verifier_error` into `internal_error` "for consistency" would silently merge the two Grafana buckets), the `Verifier` body's `fix` + `docs` link halves (the `broken_at` field substring + `pic/verify` docs URL are the operator's first 30 seconds during a chain-walk fault), the `.with_detail(e.to_string())` carry-through of the inner `VerifierError` Display (the actionable triage half — which pca id failed, why — a refactor that dropped detail "to avoid leaking internals" would strip this surface), the `ApiError::NotFound` Display string `"not found"` (operator log filters and `tracing::error!(error = %e, ...)` shape key on this exact substring), the `ApiError::Verifier` Display's `"verifier: "` prefix + inner Display passthrough (log aggregators split chain-walker faults on the prefix; a refactor to `"chain: {0}"` would silently break every log filter keyed on `"verifier:"`), and the `PcaView::predecessor_id` None-arm wire shape (the dashboard's chain-walker keys on `predecessor_id === null` — key-presence with null value, NOT key-absence — to decide whether to render the "← prev" link; a `#[serde(skip_serializing_if = "Option::is_none")]` regression would silently break root-hop detection). Files:

- [crates/proxy/src/api/mod.rs](../../crates/proxy/src/api/mod.rs) — 6 new tests. `api_error_verifier_arm_maps_to_500_with_verifier_error_code` pins the 500 status on the third (previously-untested) variant. `api_error_verifier_body_carries_fix_and_pic_verify_docs_link` pins the `code="verifier_error"` envelope + the `broken_at` substring in `fix` + the `pic/verify` substring in `docs`. `api_error_verifier_body_carries_detail_from_inner_error` pins that `.with_detail(e.to_string())` surfaces the inner `VerifierError::Missing` Display substring `"not found in cache"`. `api_error_display_renders_not_found_string_for_grep` pins the exact `thiserror` Display token `"not found"` (no variant-name prefix). `api_error_display_renders_verifier_prefix_with_inner_message` pins both halves of `#[error("verifier: {0}")]` — the literal `"verifier: "` prefix AND the inner-Display passthrough after the colon. `pca_view_predecessor_id_serializes_null_when_none_for_root_hop` pins that the `Option<Uuid>` field serializes as JSON `null` (key-present + value-null) for hop-0 chain roots — symmetric counterpart to the `Some` case already covered in `pca_view_serializes_with_stable_field_names`.

proxy bin tests: **694 → 700**. Cumulative across rounds 1–78: proxy **+543**, cli +17, policy-engine +85, shared-types +13 — **658 new pure-helper unit tests** total. Gate floors unchanged (workspace 45/55, per-crate proxy 45 / shared-types 95 / policy-engine 88 / cli 10). `cargo fmt --all --check`, `cargo clippy --workspace --all-targets --locked -- -D warnings -A clippy::type_complexity -A clippy::too_many_arguments -A clippy::result_large_err`, `cargo test --workspace --locked`, and `RUSTFLAGS="-D warnings" cargo build --workspace --release --locked` all green.

**2026-05-17 (round 79) — 6 more pure-helper tests on oauth/error Internal+Upstream+BadRequest body boundaries + policy-engine decision Pattern Debug + Decision serde required-field/u32-domain pins.** Two modules where prior rounds (12+46+50 + 7-era decision baseline) had pinned the typical happy path but specific operator-visible Display/body shapes and the deserialize-side contract remained unpinned. On `oauth/error`: the `Internal(String)` variant's `#[error("internal error")]` masks the inner String — a refactor to `#[error("internal error: {0}")]` would silently leak adapter-built `format!("token={token}")` payloads into log aggregators that summarize the textual Display rather than the structured `?self` field, and the no-leak contract is what makes `Internal(...)` safe to use freely from adapter sites; the `Upstream(reqwest::Error)` `body()` arm deliberately does NOT call `.with_detail(...)` (the inner reqwest error carries the full URL + any embedded path/query secrets — round 78's coverage routed through `Internal` which shares the match arm with `Db`/`Crypto`, but the `Upstream` arm is its own arm and a future arm-split refactor wouldn't surface in the existing test); the `BadRequest` fix string is the docs-page anchor (`response_type=code` / `S256` / `scope` / `redirect_uri` are the four substrings the OAuth-intercept docs link, and a "soften the message" refactor would orphan every cross-reference). On `policy-engine/decision`: `Pattern::Regex(regex::Regex)`'s Debug source visibility is load-bearing for read-filter operator triage (`?pattern` in `tracing::warn!` surfaces the literal pattern source; a manual Debug impl that elided it for "redact policy text" would force operators to cross-reference the YAML by id rather than grep the log line); `Decision::Block`'s `override_allowed: bool` has NO `#[serde(default)]` (a regression that added one would silently flip every legacy `block:` policy without explicit override language to `false` and orphan the approver UI for that policy); `Decision::RateLimit`'s `burst: u32` / `per_seconds: u32` reject negative values AND u32-overflow values on deserialize (a refactor to `i32` or `u64` for "SQL alignment" would silently change the policy-author surface — negatives would two's-complement-wrap or u32::MAX+1 would pass through, both regressions catchable here). Files:

- [crates/proxy/src/oauth/error.rs](../../crates/proxy/src/oauth/error.rs) — 3 new tests. `internal_variant_display_does_not_carry_inner_string` pins the exact `"internal error"` Display string against `Internal("integer overflow at line 42")` (no inner-String substrings allowed). `upstream_body_omits_detail_to_avoid_leaking_reqwest_error_url` pins the Upstream arm directly via a real reqwest::Error built against an RFC 5737 black-hole URL (`http://192.0.2.1:1/secret-path?token=hunter2`) — asserts `body.detail.is_none()` AND that fix+docs survive. `bad_request_body_fix_mentions_required_oauth_parameters` pins all four operator-docs substrings (`response_type=code`, `S256`, `scope`, `redirect_uri`) + that the docs URL points at `oauth/intercept` (not the generic `troubleshooting` page — BadRequest is a misconfigured agent, not a proxy bug).
- [crates/policy-engine/src/decision.rs](../../crates/policy-engine/src/decision.rs) — 3 new tests. `pattern_regex_debug_includes_regex_source_for_operator_grep` pins both `Literal` (needle visible) and `Regex` (source visible, via the inner `regex::Regex` Debug). `decision_block_requires_explicit_override_allowed_field_on_deserialize` walks `override_allowed: true` / `override_allowed: false` (both round-trip) and missing-field (must error with `"override_allowed"` substring in the rendered error). `decision_rate_limit_rejects_negative_or_overflow_values_on_deserialize` walks negative burst, negative per_seconds, u32::MAX boundary (must round-trip), and u32::MAX+1 (must reject) — pinning both halves of the u32 domain.

proxy bin tests: **700 → 703**. policy-engine lib tests: **143 → 146**. Cumulative across rounds 1–79: proxy **+546**, cli +17, policy-engine **+88**, shared-types +13 — **664 new pure-helper unit tests** total. Gate floors unchanged (workspace 45/55, per-crate proxy 45 / shared-types 95 / policy-engine 88 / cli 10). `cargo fmt --all --check`, `cargo clippy --workspace --all-targets --locked -- -D warnings -A clippy::type_complexity -A clippy::too_many_arguments -A clippy::result_large_err`, `cargo test --workspace --locked`, and `RUSTFLAGS="-D warnings" cargo build --workspace --release --locked` all green.

**2026-05-17 (round 80) — 6 more pure-helper tests on notifier/webhook secret hex-case + empty-body sign + with_burst replace-on-chain + policy-engine loader set_yaml content round-trip + mtime token format + PolicyLoadError inner-message passthrough.** Two modules where prior rounds (15+50+53 + 5's loader baseline) had pinned much of the surface but specific operator-actionable boundaries remained unpinned. On `notifier/webhook`: `WebhookSecret::from_hex` accepts uppercase / mixed-case hex via `u8::from_str_radix` (a tightening to lowercase-only for "canonical form" would silently reject operators pasting hex from Vault UIs which often render uppercase — the existing tests pinned only lowercase happy paths); `WebhookSecret::sign` on an EMPTY body must still emit the format-invariant `sha256=<64-lower-hex>` header (a refactor that early-returned `String::new()` "for performance" on empty input would silently produce an unverifiable empty signature header — receivers would 401 every empty-body event, and the existing tests only exercised non-empty bodies); `WebhookNotifier::with_burst` on a chained second call must REPLACE the prior suppressor (last-write-wins), not push onto a hypothetical Vec or panic — a refactor that composed suppressors via append would double the gating logic and silently change runtime semantics, and the existing `with_burst` test only pinned the first call. On `policy-engine/loader`: `StaticPolicyLoader::set_yaml` followed by `load()` must surface the NEW yaml bytes (the existing tests pinned the version-token bump but never directly asserted the `yaml` field changed — a `Mutex<String>` → `OnceCell<String>` refactor would freeze the YAML at the initial value while still bumping the AtomicU64 revision, silently masking every operator policy edit); `FilePolicyLoader`'s mtime token format is `mtime:<unsigned-nanos-since-epoch>` (the existing test pinned only the `starts_with("mtime:")` prefix — a refactor that swapped to RFC 3339 timestamps or to a hex hash would silently break log filters keyed on the nanos digits); `PolicyLoadError::Io` and `PolicyLoadError::Backend` Display must pass the inner error message through verbatim (the existing variant-renders-each-variant test used fixed operator-style strings; pin the byte-identical inner-message passthrough so a "consistency across platforms" refactor that truncated or normalized the inner string would strip the actionable triage half operators rely on during permission denials). Files:

- [crates/proxy/src/notifier/webhook.rs](../../crates/proxy/src/notifier/webhook.rs) — 3 new tests. `secret_from_hex_accepts_uppercase_and_mixed_case_hex_letters` walks lowercase + uppercase + mixed case and asserts byte-identical signatures via the only externally-observable identity (signing a fixed body) — the inner `Vec<u8>` field is private. `secret_sign_on_empty_body_still_returns_sha256_prefix_and_64_hex_chars` pins all three invariants on the empty body: `sha256=` prefix, exactly 64 lowercase hex chars, byte-stable across two calls, AND distinct from the same key signing a single-space body (sanity that empty isn't a degenerate fixed-output shortcut). `webhook_notifier_with_burst_replaces_prior_suppressor_on_chained_call` chains two `with_burst` calls and asserts `burst().is_some()` survives — a refactor to Vec-of-suppressors would compile (the type still satisfies `Option<BurstSuppressor>`) but the second call would either panic or silently composite, both of which surface here.
- [crates/policy-engine/src/loader.rs](../../crates/policy-engine/src/loader.rs) — 3 new tests. `static_loader_load_returns_new_yaml_bytes_after_set_yaml` walks three distinct YAML payloads through set_yaml + load and asserts each post-set load surfaces the new `id:` substring, the new content `drive.files.get`, AND that the prior id substrings are GONE (no append-only buffer regression). `file_loader_mtime_token_format_is_mtime_colon_nanos_for_grep` writes a tempfile, asserts `mtime:` prefix + all-digit non-empty nanos suffix, and pins that `version_token_sync` produces a byte-identical token to the async `load().version` (defense in depth across the two helpers). `policy_load_error_io_display_passes_inner_error_message_through_unchanged` pins both the Io variant (with `"permission denied (os error 13)"`-shape inner string) and the symmetric Backend variant (`"pg pool exhausted"`) — both must `starts_with` their prefix AND `ends_with` the inner string byte-for-byte.

proxy bin tests: **703 → 706**. policy-engine lib tests: **146 → 149**. Cumulative across rounds 1–80: proxy **+549**, cli +17, policy-engine **+91**, shared-types +13 — **670 new pure-helper unit tests** total. Gate floors unchanged (workspace 45/55, per-crate proxy 45 / shared-types 95 / policy-engine 88 / cli 10). `cargo fmt --all --check`, `cargo clippy --workspace --all-targets --locked -- -D warnings -A clippy::type_complexity -A clippy::too_many_arguments -A clippy::result_large_err`, `cargo test --workspace --locked`, and `RUSTFLAGS="-D warnings" cargo build --workspace --release --locked` all green.

**2026-05-18 (round 81) — 6 more pure-helper tests on policy-engine ops + trace trait-derive pins.** Two policy-engine modules where prior rounds (ops via the OpsAtom/OpsExpression baseline + rounds 7/52 indirect; trace via round-2's policy_trace baseline) had pinned the typical happy path but specific Default-construction, zero-input-template, and Copy/Eq trait-derive contracts the engine's hot path depends on remained unpinned. On `ops`: `OpsAtom` derives both Serialize and Deserialize but the existing tests only exercise the parse/to_canonical pair — a refactor that camelCased the field names via `#[serde(rename_all = "camelCase")]` would silently break the upstream `provenance-plane` CBOR consumer that JSON-decodes via the literal field names, and the existing tests would still pass; `OpsExpression::default()` produces an empty `required` Vec, but no test directly asserted that the Default value is *trivially satisfied by ANY leaf set* (including empty) — the property every Layer-A no-required-ops policy depends on, where a refactor adding a sentinel "deny" atom to the Default value would mass-deny every block-only policy on next reload; `OpsExpression::resolve(&[], &ctx)` with zero templates is the load-bearing shape for Layer-B-only policies (the most common policy author shape — a `block` rule that doesn't care about chain authority), and a "must declare at least one required_op" guard regression would silently reject every block-only policy. On `trace`: `PolicyEvalMode::Comprehensive` was never directly compared against `FailFast` (the existing test pinned only the Default value) — a refactor that collapsed the variants would silently break the dashboard's "explain this denial" replay; `PolicyEvalMode` and `PolicyLayer` both derive `Copy`, which is load-bearing for the `set_layer` helper's `iter_mut().find(|l| l.layer == layer)` closure in [adapters/policy_trace.rs](../../crates/proxy/src/adapters/policy_trace.rs) — a refactor that dropped `Copy` would surface as borrow-check errors at hundreds of engine call sites rather than at this single trait-bound assertion, and the three-variant distinct-equality walk catches an alias regression (LayerA/LayerB merge mistake). Files:

- [crates/policy-engine/src/ops.rs](../../crates/policy-engine/src/ops.rs) — 3 new tests. `ops_atom_serde_json_round_trip_carries_three_named_fields_no_extras` pins the wire shape directly via `serde_json::to_string` round-trip + an `as_object().unwrap().len() == 3` exact-field-count assertion (catches a derive that added an extra field via `#[serde(skip_serializing_if = ...)]` regression). `ops_expression_default_is_empty_required_vec_trivially_satisfied` walks `is_satisfied_by` against three leaf-set shapes: empty (no leaf ops), single-leaf, and multi-leaf with distinct schemes — all must pass against the Default. `ops_expression_resolve_with_empty_templates_list_returns_empty_required` pins both the construction path (zero templates → empty required Vec) AND the composed contract (the resulting expression is trivially satisfied — composes with the Default test to cover both construction paths).
- [crates/policy-engine/src/trace.rs](../../crates/policy-engine/src/trace.rs) — 3 new tests. `policy_eval_mode_comprehensive_variant_compares_distinct_from_fail_fast` walks both reflexive equalities (FailFast == FailFast, Comprehensive == Comprehensive) AND the cross-variant inequality. `policy_eval_mode_is_copy_at_use_sites` pins via the canonical `fn require_copy<T: Copy>()` trait-bound check (zero-cost — no instantiation) plus a use-by-value sanity (`let a = ...; let b = a; assert_eq!(a, b)` would fail-compile on move). `policy_layer_is_copy_at_use_sites_and_distinct_across_three_variants` pins the symmetric Copy bound on `PolicyLayer` AND the three-variant pairwise-distinct equality walk (LayerA != LayerB != ReadFilter, plus reflexive for each).

proxy bin tests: **706 → 706** (no changes in proxy crate). policy-engine lib tests: **149 → 155**. Cumulative across rounds 1–81: proxy +549, cli +17, policy-engine **+97**, shared-types +13 — **676 new pure-helper unit tests** total. Gate floors unchanged (workspace 45/55, per-crate proxy 45 / shared-types 95 / policy-engine 88 / cli 10). `cargo fmt --all --check`, `cargo clippy --workspace --all-targets --locked -- -D warnings -A clippy::type_complexity -A clippy::too_many_arguments -A clippy::result_large_err`, `cargo test --workspace --locked`, and `RUSTFLAGS="-D warnings" cargo build --workspace --release --locked` all green.

**2026-05-18 (round 82) — 6 more pure-helper tests on auth_middleware AuthFail Display-prefix pins + api/blocked ApiError Display-prefix pins.** Two modules where prior rounds (16+52+76 for auth_middleware; 5+6+60+71 for api/blocked) had pinned the Display *inner-string* passthrough (e.g. `assert!(s.contains("network timeout"))`) but the *prefix half* of every `#[error("<prefix>: {0}")]` annotation — the operator-facing log-filter substring — remained unpinned across both error enums. On `auth_middleware::AuthFail`: the three `<prefix>: {0}` variants (`Refresh` → `"google token refresh failed: "`, `CatKey` → `"CAT key fetch failed: "`, `Other` → `"internal: "`) are what Grafana / Loki filters key on to bucket bearer-refresh failures separately from CAT-key fetch failures separately from catch-all internal errors — a refactor that softened any prefix (the natural "tidy up error messages" mistake) would silently collapse three distinct dashboards onto one. On `api/blocked::ApiError`: the `NotFound` variant's exact `"not found"` Display string (no variant-name prefix, no trailing punctuation — operator log filters key on the exact substring as the "row already resolved or expired" signal); the `PicRefused` variant's `"PIC invariant refused: "` prefix (spec.md §6.6 escape-hatch surface; log aggregators bucket "operator override refused by Trust Plane" on this prefix separately from policy decisions which never use the "invariant" qualifier); the `Internal` variant's `"internal: "` prefix (catch-all for "this shouldn't happen" code paths — must NOT collapse to `Db`'s transparent shape, since Grafana distinguishes "proxy bug" from "postgres health issue" on prefix presence). Files:

- [crates/proxy/src/auth_middleware.rs](../../crates/proxy/src/auth_middleware.rs) — 3 new tests. `auth_fail_refresh_display_carries_google_token_refresh_failed_prefix` pins the full `"google token refresh failed: network timeout"` Display via `assert_eq!`, not just `contains` (the existing test only pinned inner substring). `auth_fail_cat_key_display_carries_cat_key_fetch_failed_prefix` walks three distinct inner messages (`"trust plane 503"`, `"expected 32 bytes"`, `"transport: connection refused"`) so a refactor that hardcoded the prefix into a smaller uniform wrapper surfaces on at least one of the three inputs. `auth_fail_other_display_carries_internal_prefix_with_inner_message` pins the catch-all `"internal: {0}"` shape — the only variant prefix that distinguishes "proxy bug, file an issue" from the more-specific external-system variants.
- [crates/proxy/src/api/blocked.rs](../../crates/proxy/src/api/blocked.rs) — 3 new tests. `api_error_not_found_display_is_exact_not_found_string` pins the exact two-word `"not found"` Display (no variant-name prefix) — distinct from the wire-shape `error` field which IS `"blocked action not found"` (the operator-UI title); the Display is the log substring, they're intentionally distinct. `api_error_pic_refused_display_carries_pic_invariant_refused_prefix` pins the full `"PIC invariant refused: ops not subset of predecessor: missing [drive:write:bob/*]"` shape via `assert_eq!` against the canonical Trust-Plane-emitted missing-ops syntax. `api_error_internal_display_carries_internal_prefix_with_inner_string` pins the `"internal: unexpected None on session lookup"` shape — the prefix is what keeps Internal from collapsing to Db's transparent shape on operator dashboards.

proxy bin tests: **706 → 712**. policy-engine lib tests unchanged. Cumulative across rounds 1–82: proxy **+555**, cli +17, policy-engine +97, shared-types +13 — **682 new pure-helper unit tests** total. Gate floors unchanged (workspace 45/55, per-crate proxy 45 / shared-types 95 / policy-engine 88 / cli 10). `cargo fmt --all --check`, `cargo clippy --workspace --all-targets --locked -- -D warnings -A clippy::type_complexity -A clippy::too_many_arguments -A clippy::result_large_err`, `cargo test --workspace --locked`, and `RUSTFLAGS="-D warnings" cargo build --workspace --release --locked` all green.

**2026-05-18 (round 83) — 6 more pure-helper tests on adapters/error AppError Display-prefix pins (the third and final error enum after rounds 82's ApiError + AuthFail).** [crates/proxy/src/adapters/error.rs](../../crates/proxy/src/adapters/error.rs) had 14 tests covering `.code()` + `.status()` + `.body()` for every variant + `into_response()` + `upstream_error_kind`, but ZERO of them pinned the `Display` half — every `#[error("...")]` annotation was untested. Operator log filters key on the Display *prefix* (Grafana / Loki splits buckets on substrings like `"Trust Plane refused PCA:"` vs `"policy engine:"` vs `"policy ops template:"`); a refactor that "harmonized" prefixes for consistency would silently merge dashboards onto each other. Six variant prefixes pinned: `PolicyBlocked` (uses `{policy_id:?}` Debug-form which renders Option as `Some("p1")` / `None` — operators grep both shapes; a switch to `{policy_id}` would either panic or require unwrap_or which silently merges the two buckets), `RequireConfirmation` (`"requires user confirmation: "`), `PicInvariantViolation` (`"Trust Plane refused PCA: "` — distinct from `OAuthError::PicInvariant`'s prefix so adapter-side monotonicity faults don't merge with OAuth-side ones on dashboards), `Policy` (`"policy engine: "` for engine-evaluation errors), `OpsTemplate` (`"policy ops template: "` for operator-authored template faults — distinct from `Policy` to let runbooks distinguish "your YAML is malformed" from "the engine rejected your structure" without re-walking the trace), `Internal` (`"internal error: <inner>"` — explicitly INCLUDES the inner String, asymmetric to `OAuthError::Internal` which masks it; the asymmetry is intentional since AppError surfaces to authenticated agents and the structured 500 response collapses to a no-detail title via `body()`). Files:

- [crates/proxy/src/adapters/error.rs](../../crates/proxy/src/adapters/error.rs) — 6 new tests. `app_error_policy_blocked_display_carries_policy_id_debug_form_and_reason` walks both `Some(_)` and `None` policy_id shapes and pins exact `Some("p1")` / `None` Debug-form rendering. `app_error_require_confirmation_display_carries_prefix_with_inner_reason` pins the full Display via `assert_eq!`. `app_error_pic_invariant_violation_display_carries_trust_plane_prefix` pins against the canonical missing-atoms syntax operators see in upstream Trust Plane refusal bodies. `app_error_policy_display_carries_policy_engine_prefix_with_inner_error_message` constructs a real `rego::Error::BadDecision`, pins the `"policy engine: "` prefix AND the inner `rego::Error` Display passthrough (`"invalid decision shape:"`) so cross-module drift surfaces. `app_error_ops_template_display_carries_policy_ops_template_prefix` constructs a real `OpsParseError::UnknownVar`, pins the symmetric inner-Display passthrough on the sibling arm. `app_error_internal_display_carries_internal_error_prefix_with_inner_string` pins the full `"internal error: <inner>"` shape and explicitly documents the asymmetry with `OAuthError::Internal`.

proxy bin tests: **712 → 718**. policy-engine lib tests unchanged. Cumulative across rounds 1–83: proxy **+561**, cli +17, policy-engine +97, shared-types +13 — **688 new pure-helper unit tests** total.

**2026-05-18 (round 84) — 6 more pure-helper tests on pic/verifier VerifierError Display-prefix + named-field pins (the chain-walker error enum, completing the round-79/82/83 Display sweep across error.rs / api_error / oauth_error / app_error / auth_fail / api_blocked / app_error and now verifier).** [crates/proxy/src/pic/verifier.rs](../../crates/proxy/src/pic/verifier.rs) already had `verifier_error_display_carries_named_field_values` pinning two of the eight variants' inner-field substring rendering, but six of the eight `#[error("...")]` annotations — every prefix half plus the full single-line Display shape of the multi-line `ContinuityBroken` attribute — were untested. Operator chain-walker dashboards split buckets on these exact substrings: `"pca <uuid> not found"` vs `"CAT signature failed to verify on pca <uuid>"` vs `"continuity broken between pca <child> and predecessor <parent>: provenance.cat_sig mismatch"` vs `"p_0 changed across hop (<child> != <parent>)"` vs `"decoding signed PCA bytes: <inner>"` vs `"CAT key fetch: <inner>"`. A refactor that "harmonized" any prefix (the natural "tidy up error messages" mistake — collapsing `"CAT signature failed"` and `"CAT key fetch"` into a shared `"CAT: ..."` shape, or softening `"continuity broken"` to `"chain break"`) would silently merge dashboards onto each other and force operators to cross-reference the structured trace just to recover what variant fired. Six variants pinned with exact `assert_eq!` shapes: `Missing(Uuid)` (uuid renders via lowercase-hyphenated Display, not Debug-braced form — a `{0:?}` regression would silently wrap it in `Uuid("...")` and break log filters keyed on the bare uuid substring); `BadCatSignature(Uuid)` (symmetric to Missing on the uuid-rendering contract but distinct prefix); `ContinuityBroken { child, parent }` (the only multi-line `thiserror` attribute in the enum — pin that Display collapses to a single line with both uuids AND the `": provenance.cat_sig mismatch"` suffix preserved verbatim); `P0Mismatch { child_p0, parent_p0 }` (parenthesized inequality with child-first / parent-second ordering matching `HopOrder`'s parameter order for operator muscle memory); `Decode(String)` (`"decoding signed PCA bytes: "` prefix + inner CBOR-decoder triage message passthrough); `CatKey(String)` (`"CAT key fetch: "` prefix — distinct from `BadCatSignature`'s `"CAT signature failed"` so operators bucket "key unavailable" separately from "key disagreed"). Files:

- [crates/proxy/src/pic/verifier.rs](../../crates/proxy/src/pic/verifier.rs) — 6 new tests. `verifier_error_missing_display_carries_pca_not_found_prefix_and_uuid`, `verifier_error_bad_cat_signature_display_carries_pca_uuid_and_signature_prefix`, `verifier_error_continuity_broken_display_renders_both_named_uuids_and_full_suffix`, `verifier_error_p0_mismatch_display_carries_full_shape_with_both_principals`, `verifier_error_decode_display_carries_decoding_signed_pca_prefix_with_inner_string`, `verifier_error_cat_key_display_carries_cat_key_fetch_prefix_with_inner_string` — each pins the full Display via `assert_eq!` against a known constructor input so any prefix-softening or named-field-ordering refactor surfaces immediately.

proxy bin tests: **718 → 724**. policy-engine lib tests unchanged. Cumulative across rounds 1–84: proxy **+567**, cli +17, policy-engine +97, shared-types +13 — **694 new pure-helper unit tests** total. Gate floors unchanged (workspace 45/55, per-crate proxy 45 / shared-types 95 / policy-engine 88 / cli 10). `cargo fmt --all --check`, `cargo clippy --workspace --all-targets --locked -- -D warnings -A clippy::type_complexity -A clippy::too_many_arguments -A clippy::result_large_err`, `cargo test --workspace --locked`, and `RUSTFLAGS="-D warnings" cargo build --workspace --release --locked` all green.
**2026-05-18 (round 85) — 6 more pure-helper tests on policy-engine match_expr MatchError Display-prefix + named-field + Error::source chain pins (the match-expression evaluator's error enum, sibling to round-84's verifier sweep).** [crates/policy-engine/src/match_expr.rs](../../crates/policy-engine/src/match_expr.rs) had 30+ tests pinning `evaluate()` happy paths plus extensive `matches!(err, MatchError::BadShape { ref op, .. } if op == ...)` shape assertions, but ZERO directly pinned the `#[error("...")]` Display output for any of the three variants, AND the `Error::source()` chain that `Template(#[from] OpsParseError)` is supposed to wire was never directly asserted. The Display strings are what the dashboard's "policy authoring errors" panel renders verbatim to the operator (the engine itself never surfaces these on the public wire — they flow up through `AppError::Policy` / `AppError::OpsTemplate` to per-policy debug tooling), and the `source()` chain is what makes anyhow-style walkers in adapter code able to recover the inner `OpsParseError` without parsing the Display string. Six surfaces pinned: `UnsupportedOp(String)` — the BACKTICKED operator name (` `{0}` ` syntax) is load-bearing for operator log filters to distinguish "operator typo" from `BadShape`-style faults, and a refactor that dropped the backticks "for consistency with the other variants" would silently merge two distinct Loki buckets; `BadShape { op, expected, got }` named-field rendering order (op/expected/got matches the struct field declaration order — pin all three values distinct so a `expected/got/op` flip "for grammar" surfaces); `BadShape.expected: &'static str` type contract across four typical descriptors (a refactor to `String` would surface as compile errors at construction sites — `&'static str` keeps the variant cheap-Clone for retry tracing); `Template(#[from] OpsParseError)` distinct prefix `"template error: "` vs the sibling adapter-layer `AppError::OpsTemplate` prefix `"policy ops template: "` (the two prefixes are intentionally different layers — engine-side inline match-expr template faults vs adapter-side required_ops template faults — and a "harmonize" refactor would silently merge the two buckets); `BadShape.op: String` accepts dotted field paths (`"user.contact.email"`) — a refactor that canonicalized to the last segment for "display brevity" would silently drop the path context operators rely on to locate the offending clause; `std::error::Error::source()` chain — `Template` returns Some via `#[from]`, leaf variants `UnsupportedOp`/`BadShape` return None (a hand-rolled `From` impl that omitted `source()` wiring would surface here). Files:

- [crates/policy-engine/src/match_expr.rs](../../crates/policy-engine/src/match_expr.rs) — 6 new tests. `match_error_unsupported_op_display_carries_backticked_operator`, `match_error_bad_shape_display_renders_all_three_named_fields_in_order`, `match_error_bad_shape_display_static_str_expected_field_supports_typical_descriptors` (walks four descriptors and pins prefix-suffix bracketing), `match_error_template_display_carries_template_error_prefix_with_inner_passthrough` (cross-module prefix-distinction pin against `AppError::OpsTemplate`), `match_error_bad_shape_display_op_field_supports_dotted_field_paths`, `match_error_implements_std_error_trait_for_source_chain_walking` (asserts `source()` returns None for leaf arms and Some for the `Template` arm with the inner Display passing through).

proxy bin tests unchanged at 724. policy-engine lib tests: **155 → 161**. Cumulative across rounds 1–85: proxy +567, cli +17, policy-engine **+103**, shared-types +13 — **700 new pure-helper unit tests** total. Gate floors unchanged (workspace 45/55, per-crate proxy 45 / shared-types 95 / policy-engine 88 / cli 10). `cargo fmt --all --check`, `cargo clippy --workspace --all-targets --locked -- -D warnings -A clippy::type_complexity -A clippy::too_many_arguments -A clippy::result_large_err`, `cargo test --workspace --locked`, and `RUSTFLAGS="-D warnings" cargo build --workspace --release --locked` all green.

**2026-05-18 (round 86) — 6 more pure-helper tests on policy-engine rego::Error Display-prefix + named-field + transparent-arm passthrough pins (the engine entrypoint's error enum, sibling to round-79/85's decision/match_expr sweep).** [crates/policy-engine/src/rego.rs](../../crates/policy-engine/src/rego.rs) had 25+ tests pinning `Engine::evaluate()` happy paths plus `matches!(err, Error::BadDecision(_))` / `Error::BadRegex { .. }` shape assertions, but ZERO directly pinned the `#[error("...")]` Display output for the six variants. The Display strings flow through `AppError::Policy`'s `"policy engine: "` adapter-side wrapper (round 83 pinned that), so the composed log line operators read is `"policy engine: invalid decision shape: <inner>"` (two-prefix stack giving a runbook the engine-vs-decision-shape axis split). Round 85 pinned the sibling `match_expr::MatchError` Display surfaces; this round completes the two-module sweep across the engine's primary error types. Six surfaces pinned: `Yaml(#[from] serde_yaml::Error)` — `"YAML parse error: "` prefix + inner serde_yaml position-info passthrough (constructed against a real failing serde_yaml parse and checked via suffix-after-prefix byte-identity to the inner Display, so operator tools that split on `": "` to recover line+column info don't silently break); `BadDecision(String)` — `"invalid decision shape: "` prefix (distinct from a future generic `"bad decision"` softening — runbook substring split on `"invalid decision shape"` keys on the structure-validation specificity); `BadReadFilter(String)` — `"invalid read_filter: "` prefix with the UNDERSCORE-form `read_filter` matching the YAML key authors wrote (a refactor to space-form `"read filter"` for "human-readability" would silently break the operator's cross-reference into the YAML); `BadRegex { pat, source }` — BACKTICKED `{pat}` field is load-bearing for delimiting pattern boundaries when the pattern itself contains a colon (the field separator), AND the `{source}` substitution carries the regex compiler's actionable triage ("unbalanced parenthesis" / "missing character class"); `Match(#[from] MatchError)` — `#[error(transparent)]` forwards Display BYTE-IDENTICALLY to the inner (cross-module drift surfaces via the explicit `assert_eq!` against round-85's pinned `"unsupported operator `weird_op`"` shape); `Ops(#[from] OpsParseError)` — symmetric `transparent` arm where `Error::source()` is forwarded to the inner leaf (returns None since `OpsParseError::UnknownVar` is a leaf — pin that contract so a refactor that swapped `transparent` for an explicit prefix wrapper would silently start exposing the leaf via `source()` and break anyhow-style walkers in adapter code that expect a fresh `source()` link only at the prefixed arms). Files:

- [crates/policy-engine/src/rego.rs](../../crates/policy-engine/src/rego.rs) — 6 new tests. `rego_error_yaml_display_carries_yaml_parse_error_prefix_with_inner_serde_message` (byte-identity suffix check against the inner serde_yaml Display), `rego_error_bad_decision_display_carries_invalid_decision_shape_prefix`, `rego_error_bad_read_filter_display_carries_invalid_read_filter_prefix`, `rego_error_bad_regex_display_renders_pat_in_backticks_and_full_source_message` (constructs a real `regex::Regex::new("(unbalanced")` failure and pins both the backticked-pat prefix AND the inner regex::Error Display passthrough), `rego_error_match_arm_transparent_display_strips_no_prefix` (cross-module drift surface against round 85), `rego_error_ops_arm_transparent_display_forwards_inner_and_source_chain` (pins `transparent` Display passthrough AND the `source()` is None contract for the leaf-wrapping arm).

proxy bin tests unchanged at 724. policy-engine lib tests: **161 → 167**. Cumulative across rounds 1–86: proxy +567, cli +17, policy-engine **+109**, shared-types +13 — **706 new pure-helper unit tests** total. Gate floors unchanged (workspace 45/55, per-crate proxy 45 / shared-types 95 / policy-engine 88 / cli 10). `cargo fmt --all --check`, `cargo clippy --workspace --all-targets --locked -- -D warnings -A clippy::type_complexity -A clippy::too_many_arguments -A clippy::result_large_err`, `cargo test --workspace --locked`, and `RUSTFLAGS="-D warnings" cargo build --workspace --release --locked` all green.

**2026-05-18 (round 87) — 6 more pure-helper tests on adapters/policy_trace mutation primitives + summary fallback + asymmetric helper contracts.** [crates/proxy/src/adapters/policy_trace.rs](../../crates/proxy/src/adapters/policy_trace.rs) already had 10 tests covering the happy paths (replace-passed-entry, append-when-absent, the per-helper `passed`/`failed` arm choice, the comma-join in `summary`), but six operator-actionable surfaces remained unpinned: the `.unwrap_or("unknown")` fallback in `summary()` when a `LayerOutcome` carries `passed: false` AND `error_code: None` (the "trace was hand-mutated without a code" tripwire — operators bucket on `=unknown` to distinguish from any future named code, and a refactor that collapsed the fallback to an empty string would silently produce `layer_b=` and break log-derived metrics); the `mark_layer_a_failed` helper's hardcoded `None` for `matched_rule_id` (the dashboard's "PCA refused" alert keys on `layer_a` rows with null rule id as the discriminant against any future Layer A variant that carries a policy id, and a refactor that "for consistency with mark_read_filter" added a policy_id parameter would silently start populating the field); the `mark_read_filter(blocked=false)` arm's four-axis shape (`passed=true` + `error_code=None` + `matched_rule_id=Some` + `detail=Some` — the ONLY place in the adapter where a passed layer surfaces a detail, since the public `LayerOutcome::passed()` constructor drops detail; a refactor that routed through `LayerOutcome::passed()` for "consistency" would silently strip the per-event quarantine count from the structured log); the `set_layer` single-replace semantic when duplicates exist (`iter_mut().find(...)` returns first match; a refactor to `filter().for_each()` would replace-all and mask malformed traces — pin the current single-replace as the correct contract); the single-element `summary` join with no trailing separator (the empty-Vec case was pinned by `summary_renders_read_filter_label_and_empty_layers`; this fills in the single-element boundary against a refactor that built the string with a manual `for { push(',') ... }` loop); the symmetric `mark_read_filter(blocked=true, matched_policy_id=None)` arm — the dashboard distinguishes per-policy hits from global-scanner hits on the presence of `matched_rule_id`, and a refactor that unwrap_or-defaulted to `"unknown"` would silently merge the buckets. Files:

- [crates/proxy/src/adapters/policy_trace.rs](../../crates/proxy/src/adapters/policy_trace.rs) — 6 new tests. `summary_renders_unknown_for_failed_layer_with_no_error_code` (constructs a hand-rolled LayerOutcome with passed=false + error_code=None and pins the `=unknown` fallback), `mark_layer_a_failed_always_sets_matched_rule_id_to_none` (pins the hardcoded-None contract), `mark_read_filter_passed_arm_carries_none_error_code_with_detail_present` (pins the four-axis passed-with-detail shape), `set_layer_replaces_only_first_matching_entry_when_duplicates_exist` (pins single-replace semantic against a deliberate-duplicate trace), `summary_single_failed_layer_renders_without_trailing_separator` (single-element join boundary), `mark_read_filter_blocked_with_none_policy_id_omits_matched_rule_id` (symmetric None arm).

proxy bin tests: **724 → 730**. policy-engine lib tests unchanged at 167. Cumulative across rounds 1–87: proxy **+573**, cli +17, policy-engine +109, shared-types +13 — **712 new pure-helper unit tests** total. Gate floors unchanged (workspace 45/55, per-crate proxy 45 / shared-types 95 / policy-engine 88 / cli 10). `cargo fmt --all --check`, `cargo clippy --workspace --all-targets --locked -- -D warnings -A clippy::type_complexity -A clippy::too_many_arguments -A clippy::result_large_err`, `cargo test --workspace --locked`, and `RUSTFLAGS="-D warnings" cargo build --workspace --release --locked` all green.

**2026-05-18 (round 88) — 6 more pure-helper tests on oauth/error OAuthError Display tightening + previously-uncovered variant Display pins (the OAuth handler's error enum's remaining Display gaps after round 79's body-boundary sweep).** [crates/proxy/src/oauth/error.rs](../../crates/proxy/src/oauth/error.rs) had 13 tests covering `status()`, `body().code`, `body().detail`, the BadRequest/Internal/Upstream body boundaries (round 79), and an existing `oauth_error_display_strings_pinned_for_log_filters` test that pinned five variants via `.contains(...)` substring checks — but four variants had NO Display assertion at all (`BadRequest`, `BridgeRejected`, `Db`, `Crypto`, `Upstream`'s Display string) and the five substring-pinned variants were vulnerable to a refactor that prepended a wrapper prefix or trailing punctuation (which `.contains()` would silently allow). Operator log filters key on these prefixes to bucket OAuth-handler faults across multiple Grafana panels; tightening the assertions to `assert_eq!` against the full Display shape catches the "softened to consistent prefix" refactor class. Six surfaces pinned: `BadRequest(String)` — full `"invalid request: {0}"` shape (the title in `body()` is `"invalid request"` but the wire `code` is `"bad_request"`; pin the Display matches the title-form, NOT the code-form, so a "consistency with code" refactor surfaces); `BridgeRejected(String)` — `"federation token rejected: {0}"` prefix had ZERO test coverage (operator dashboards bucket federation-bridge JWT validation failures on the `"federation token"` qualifier separately from upstream-call and PIC monotonicity failures); `Db(#[from] sqlx::Error)` — `"database error"` no-leak contract (symmetric to round 79's `Internal` mask: the inner sqlx::Error Display carries schema/query/constraint internals — pin the wrapper Display does NOT contain the sqlx "no rows" substring); `Crypto` unit variant — fixed `"crypto failure"` two-word Display (no detail; pin so a refactor that promoted to `Crypto(String)` for "consistency with the detail-carrying arms" surfaces); `Upstream(#[from] reqwest::Error)` — `"upstream call failed"` URL-no-leak contract (symmetric to round 79's body-boundary pin; constructed against a real reqwest::Error with a sentinel `secret-path` substring and `hunter2` credential, pin both are absent from the wrapper Display); five substring-pinned variants (`PkceFail`/`SessionGone`/`UnknownClient`/`BadAuthCode`/`PicInvariant`) tightened to full `assert_eq!` shapes so a refactor that prepended `"oauth: "` or appended `"."` would surface (the existing `.contains()` checks would silently allow either). Files:

- [crates/proxy/src/oauth/error.rs](../../crates/proxy/src/oauth/error.rs) — 6 new tests. `oauth_error_bad_request_display_carries_invalid_request_prefix_with_inner_string`, `oauth_error_bridge_rejected_display_carries_federation_token_rejected_prefix`, `oauth_error_db_display_does_not_carry_inner_sqlx_string` (no-leak symmetric to Internal), `oauth_error_crypto_display_is_fixed_no_detail_form`, `oauth_error_upstream_display_masks_inner_reqwest_url_for_no_secret_leak` (URL no-leak via real reqwest::Error with sentinel `secret-path`/`hunter2`/`192.0.2.1` substrings asserted absent), `oauth_error_display_tightened_for_five_substring_pinned_variants` (PkceFail/SessionGone/UnknownClient/BadAuthCode all unit-shaped via `assert_eq!`; PicInvariant via the full prefix+inner shape).

proxy bin tests: **730 → 736**. policy-engine lib tests unchanged at 167. Cumulative across rounds 1–88: proxy **+579**, cli +17, policy-engine +109, shared-types +13 — **718 new pure-helper unit tests** total. Gate floors unchanged (workspace 45/55, per-crate proxy 45 / shared-types 95 / policy-engine 88 / cli 10). `cargo fmt --all --check`, `cargo clippy --workspace --all-targets --locked -- -D warnings -A clippy::type_complexity -A clippy::too_many_arguments -A clippy::result_large_err`, `cargo test --workspace --locked`, and `RUSTFLAGS="-D warnings" cargo build --workspace --release --locked` all green.

**2026-05-18 (round 89) — 6 more pure-helper tests on policy-engine yaml schema PolicyDoc Clone + override-rename + empty-array + default_pic_mode + empty-list Some-vs-None + closed-enum reject.** [crates/policy-engine/src/yaml.rs](../../crates/policy-engine/src/yaml.rs) already had 17 tests covering each individual field's parse path, default-value helper symmetry (Mode + QuarantineActionCfg), the kebab/snake wire-string distinction, and the burst-cfg empty-object boundary — but six operator-actionable surfaces remained unpinned: the PolicyDoc Clone derive across every optional field together (the dashboard's policy-list endpoint clones for the wire-serialize, and a refactor that switched any field to Cow or dropped the derive would surface as a borrow-checker rewrite at call sites rather than at a single trait-bound assertion); the `#[serde(rename = "override")]` attribute on `override_` (Rust's reserved-keyword escape — pin BOTH directions so a refactor that dropped the rename would silently start emitting `override_` on serialize and rejecting `override` on deserialize, fail-parsing every existing operator-authored policy); the empty top-level YAML array (`[]`) is a VALID input that returns an empty Vec (the empty-engine boot path `Engine::new("[]")` depends on this — a refactor that required at least one policy would silently break every test fixture and dev-default); the `default_pic_mode` helper's direct return value (symmetric to the existing `default_quarantine_action_helper_returns_replace_with_marker` test — the existing `defaults_match_safe_production_posture` test pins it but isn't dedicated, and the Audit default is the safe-posture contract a refactor flipping to RuntimeGate would silently break for every customer); the `to: []` explicit-empty-list distinction from None on `RecipientsCfg.to` (the notifier logic distinguishes "operator explicitly set empty — suppress all email to this list" from "inherit from global" — a refactor that collapsed empty to None for "treat empty as unset" would silently start email-blasting an operator who deliberately blanked a list); the `AuditBodyMode` closed-enum reject of unknown variants (symmetric to the existing `parse_policies_rejects_unknown_mode_value` on Mode — pin that `audit_body: hashing` typos and kebab/snake-mismatch values fail-parse rather than silently forward-compat into Hash, since silent compat would leave the privacy default in force without operator knowledge). Files:

- [crates/policy-engine/src/yaml.rs](../../crates/policy-engine/src/yaml.rs) — 6 new tests. `policy_doc_clone_preserves_all_optional_fields_independently` (round-trips a full-shape policy through Clone and verifies every optional field plus the inner serde_yaml::Value slots), `policy_doc_override_field_renamed_from_override_serde_attr_round_trip` (pins deserialize accepts `override` AND serialize emits `override` NOT `override_`), `parse_policies_empty_yaml_array_returns_zero_policies_without_error` (covers both `[]` and `--- []\n` renderings), `default_pic_mode_helper_returns_audit_directly` (symmetric to default_quarantine_action's dedicated pin), `recipients_cfg_empty_to_list_round_trips_as_some_empty_vec_distinct_from_none` (pins Some(empty) NOT None for explicit `to: []`, with cc/bcc covering the sibling shapes), `audit_body_unknown_variant_value_is_rejected_by_closed_enum` (pins both typo-extra-suffix and kebab/snake-mismatch failures).

proxy bin tests unchanged at 736. policy-engine lib tests: **167 → 173**. Cumulative across rounds 1–89: proxy +579, cli +17, policy-engine **+115**, shared-types +13 — **724 new pure-helper unit tests** total. Gate floors unchanged (workspace 45/55, per-crate proxy 45 / shared-types 95 / policy-engine 88 / cli 10). `cargo fmt --all --check`, `cargo clippy --workspace --all-targets --locked -- -D warnings -A clippy::type_complexity -A clippy::too_many_arguments -A clippy::result_large_err`, `cargo test --workspace --locked`, and `RUSTFLAGS="-D warnings" cargo build --workspace --release --locked` all green.

**2026-05-18 (round 90) — 6 more pure-helper tests on oauth/bridge validate_federation_token payload-only contracts + FederationClaims Debug + state field round-trip.** [crates/proxy/src/oauth/bridge.rs](../../crates/proxy/src/oauth/bridge.rs) already had 15 tests covering the happy path + the three operator-visible failure modes (malformed-jwt-segments / bad-base64 / bad-claims missing-field) + clock-skew boundaries on `iat` and `exp` + the `iss`/`pca_0_cbor_b64` Option round-trips + `infer_idp` classifier across known/secondary/mixed-case/fallback shapes, but six surfaces remained unpinned. The module's docstring commits explicitly to "payload-only" validation as the M0/M1 stub contract (signature step stubbed so the swap to JWKS-backed verify is one function) — pin that contract end-to-end so a refactor that started inspecting the header or signature segments would surface here rather than only at downstream call sites. On `validate_federation_token`: the value-validation deserialize fault path (invalid uuid in `pca_0_id` rejects with `"bad claims"` — distinct from the existing missing-field test; a refactor that swapped to `pca_0_id: String` with deferred runtime parse would silently start accepting garbage); the header-segment ignore contract (three distinct header shapes — random b64, empty string, raw garbage — all MUST accept because the function never inspects the header); the signature-segment ignore contract (symmetric — three distinct signature shapes all accept); the extra-segments contract (5-part JWT with 4 dots accepts because the `(parts.next(), parts.next(), parts.next())` destructure takes only the first 3 and leaves the rest unread — pin the current behavior so a future tightening that enforced exactly-3-segments is a deliberate change). On `FederationClaims`: the Debug derive carries `pca_0_id` (uuid hyphenated-lowercase form via Uuid's Debug==Display) + `p_0` principal + struct name — operator forensics on the OAuth callback path keys on these substrings; a manual Debug impl that hid either field "in the name of don't log PII" would silently break the join across request_id and bridge-decoded principal; the `state` field byte-identical round-trip across three operator-correlation-id shapes (uuid-style, mixed-case opaque, embedded-separators) — a refactor that normalized the value (lowercased / trimmed / URL-decoded) would silently break operator dashboards that join the proxy's request_id against the bridge's emitted state. Files:

- [crates/proxy/src/oauth/bridge.rs](../../crates/proxy/src/oauth/bridge.rs) — 6 new tests. `validate_federation_token_with_invalid_uuid_pca_0_id_carries_bad_claims_msg` (value-validation distinct from missing-field), `validate_federation_token_ignores_header_content_payload_only_validation` (3 header shapes, all accept), `validate_federation_token_ignores_signature_content_payload_only_validation` (3 signature shapes, all accept), `validate_federation_token_accepts_jwt_with_extra_segments_using_first_three` (5-part JWT documents current first-3 behavior), `federation_claims_debug_carries_pca_0_id_and_p_0_for_log_grep` (Debug rendering includes uuid + principal + struct name), `validate_federation_token_state_field_round_trips_verbatim_through_payload` (3 distinct state shapes, byte-identical).

proxy bin tests: **736 → 742**. policy-engine lib tests unchanged at 173. Cumulative across rounds 1–90: proxy **+585**, cli +17, policy-engine +115, shared-types +13 — **730 new pure-helper unit tests** total. Gate floors unchanged (workspace 45/55, per-crate proxy 45 / shared-types 95 / policy-engine 88 / cli 10). `cargo fmt --all --check`, `cargo clippy --workspace --all-targets --locked -- -D warnings -A clippy::type_complexity -A clippy::too_many_arguments -A clippy::result_large_err`, `cargo test --workspace --locked`, and `RUSTFLAGS="-D warnings" cargo build --workspace --release --locked` all green.

**2026-05-14 honest reset — floor lowered to 35% lines / 42% functions.** The original §6.2 ladder pinned the adoption floor at 60% / 60%, but a `cargo llvm-cov --workspace` run reports `TOTAL 36.94% lines / 43.94% functions` (40.22% is the *regions* metric — easy to misread; the `--fail-under-lines` flag checks the line column). The five most recent CI runs of `coverage.yml` on `main` all exited `failure` for exactly this reason — the gate was red the day it landed (b7d618b) and stayed red across every subsequent push. The floor is now `--fail-under-lines 35 --fail-under-functions 42`, just under the measured numbers, so the gate enforces a no-regression line that the workspace actually clears. The biggest pull-down sources are [crates/proxy/src/api/](../../crates/proxy/src/api/) (handlers at 0% — exercised only by integration tests; the `crates/proxy/tests/` directory is empty), [crates/proxy/src/server.rs](../../crates/proxy/src/server.rs) at 0%, and [crates/cli/src/main.rs](../../crates/cli/src/main.rs) at 3.91%. Backfilling those is the work that earns the next bump.

**Ratchet plan.**

The revised phase ladder lives only in this YAML's `--fail-under-*` flags. Bump it in a single-line PR alongside the test backfill that earned the bump — never bump speculatively. Updated targets:

| Phase | Lines | Functions | What earns the bump |
|---|---:|---:|---|
| adoption | 35% | 42% | n/a — 2026-05-14 measured baseline |
| round-7 (today) | 45% | 55% | ✅ 158 pure-helper unit tests across proxy + cli + policy-engine + shared-types (rounds 1–7) lifted measured coverage to **47.72% lines / 56.67% functions** |
| later | 70% | 70% | wiremock+postgres harness lands → `crates/proxy/tests/` exercises `api/*` handlers; CLI integration tests + `server.rs` boot-path tests |

The 60 → 70 → 80 ladder in §6.2 is left as the original *aspirational* document — this status block is the operational source of truth.

**Deviations from §6.2 sketch.**

1. ~~**No per-crate thresholds.** `cargo-llvm-cov`'s `--fail-under-*` is workspace-wide. Per-crate enforcement would need a `cargo llvm-cov report --output-format json` post-processing step. Holding off until the workspace floor is so high that the lowest-coverage crate is dragging it down.~~ **Resolved 2026-05-16.** [scripts/coverage-per-crate.sh](../../scripts/coverage-per-crate.sh) parses the same `coverage.json` the workflow already emits (jq + awk; bash-3 compatible so it runs on a macOS dev laptop as well as ubuntu-latest CI) and enforces one floor per workspace crate: **shared-types 95% / policy-engine 88% / proxy 45% / cli 10%** (each ~3–5 points below measured at the time of landing). Wired into [.github/workflows/coverage.yml](../../.github/workflows/coverage.yml) as a separate step after the workspace `--fail-under-*` gate, so the operator sees which crate slipped rather than just "workspace dropped". The workspace gate stays as the wider safety net; the per-crate gate catches narrower drops (e.g. shared-types or policy-engine regressing alone while proxy's denominator masks the loss). Failure-path verified locally by mutating one crate's `lines.covered` to 0 in a copy of the JSON and re-running the script — exits 1 with the failing crate name in stderr.
2. **No Codecov / coveralls integration.** The lcov artifact is uploaded; a downstream uploader can be wired in via a follow-up workflow without touching the gate itself.

---

## 7. Rollout order & dependencies

Suggested sequence (each step is independently shippable):

1. **§4 ErrorCode registry** — smallest, unblocks §3. ~1 day. ✅ shipped 2026-05-12.
2. **§3 PolicyTrace** — depends on §4. ~3 days incl. dashboard wiring. ✅ shipped 2026-05-12 (types + engine entry + adapter wiring; `X-Proxilion-Trace-Id` surfaced on responses).
3. **§5 PolicyLoader trait + cache** — independent of §3/§4 but easier to test once trace exists. ~3 days. ✅ shipped 2026-05-12 (`FilePolicyLoader` is the production path; `DbPolicyLoader` is a one-line plug-in).
4. **§2 ConfigBuilder** — independent. Defer until embed API is on the roadmap; until then, env-only is fine. ~2 days. ✅ Phases 1, 2, & 3 shipped (Phases 1 & 2 on 2026-05-12 — builder + `from_file` (TOML) + `Config::load()` precedence chain wired into `main.rs`; Phase 3 on 2026-05-13 — `Config::from_env()` removed, callers fully migrated).
5. **§6 Coverage gate** — adopt at the *current* level immediately; ratchet over months. ✅ Phase 1 shipped 2026-05-12 (originally 60% / 60%; honest reset on 2026-05-14 to **35% lines / 42% functions** in [.github/workflows/coverage.yml](../../.github/workflows/coverage.yml) — the original floor was set above measured reality and the gate was red on `main` from day one; see §6.4 status).

§3 and §4 should land in the same PR if possible — `LayerOutcome` references `ErrorCode` directly.

---

## 8. Out of scope (explicit non-goals)

Patterns from qiuth that look interesting but **do not** belong in Proxilion:

- **Validator pipeline (IP / TOTP / Certificate / HMAC).** Proxilion doesn't do factor-based auth on inbound; PIC chain verification is the equivalent and is already pluggable via the Trust Plane.
- **Zero-deps philosophy.** Qiuth's `0 deps` is a TS-library virtue. Proxilion is a service with a deliberately heavy stack (Axum, sqlx, regorus, rustls). Don't optimize for a metric that isn't ours.
- **Drop-in framework middleware for Express/Fastify/Koa/Hono.** Proxilion is single-runtime (Axum). The qiuth pattern of "one trait, four adapters" only pays off if you ship to multiple frameworks.
- **Fluent builder for everything.** The §2 builder is for `Config` specifically. Don't reflexively builder-ize `Decision`, `PolicyTrace`, or per-request structs — they're constructed once and don't need the ceremony.
- **CLI for credential generation.** Qiuth's `src/cli/generate.ts` makes sense for an operator-distributed library. Proxilion's operator entry point is the dashboard + `proxilion-ctl` (if/when), not a separate generator binary.

---

## Appendix — File references summary

**Qiuth source of truth:**
- Config builder: [qiuth-main/src/config/config-builder.ts:25-279](../../qiuth-main/src/config/config-builder.ts#L25-L279)
- Authenticator pipeline: [qiuth-main/src/core/authenticator.ts:61-131](../../qiuth-main/src/core/authenticator.ts#L61-L131)
- Lookup function type: [qiuth-main/src/middleware/express.ts:43-45](../../qiuth-main/src/middleware/express.ts#L43-L45)
- Coverage config: [qiuth-main/vitest.config.ts](../../qiuth-main/vitest.config.ts)

**Proxilion targets:**
- Config to refactor: [crates/proxy/src/config.rs:51-102](../../crates/proxy/src/config.rs#L51-L102)
- Decision to extend: [crates/policy-engine/src/decision.rs:5-20](../../crates/policy-engine/src/decision.rs#L5-L20)
- Errors to formalize: [crates/proxy/src/adapters/error.rs:9-103](../../crates/proxy/src/adapters/error.rs#L9-L103)
- Policy engine to wrap: [crates/policy-engine/src/lib.rs](../../crates/policy-engine/src/lib.rs)
